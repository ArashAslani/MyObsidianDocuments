تکنولوژی پیشنهاد شده توسط مایکروسافت برای کار با دیتابیس هست.
ORM (Object Relational Mapper) مایکروسافت هست.
وظیفه ایجاد اتصال و کار با دیتابیس را دارد.

چرا EF?
سازی سازی کدهای لایه دسترسی به داده ها
سپردن دیتابیس، اتصال دستورات به EF
افزایش بهره وری
ایجاد یک راه حل سازگار و هماهنگ
نوشتن query ها با LINQ
عدم وابستگی به دیتابیس مقصد
تمرکز بر روی Domain و قوانین کسب و کار

NuGet چیست؟
مدیریت بسته های پلت فرم توسعه ماکروسافت را به عهده داره.

Ef از نسخه 6 به بعد اپن سورس شد.

رویکرد های استفاده از EF:
Model first روشی هست مانند database First هست فقط محیط طراحی UML را به ما میدهد.
![[Pasted image 20240220040048.png]]

کدام روش را انتخاب کنیم؟
![[Pasted image 20240220040234.png]]

کلاس اصلی EF کلاس DbContext هست به خاطر همین در نمونه سازی از ApplicationDbcontext ها از نام آن استفاده میشود.

به کلاس هایی در روند database first ایحاد میشوند و با دنیای بیرون ارتباطی ندارند و صرفا کلاس هایی با پروپرتی هایی جداول و ارتباط بین آن ها هستند به اصطلاح POCO میگویند. Plain old clr object
در زمان code فرصت کلاس های پوکو را خودمان ایجاد میکنیم.

قسمت 2 شامل کار با دیاگرام های edmx هست.
پشت زمینه فایل های Edmx مملو از کد های Cml هست که مشخصات پروپرتی های جداول و نوع ارتباط یا association های آنها مشخص شده.
در قسمت StorageModel مشخصات دیتابیس و راه ارتباطی مشخص میشود.
ConceptioalModels بخش معرفی موجودیت ها و فیلد های جداول ماست.
و زمان اتصال به دیتابیس DDl تولید میشود. تا روی دیتابیس اجرا شوند و جداول تولید شوند. و زمان تغییر تمام ارتباطات و جداول از بین میرود و دوباره کد تولید میشود تا دیتابیس ساخته شود.
اما اگر داخل sql سرور تغییرات اعمال شود و در ویژیال استودیو تغییرات را اپدیت کنیم مشکلی برای از بین رفتن اطلاعات نداریم.
نمای داخل ویژیال استودیو
![[Pasted image 20240223185022.png]]

Code first :
دیتابیس از قبل وجود ندارد و کار از نوشتن مدل های Domain شروع میشود. 
این کلاس ها ربطی به EF ندارند.
Ef یک Context دراد که تبادلات بین کلاس ها و دیتابیس را مدیریت میکند.
مدل ها بر اساس قرار دادها و روابط بین کلاس ها در حافظه ساخته میشوند. مدل های ایجاد شده قابل مشاهده نیستن مگر با یک ابزار خاص.

برای ارتباط با دیتابیس  در لایه DataLeyer یا DataAccess در رفرنس ها Ef را به پروژه اضافه میکنیم.
کلاس Context را میسازیم و با using system.data.entity کلاس را DbContext ارث بری میکنیم.
با معرفی پراپرتی DbSet<> موجودیت مورد نظر را به ان معرفی میکنیم.
میتوان برای بعضی DbSet ها میتوان پراپرتی را تعریف نکرد.
DbSet امکان عملیات مجموعه ای را به ما میدهد. مانند کوئری گرفتن از دیتابیس و اعمال عملیات حذف و اضافه و ....

Migration بحثی هست که پروژه های Code first برای ایجاد و اعمال تغییرات در دیتابیس از ان بهره می برند. زمانی که با  add-migration filan شروع به ساختن migration ها میکنیم.
در یک پوشه به اسم Migrations کلاس Configuration که از DbMigrationsConfiguration<MyContext< ارث بری میکند.
این کلاس در سازنده AutomaticMigrationEnable = false دارد

Update database
زمانی که این دستور را با -verbose میزنیم تمام جزئیات ساخت دیتابیس و جداول را نشان میدهد.
  -script  به ما اسکریپت TSql جدول را در یک فایل میسازد و میدهد.
  و تغییرات را در دیتابیس اعمال نمیکند تا TSql ساخته شده را خودمان در دیتابیس اجرا کنیم.
  همین طور که میتوان دید جدول migration را نیز خود ef کنترل میکند و به ازای اپدیت و تغییرات جدید به ان insert میکند. Model یک هش از تغییرات هست که با استفاده از همین مقدار و مقایسه آن با مقدار هش های گذشته متوجه ایجاد تغییرات میشود.
  productVersion هم شامل ورژن پروژه و اعداد تصادفی هست.
  ![[Pasted image 20240224152058.png]]

App.Config یک فایل هست که خود Ef اون رو میسازه و مشخصات اتصال و خوده Ef را درون خودش نگه میدارد.
تا بتواند روی دیتابیس مورد نظر اسکریپت ها و عملیات مورد نظر را ایجاد کند.
![[Pasted image 20240224152505.png]]

پکیج Entity Framework power tools Beta 4
یک ابزار برای راحتی کار در پروژه های کد فرست هست. که عملیات ریورس اینجینیرینگ هم انجام میدهد. یعنی از روی جداول میتوان کلاس های C# را بسازد.
که از 2013 اپدیت نشده و برای VS ها 2013 به بالا باید تغییراتی در ان انجام شود.

نکته در Entity هایی که شامل کالکشن و رفرنسی به entity دیگر هست بهتر هست داخل کانستراکتور کالکشن هارا new کنیم که گاها خواستیم روی جداول foreach بزنیم به NullReference بر نخوریم.

![[Pasted image 20240224161534.png]]

Fluent Api
![[Pasted image 20240224170337.png]]

![[Pasted image 20240224170406.png]]

![[Pasted image 20240224170435.png]]

DbContext ovveride
onModelCreating : که داخل ان Model builder وجود دارد و میتوان در آن تنظیمات Fluent Api را نوشت.

در صورتی که در یک فایل جداگانه میخواهیم از قابلیت Fluent Api استفاده کنیم. کلاس Mapping ما باید از EntityTypeConfiguration<EntityName< ارث بری بکند. که باید کانستراکتور بدون مقدار در نظر گرفت. و چون modelBuilder و HasEntity با پاس دادن به کلاس پدر در نظر گرفته شده میتوانیم از آنها در سازنده صرف نظر کنیم. و به نوشتن ویژگی ها بپردازیم.
![[Pasted image 20240224171355.png]]

برای معرفی این کلاس mapper باید آن را در OnModelCreating رجیستر یا معرفی کنیم.
![[Pasted image 20240224171518.png]]

استفاده از پکیج Entity Framework power tools Beta 4 در زمان reverse کردن دیتا از دیتابیس به مدل های poco دارای فولدر بندی های مناسب تر و کد ها تمیزتر هست.


Initialization Strategy
![[Pasted image 20240224172701.png]]

اولین عملکرد که عملکرد پیشفرض هم هست CreateDatabaseIfNotExist هست.
![[Pasted image 20240224172829.png]]

در تمام این استراتژی ها ممکن هست دیتا های ما ازبین بروند.
که بهترین روش استفاده از :
![[Pasted image 20240224172916.png]]
که با استفاده از Migration ها دیتا های قبلی را نگه داشته و تغییرات را اعمال میکند.

استراتژی وجود دارد که تمام تغییرات را مد نظر نمیگیرد و ایگنور میکند.
![[Pasted image 20240224173054.png]]

ویژگی مشترک بین این استراتژی ها ارث بری از IDatabaseInitializer<TContext< هست.

در اصل در این حالات میخواهیم زمانیکه کد را اجرا میکنیم تمام تغییرات مد نظر ما روی دیتابیس ایجاد شود.
پس نیاز به یک دستور اجرایی مانند .toList داریم تا ارتباط با دیتابیس از طریقی ایجاد شود و استراتژی های ما اجرا شود.
![[Pasted image 20240224173942.png]]
![[Pasted image 20240224174312.png]]

زمانی که میخواهیم فیلدی را حذف کنیم از جدول باید در Configuration مقدار AutomathicMigrationDataLossAllowed را فعال کنیم.

![[Pasted image 20240224174824.png]]

در متد Seed میتوان دیتاهایی را که در زمان ایجاد دیتابیس نیاز داریم قرار دهیم.


Migrations :
دادن نام به هر migration را میتوان با دستور Add-Migration migration Name" میتوان اینکار را انجام داد.
با اینکار یک اسنپ شات snapshot از مدل های کدفرست ایجاد میشود.
که شامل یک متد Up و متد Down هست.

در متد Up مشخصات ساخت جداول است. که در این متد تایپ های مخصوص EF وجود دارند تا جداول را بسازند.

در متد Down حذف اتفاق می افتد. اول  کلید های خارجی و بعد ایندکس ها و بعد جداول پاک میشوند.

در ورژن های جدید Enable-migration منسوخ شده است.

*در زمان هر درخواست با توجه به استراتژی پیشفرضی که در EF وجود دارد دیتابیس به وسیله تاریخچه مایگرشن ها چک میشود تا در صورت وجود تغییرات به دیتابیس اعمال شوند برای انجام نشدن این Transaction میتوان از استراتژی NullDatabaseInitializer استفاده کرد.

تراکنش ها جایی که saveChanges صدا زده میشوند commit میشوند.
استفاده از addRage بعد از ایجاد درست اطلاعات commit میکند اگر نه RoleBack اتفاق می افتد.


زمان واکشی اطلاعات ترنزکشنی در دیتابیس ایجاد نمیشود و صرفا یک query و نه non-query اجرا میشود.
دستورات non-Query جواب خاصی مانند دستورات query برنمیگرداند و در ado.net برای اعمال این دستورات از execute none query استفاده میشد.


زمانی که .tolist استفاده نمیکنیم برای واکشی اطلاعات و لیست مورد نظر را در loop میگذاریم تا اجرا شود متنها بعد از تمام شدن لوپ و نمایش دادن تمام اطلاعات connection بسته میشود.
در حالت درست connection بعد از زدن کوئری بسته میشود و نه بعد از چاپ تمام اطلاعات.

![[Pasted image 20240224193023.png]]

![[Pasted image 20240224193004.png]]

BadWay :
![[Pasted image 20240224193047.png]]
![[Pasted image 20240224193102.png]]


در Select معمولی و استفاده ساده از where در tsql شرط top(1) گذاشته نمیشود و با استفاده از first این شرط گذاشته میشود. 

زمانی که در یک condition از متغیر استفاده میکنیم query هم از پارامتر استفاده میکند اما اگر از متغیر استفاده نکنیم و مستقیما در شرط مقدار شرط را بگذاریم در شرط تولید شده از همان استفاده میکند.

![[Pasted image 20240224194047.png]]![[Pasted image 20240224194118.png]]
![[Pasted image 20240224194145.png]]

زمانی که بعد از .tolist مرتب سازی یا شرط میگذاریم تمام اطلاعات از دیتابیس به صورت یک سلکت ساده در مموری اورده میشود و شرایط اعمال میشود که روش بهینه ای نیست و بهتر هست قبل از .tolist اینکارا بکنیم تا در یک کوئری واحد و فقط رکورد هایی واجد شرایط به مموری اورده شوند.

استفاده از find() :
در این حالت اگر مقدار Id را که یک کلید اصلی هست و ویژگی منحصر به فرد بودن دارد را اگر در شرط یافتن بگذاریم در کوئری ایجاد شده Top(2) را خواهیم داشت چرا که در صورت وجود دوتا کاندید واجد شرایط به اکسپشن بخوریم و از نادرست بودن اطلاعات مطلع شویم.
نکته دیگری که استفاده از find وجود دارد این هست اگر رکورد واجد شرایط قبلا از دیتابیس واکشی شده باشد و در context وجود داشته باشد دیگر کوئری سمت دیتابیس نمی رود و کانکشنی ایجاد نمیشود و مستقیما ابجکت شناسایی و پاس داده میشود.

![[Pasted image 20240224195224.png]]![[Pasted image 20240224195237.png]]

استفاده از Take و skip و update
![[Pasted image 20240224195857.png]]![[Pasted image 20240224195915.png]]
![[Pasted image 20240224195934.png]]

در صورتی که دیتایی تغییر ندهیم و همین درخواست را دوباره تکرار کنیم به خاطر اینکه مدل ما تغییری نکرده به ازای اپدیت کوئری ایجاد نمیشود و Ef متوجه عدم تغییر میشود. و فقط واکشی اطلاعات اتفاق می افتد.

در صورت تغییر یک فیلد در کوئری به ازای همان مقدار تغییر کوئری ایجاد میشود:

![[Pasted image 20240224200259.png]]

این اتفاقات در حالت ویندوزی بود.
به پروژه های سمت وب Disconnected میگویند چرا که کانتکست های متفاوتی دارند.
![[Pasted image 20240224200749.png]]  ![[Pasted image 20240224200946.png]]

![[Pasted image 20240224201058.png]]
چون نمیداند چه پروپرتی تغییر کرده تمام مقادیر را گرفته و با اطلاعت جدید مقدار دهی میکند هرچند فقط یک فیلد تغییر کرده باشد.
در صورتی که state.modified باشد به صورت اتوماتیک Atach شدن و اتصال ابجکت به context اتفاق می افتد.

حذف اطلاعات با شرایط خاص:
cascade : حذف آبشاری
NoAction : حذف بدون ساید افکت

بازیابی اطلاعات بصورت Graph:

![[Pasted image 20240224203133.png]]

Eger Loading : یک بار درخواست برای یک مدل زده میشود و تمام مدل های مربوط به آن هم واکشی شود. چه نیاز داریم چه نیاز نداریم.
![[Pasted image 20240224204741.png]]
![[Pasted image 20240224205009.png]]
چون ممکن هست کاستومری وجود داشته باشد که کاستومر ما هیچ کانتکتی نداشته باشد نمیتواند inner join بزند.
![[Pasted image 20240224205313.png]]
![[Pasted image 20240224205355.png]]
![[Pasted image 20240224205431.png]]
![[Pasted image 20240224205501.png]]


Explicit Loading : بازیابی اطلاعات به صورت صریح میگوییم برای این ایتم خاص دیتای خاصی لود شود. و از قبل مانند Eger این کار را نمیکنیم.
![[Pasted image 20240224210915.png]]
load کار Execute را برای ما میکند.
![[Pasted image 20240224211114.png]]
![[Pasted image 20240224211042.png]]
Lazy Loading : بازیابی اطلاعات به صورت virtual که امکان این را میدهد که هر موقع نیاز هست دیتا واکشی شود. یعنی اگر کالکشن ما در موجودیت Virtual نباشد اطلاعات وابسته واکشی نمیشوند.
در حالت پیشفرض بدون استفاده از vitual اطلاعات وابسته لود نمیشود. 
![[Pasted image 20240224204222.png]]

اما با virtual
![[Pasted image 20240224211508.png]]
![[Pasted image 20240224211446.png]]
![[Pasted image 20240224211603.png]]
![[Pasted image 20240224211632.png]]
![[Pasted image 20240224211703.png]]
اگر مدل های وابسته صدا زده نشوند و کاری باش انجام ندهیم درخواست واکشی برای آن موجودیت وابسته زده نمیشود.
نکته ای وجود دارد این هست که فرضا یک جدول نمایش کاربران داریم که میخواهیم یکسری اطلاعات تماس هم از کاربر نمایش دهیم.
در این حالت به ازای هر رکوردی که داریم یک کوئری واکشی سمت سرور زده میشود و بار زیادی ایجاد میکند.

Projection : بازیابی اطلاعات به صورت anonymous Type یعنی فیلد هایی داریم که مختص به یک مدل نیستند.

![[Pasted image 20240224212313.png]]

![[Pasted image 20240224212557.png]]![[Pasted image 20240224212619.png]]



Mapping Stored Procedure :
![[Pasted image 20240224213931.png]]
![[Pasted image 20240224215144.png]]
یا اگر SP ها نام متفاوتی داشته باشند:
![[Pasted image 20240224215934.png]]
بعد
migration
![[Pasted image 20240224215221.png]]![[Pasted image 20240224215243.png]]
and another Sps
![[Pasted image 20240224215329.png]]

اگر Forigen key 
هارا از مدل وابسته حذف کنیم تا خود ef انها را بسازد شرایط مختلف وجود entity های وابسته چک میشود.
![[Pasted image 20240224215620.png]]![[Pasted image 20240224215646.png]]
قبل:
![[Pasted image 20240224215727.png]]

حل مشکل تطبیق نام پروپرتی و SP ها :
![[Pasted image 20240225140639.png]]
با استفاده از Navigation ها نیز میتوان تطابق نام foreign key هارو درست کرد.


Encapsulating Ef code
![[Pasted image 20240225153846.png]]
 به این جدا سازی Separation of concerns می گویند.
  
 ![[Pasted image 20240225154036.png]]
![[Pasted image 20240225154528.png]]

پترن unitOfWork در واقع نگهداری میکنه از لیستی از ابجکت که از بیزنس ترکزکشن تاثیر گرفتن و تمام تغییرات رو دنبال میکنه تا در نهایت همه رو باهم اعمال کنه. تا مشکل های concurrency رو هم حل کنه. و جفت میشه با Repository.
ترکیب ریپازیتوری و unitofwork تشکیل DbContext میدهند.
