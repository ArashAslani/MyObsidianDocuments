به زبان ساده کد تمیز به کدی گفته میشود که خوانا باشد و بتوان به راحتی آن را توسعه داد. Maintainable and Readable 
کد کثیف به مرور زمان یک پروژه استیک به جای میگذارد که هزینه سنگینی برای برنامه نویسان و سازمان به جای میگذارد.
به حدی که شاید منجر به تصمیم گیری به بازنویسی پروژه شود.
از قواعدی که باید مد نظر داشت این هست که در هربار اضافه کردن ویژگی و یا ریفکتور سعی کنیم کلاس بیسی که با آن کار میکنیم را تمیز تر کنیم.

قانون اول :  نام گذاری
	از نام گذاری مبهم و کم اطلاعات جلو گیری شود. 
	 پیشوند ها و پسوند ها
	نباید از پیشوند ها یا پسوند ها به صورت غیر ضروری استفاده کرد مثال: strName, intAge یا در کلاس Person فیلد PersonName نداشته باشیم. به طول کلی از Encoding های الکی دوری کنیم. مباحث بالا
	پرهیز از mental mapping یعنی چیز هایی که در ذهن من معنی میده ولی در حالت عادی معنی نمیدهد.
	اسم کلاس نباید به صورت فعلی باشد.
	از پسوند Manager و Processor , Provider, Handler, Factory نباید در یک سیستم استفاده شود چرا که تمایز قرار دادن بین این مفاهیم سخت است.
	کلاس ها باید عبارت های اسمی و متد های باید عبارت های فعلی داشته باشند.
	در کلاس هایی که چند کانستراکتور و مقدار های دیفالت وجود دارد میتوان به کمک static متد ها و نام گذاری واضح برای آنها و ریترن کردن کلاس تمایز بین ایجاد هر کلاس با کانستراکتور های متفاوت را متوجه شد.
	برای هر کانسپت یک کلمه خاص در نظر بگیرید.
	از نام های مطرح در طراحی نرم افزار و مربوط به بیزنس انتخاب کنیم.
	اسم های کوتاه به اسم های بلند ترجیح داده میشوند در صورتی که شفاف باشند.
	
قانون دوم فانکشن های تمیز :
	متد باید کوتاه باشد.
	در هر بلاک شرطی باید به طول خلاصه میتواند یک خط کد باشد. از گفتن قصه در توابع باید جلوگیری کرد.
	دائم باید ریفکتور کرد.
	فانکشن ها باید فقط و فقط یک کار انجام دهند.
	اگر نتوانیم فانکشن مستقلی از دل یک فانکشن بیرون بکشیم یعنی به این مفهوم رسیدیم.
	نکته: در صورتی که فانکشن ما reusability نمیشود پس نیاز به جدا سازی و اتمیک کردن نیست.
	قانون Step down room : میخواهیم در هر فانکشن با یک سطح از ابسترکشن مواجه شویم.
	 نشونه هایی که یک کد دارد چند کار انجام میدهد. switch case یکی از نشونه هایی که نشون میدهد یک متد دارد چندین کار انجام میدهد.
	 که میتوان از ابسترک فکتوری استفاده کرد. یعنی تمام این switch statement هارو در یک جا استفاده کرد.
	 
قانون سوم ارگمان های توابع:
	حالت ایده آل این هست که تابع هیچ ورودی نداشته باشد.
	چراکه خواندن پرامتر ها کار سختی هست.
	توابعی ورودی boolean دارن باید حواسمون باشه که در شرط مربوطه فقط یک خط فانکشن فراخوانی شود چرا که هرکدام یک کار بخصوص انجام میدهد.
	اگر متدهایی داشتیم که تعدادی از ورودی های آنها دائم تکرار میشوند. باید ورودی های تکرار شونده یک آبجکت شوند. که باعث کاهش ورودی ها میشود.

قانون چهارم ساید افکت:
	ساید افکت یک دروغ بزرگ هست. یعنی تابع ما قول داده یک کار انجام بده ولی درون خودش چندتا کار انجام میدهد.
	