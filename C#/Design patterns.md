دیزاین پترن ها :
1 - دیزاین پترن های ساختاری : این دیزاین پترن ها برای کار با ابجکت ها و نمونه های ساخته شده استفاده میشوند.
مناسب برای کار روی پروژه های بزرگ

2 - دیزاین پترن های ایجادی :

3 - دیزاین پترن های رفتاری :



  Wrapper Or Decorator (تزئین کننده) :
  مثلا یک کلاس داریم که میخواهیم به ابجکت های اون کلاس یکسری رفتار و ویژگی بدون دست کاری خود کلاس اضافه کنیم. مثلا یک کلاس برای رجیستر کردن کاربر داریم که میخواهیم در بعضی مواقع پس از رجیستر به کاربر پیام ارسال کنیم. در این شرایط میتوانیم از دکوراتور استفاده کنیم. حالا چرا کلاس اصلی را تغییر دهیم؟
  ممکن هست یک DDL داشته باشیم یعنی Component و ConcreteComponent را نداشته باشیم و نتوانیم رفتار کلاس های ان را تغییر دهیم. یا یک کلاس قدیمی داریم که نمیتوانیم رفتار های آن را به خاطر پیچیدگی عوض کنیم پس از دکوراتور یا رپر استفاده میکنیم.
  در .net 
  StreamReader
  StreamWriter
  StringReader
  StringWriter
  از این الگو استفاده شده.
  ![[Pasted image 20240416192322.png]]
component یک ابسترک کلاس هست که دارای یک متد پابلیک هست.
ConcereteComponent هرکلاسی که یک کلاس اینترفیس یا ابسترک را ارث میبرند میگویند.
Decorator یک ابسترک هست که ConcreteDecorator آن را پیاده سازی میکند.

کلاس Component میتواند ابسترکت یا اینترفیس باشد. 
تفاتی بین ConcereteComponent , ConcreteDecorator وجود ندارد.
ConcreteDecorator فقط رفتار جدیدی که به آن داده میشود را فقط میدادند. و از اینکه به چه چیزی دارد اضافه میکند اطلاعی ندارد. یعنی نمیداند کدام ابجکت را میخواهد تغییر رفتار بدهد.
اگر تغییر رفتار فقط ثابت هست میتوانیم از نوشتن ConcreteDecorator صرف نظر کنیم و کلاس Decorate را ابسترکت تعریف نکنیم و داخل خود آن تغییرات را لحاظ کنیم.
دکراتور میتواند داخل سازنده رفتار جدید را اعمال کنند.

کار هایی که تا الان با دکوراتور انجام دادیم میتوانستیم وراثت هم این کارو انجام دهیم.

![[Pasted image 20240417124807.png]]

نقاط قوت :
رعایت اصل SRP
انعطاف پذیری بالا میتوانیم به ازای هر قابلیت یک دکوراتور داشته باشیم
پیاده سازی با رایج ترین قابلیت های زبان برنامه نویسی
توسعه مستقل مستقل از دیگر دکوراتور ها
میتوان زنجیره ای از دکوراتور هارا ایجاد کنیم.

نقاط ضعف
امکان اعمال دکوراتور های تکراری وجود دارد یعنی متوجه این نیست که خودش متوجه تکرار دکوراتور روی یک ابجکت هست یا نه که میتوان با استفاده از بهینه سازی میتوان اضافه کرد
امکان عمل نکردن به LSP وجود دارد چرا که ممکن هست عملکرد و رفتار کلاس را عوض کنیم
مجبور به Override کردن تمام متد های کلاس کامپوننت هستیم.
نمیتواند قابلیت چند دکوراتور را به ارث ببرد به خاطر خاصیت تک ارث بری از کلاس در سی شارپ.
ممکن هست که برنامه نویس فراموش کند با بیس کار کند.
یدک کشیدن تمام ویژگی های کلاس Component در دکوراتور
غیر قابل استفاده برای کلاس های Protected

اگر سیستمی را داریم از ابتدا طراحی میکنیم بهتر است از الگوی استراتژی استفاده کنیم و اگر کدهای قدیمی داشتیم از دکوراتور استفاده کنیم.
ترجیحا کلاس های دکوراتور را سبک نگه داریم و برای هر ویژگی یک دکوراتور ایجاد کنیم.

الگوی های مرتبط با Decorator
Strategy
Composite



Facade Pattern : 
	پیاده سازی ساده
	عدم محدودیت در تعداد Facade
	واسط Facade به واسط زیر سیستم ها بستگی ندارد
	استفاده از Singlton و یا کلاس استاتیک برای Facade لازم هست.
	میتوانیم از Abstract برای Facade ها چندین پیازی دارند
	میتوانیم facade هارا لایه بندی کنیم
	سادی سازی کار با سیستم شلوغ
	نقاط قوت :
	1.برقراری اتصال سست در برنامه
	2.لایه بندی سیستم را برای ما انجام میدهد
	3.کلایت ها از جزئیات سطح پایین زیر سیستم ها خبر ندارند
	4.کاهش وابستگی ها - مثلا زمان تغییر در زیر سیستم ها فقط فساد تغییر میکند و لازم نیست لایه های دیگر دوباره کامپایل شوند 
	5.کاهش واستگی بین زیر سیستم ها
	6.اجبار استفاده از Facade برای کلاینت وجود ندارد
	7.معایبی ندارد ولی راهنمای جامعی برای پیاده سازی نداریم.
	چه زمانی استفاده کنیم :
	زمانی که زیر سیستم های زیادی داریم و لایه کلاینت باید نمونه سازی های بسیاری کنند از Facade استفاده میکنیم
	زمانی که میخواهیم کلایت ها در برابر تغییرات و توسعه زیر سیستم ها آسیب نبینند
	زمانی که نیاز به لایه بندی داریم
	-
	الگو های مرتبط Singleton - Abstract Factory
	-
	در رده بندی Structural patterns قرار میگیرد.
	فرض کنیم تعداد زیادی کلاس که ارتباط های بسیاری هم  داریم که یک فرد قرار هست با این کلاس ها کار کند. اگر فردی قرار باشد با این کلاس ها کار کند باید بیاییم یک راهنمای استفاده از این کلاس ها ایجاد کنیم.
	در واقع اگر با تعداد زیادی Subsystm سروکار داشته باشیم که برای یک کار مشخص استفاده میشوند به سراغ این پترن می اییم.
	![[Pasted image 20240417210240.png]]
	![[Pasted image 20240326111745.png]]
	![[Pasted image 20240326112021.png]]
مثال ماشین 
![[Pasted image 20240326113309.png]]
Start engine
![[Pasted image 20240326113342.png]]
Stopping Engine
![[Pasted image 20240326113409.png]]
Facade
![[Pasted image 20240326113503.png]]

and in client or controller just call facade methods
![[Pasted image 20240326113549.png]]

Decorator Pattern :
جزو دسته بندی Structural هست.
مثال : فرض کنید در یک کافی شاپ تعدادی نوشیدنی به صورت کافی سرو میشوند که میتوانند اسامی مختلفی داشته باشند.
دید ابجکت اورینتد : یک کلاس نوشیندنی در نظر میگیریم که میتواند شامل توضیحات و مبلغ این کلاس باشد.
به ازای هر کافی دیگر یک کلاس در نظر میگیریم که این ابسترک کلاس ارث بری میکند. حالا هر کلاس نوشیدنی متد ها اوراید میشوند و مبلغ مورد نظر برگردانده میشوند.
![[Pasted image 20240328151654.png]]
وقتی وارد کافی شاپ میشیم. یک اتفاق هم می افتد در منو هر نوشیدنی میتواند با یک چاشنی ادقام بشوند که هرکدام یک هزینه متفاوت دارد.
حالا میخواهیم قیمت انواع مختلف کافی با چانی های مختلف را داشته باشیم.
![[Pasted image 20240328151728.png]]
حالا راه حل های مختلفی داریم :
یا میتوانیم تمام حالات را به صورت کلاس در نظر بگیریم. که انفجاری از کلاس هارا رقم میزنیم.
![[Pasted image 20240328151804.png]]
یا میتوانیم کلاس کافی را شامل این چاشنی ها کنیم و برای هر چاشنی یک متد و پروپرتی True,False در نظر بگیریم.
که دیگر کلاس کافی نمیتواند ابسترک باشد و خود دارای بدنی هست.
![[Pasted image 20240328151825.png]]

![[Pasted image 20240328151848.png]]

Beverage : اسم کلاس رو کافی در نظر گرفتم. راحت بنویسم.
حالا هر متد کاست در کلاس های Drived باید دو کار را انجام دهد. یکی وضعیت چاشنی و دو محاسبه قیمت نهایی را محاسبه کنیم.
مشکلات این روش: اگر قیمت چاشنی های تغییر کند؟ باید بریم در کل کد قیمت هارو تغییر دهیم.
حالا اگر دبل موکا بخواهیم چی؟ چون یک فیلد داریم اگر درست باشد یعنی موکا داریم اگر نه یعنی موکا نیست، دبل موکا چطور محاسبه میشود.
بعضی از کافی ها ممکن هست اصلا با کافی مد نظر همخوانی نداشته باشند. درصورتی که مجبور هست همه چاشنی را در خود داشته باشد.
حالا اگر چاشنی جدیدی اضافه شود چی؟
باید فیلد و قیمت ان اضافه شود و کد تغییر میکند.

یا روش حل 
Decorator Pattern ?
فرض کنیم یک نوشیدنی دارک روست میخواهیم که خودش یک متد Cosnt داره که بتونه هزینه نوشیدنی رو محاسبه کنه.
و همچنان این کلاس از کلاس کافی ارث بری که که متد ابسترک cost را دارد.

حالا نیاز میشه یک چاشنی موکا هم به این نوشیدنی اضافه شود. که خود این کلاس موکا یک کاست داره که قیمت موکارا حساب میکند. یعنی عملا چاشنی موکا هم از کلاس کافی ارث بری میکند.
![[Pasted image 20240328152734.png]]
![[Pasted image 20240328152753.png]]
![[Pasted image 20240328152810.png]]
حالا نوشیدنی سفارشش ثبت شده و باید مبلغ مورد نظر محاسبه شود :
![[Pasted image 20240328152912.png]]
عملا کاری که EF برای اعمال اکستنشن متد ها میکند.
در این حالت هر کالتی که داریم زمان ران تایم اتفاق میافتد و قیمت محاسبه میشود.

![[Pasted image 20240328153407.png]]
![[Pasted image 20240328153446.png]]
![[Pasted image 20240328154423.png]]
ConcereteComponent :
کانستراکتور ورودی ندارد و باید اخر از همه به عنوان بیس صدا زده شود.
![[Pasted image 20240328154518.png]]

CondimentDecorator چاشنی ها :
![[Pasted image 20240328154620.png]]

Decorators :
![[Pasted image 20240328154827.png]]
که در زمان صدا زدن از داخلی ترین نمونه به صورت برگشتی مقدار Description , Cost  محاسبه میشوند و نمونه ها جایگذاری میشوند.
![[Pasted image 20240328155732.png]]


مثال دیگر رزو بلیط هواپیما :
![[Pasted image 20240328162907.png]]برای هر سفر ممکن هست امکانات مختلفی را در سفر در نظر بگیرند.
مثلا وافای دیدن تلویزیون و یا هدست.
در این حالت یک اینترفیس تعریف شده که دو متد امکانات و قیمت اورده شده.
![[Pasted image 20240328163038.png]]
یک کلاس ابسترک در نظر گرفته شده برای امکانات که ورودی آن اینترفیس هست.

![[Pasted image 20240328163112.png]]

حالا امکانات به این صورت پیاده سازی میشوند.
![[Pasted image 20240328163238.png]]
در کلاس Program یا محل فراخوانی

![[Pasted image 20240328163308.png]]



Bridge
هدف ارائه این هست که انتزاع از پیاده سازی جدا شده و توسعه داده شده.
یعنی میتوانیم به ازای یک کلاس چندین پیاده سازی داشته باشند.
اشکال ان وابستگی به کلاس ابسترک هست که در زمان تغییر باید تمام پیاده سازی ها تغییرات را اعمال کنند و نمیتوان مستقلا اجرا شوند.
الگوی Bridge برای رفع این مشکل به وجود امده تا کلاس های Concrete مشکلی نداشته باشند.
باید دو کلاس ابسترک داشته باشیم. تا یکی فضای بیرونی و یکی فضای داخلی را کنترل کنن.

نکاتی در مورد Bridge

