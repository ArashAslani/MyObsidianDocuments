===========================================================
HANDOVER PACKAGE
HyperAhan - Catalog and Pricing Modules, DDD/Business Analysis Engagement
===========================================================

PURPOSE OF THIS PACKAGE
This package is written for an AI or analyst who has NEVER seen the original
conversation between the business owner (Arash, Foolad Shahyar Sepand) and the
prior analyst. It contains everything required to continue the engagement from
exactly the point it was paused, with zero information loss. Nothing here has
been summarized, condensed, or rewritten from its original form - source
documents are reproduced verbatim, and all four analysis deliverables are
included in full, exactly as they were produced.

HOW THIS PACKAGE IS ORGANIZED (read in this order)
PART 1 - Original Source Document A (the primary, authoritative business
         document from Foolad Shahyar Sepand). Read this first; it is the
         ground truth for all business rules.
PART 2 - Original Source Documents B through G (competitor reference data
         from AhanOnline, MarkazAhan, EsfahanAhan). Read second. These are
         NON-AUTHORITATIVE background/reference material for Foolad Shahyar
         Sepand's own business, EXCEPT for the one specific point explicitly
         promoted to a confirmed fact (see Decision D-14 in Part 4: overall
         product-domain breadth). Do not treat anything else in Part 2 as a
         business rule for Foolad Shahyar Sepand unless the business owner
         explicitly confirms it, exactly as happened once already.
PART 3 - Project Knowledge Base. Full extraction of confirmed facts, business
         rules, invariants, terminology, unresolved questions, resolved
         contradictions, architectural decisions, and rejected assumptions,
         organized by domain.
PART 4 - Decision Log. All 31 individually-evidenced decisions (D-01 through
         D-31), each with Decision ID, Decision, Reason, Business Evidence,
         Alternatives Considered, Confidence, Current Status. Includes two
         explicit regression/correction records.
PART 5 - Ubiquitous Language. Complete glossary of every business concept,
         with meaning, relationships, business owner, Bounded Context (where
         known), and synonyms.
PART 6 - Project State. Exactly where the engagement stopped, what is locked,
         what is open, the current step, the next step, and the full
         remaining roadmap (Steps 7-11).

IMMEDIATE ACTION FOR WHOEVER CONTINUES THIS PROJECT
Do not begin any new analysis until Parts 1-6 have been read in full. Then
resume at Step 7 of the approved 11-step execution plan (see Part 6, Section 9
"NEXT STEP" for exact instructions). Continue applying the same working method
described at the top of Part 6, Section 1, without exception: reality over
assumptions, explicit fact/inference/unknown separation, stop-and-ask on any
contradiction or ambiguity, business modeling strictly before any technical or
architectural decisions, and explicit regression checks (with full "what
changed / why / which conclusion is now invalid" logging - never silent
overwrites) before finalizing any future step.

===========================================================
PART 1 BEGINS BELOW
===========================================================
===========================================================
ORIGINAL SOURCE DOCUMENT A (PRIMARY, AUTHORITATIVE)
Business document from Foolad Shahyar Sepand
Version 1.0 | Date 1405/05/01 | Status: Final draft for system design start
Reproduced verbatim as submitted in the original conversation.
===========================================================

نسخه: ۱.۰ | تاریخ: ۱۴۰۵/۰۵/۰۱ | وضعیت: پیش‌نویس نهایی برای شروع طراحی سیستم

---

## ۰. چرا این سند نوشته شد

این سند حاصل چند دور بحث و بررسی رقبا (آهن‌آنلاین، مرکزآهن) و همچنین بررسی فاکتورها و سفارش‌های واقعی مشتریان فولاد شهیار سپند است. هدف این است که قبل از شروع طراحی فنی سیستم (دیتابیس، API، پنل ادمین)، منطق واقعی کسب‌وکار به‌صورت شفاف و بدون ابهام مستند شود. جاهایی که هنوز تصمیم قطعی گرفته نشده، صریحاً به‌عنوان ابهام باز علامت‌گذاری شده‌اند تا کسی فرض اشتباه نگیرد.

---

## ۱. دسته‌بندی محصولات بر اساس نحوه‌ی فروش

این مهم‌ترین تصمیمی است که کل معماری سیستم روی آن بنا می‌شود. محصولات فولادی از نظر نحوه‌ی فروش به سه دسته‌ی کاملاً متفاوت تقسیم می‌شوند:

### دسته ۱ — محصولات استاندارد، کاملاً آنلاین
میلگرد، ورق، نبشی، ناودانی، پروفیل و مشابه. سایز و مشخصات استاندارد بازار دارند. قیمت مستقیماً در سایت نمایش داده می‌شود، مشتری می‌تواند بدون تماس، سفارش دهد، سبد خرید تشکیل دهد و پرداخت آنلاین انجام دهد.

### دسته ۲ — محصولات نیمه‌سفارشی (نمونه: تیرآهن)
سایز پایه استاندارد است (مثلاً شاخه‌ی ۱۲ متری) و قیمت پیش‌فرض همان سایز استاندارد در سایت نمایش داده می‌شود و قابل خرید آنلاین است. اما اگر مشتری بخواهد آن را سفارشی کند (طول متفاوت، برش خاص)، باید تماس بگیرد. یعنی سایت همیشه یک مسیر آنلاین پیش‌فرض دارد، به‌علاوه‌ی یک مسیر تماسی برای حالت سفارشی.

### دسته ۳ — محصولات کاملاً سفارشی (نمونه: والپست، بولت)
این محصولات از مواد خام (میلگرد، ورق و...) با اضافه‌شدن هزینه‌های جانبی (جوشکاری، خم‌کاری، دستمزد کارگر) ساخته می‌شوند. قیمت از پیش مشخص نیست و کاملاً به سفارش و تماس تلفنی بستگی دارد. این محصولات لزوماً نیازی به نمایش عمومی در کاتالوگ ندارند و می‌توانند فقط از طریق پنل ادمین ثبت شوند.
اما در کاتالوگ عمومی هم آن هارا به نمایش میگذاریم ولی بدون قیمت و با دکمه تماس با کارشناس.

> نتیجه‌ی طراحی: هر محصول (یا هر دسته‌بندی محصول) باید یک مشخصه‌ی "نحوه‌ی فروش" داشته باشد که تعیین می‌کند سایت برای آن دکمه‌ی «افزودن به سبد» نشان دهد، دکمه‌ی «تماس بگیرید / درخواست قیمت» نشان دهد، یا اصلاً در کاتالوگ عمومی نمایش داده نشود و فقط از پنل ادمین قابل ثبت باشد.

---

## ۲. فرایند سفارش تلفنی و ثبت آن در سیستم

برای دسته‌های ۲ و ۳ (و هر سفارش پیچیده‌ای که در سایت جا نیفتد)، فرایند به این شکل است:

۱. مشتری تماس می‌گیرد و درخواست خود را اعلام می‌کند.
۲. ادمین قیمت نهایی (شامل هزینه‌های جانبی احتمالی) را تعیین می‌کند.
۳. دو مسیر ممکن است:
- مسیر الف: ادمین کل سفارش را در سیستم می‌سازد و برای مشتری لینک یا نوتیفیکیشن ارسال می‌شود تا وارد شود، سفارش و قیمت را ببیند و پرداخت را انجام دهد.
- مسیر ب: کل معامله تلفنی و دستی انجام می‌شود (توافق، پرداخت خارج از سیستم) و ادمین فقط بعد از تکمیل معامله، سفارش را برای ثبت در گزارش‌ها و آمار وارد سیستم می‌کند.
۴. اصل کلی: هرچه قابل خودکارسازی باشد، باید در سایت انجام شود. فقط وقتی فیدبک منفی گرفتیم یا فرایند واقعاً پیچیده بود، به مسیر تلفنی می‌رویم. سفارش‌های آنلاین باید نهایتاً در همان سیستم مدیریت سفارشات ثبت شوند (نه یک مسیر جدا و ناهماهنگ).

> نتیجه‌ی طراحی: پنل ادمین باید بتواند یک سفارش را از صفر بسازد (نه فقط سفارش‌های ثبت‌شده توسط مشتری را مدیریت کند) — شامل انتخاب محصول، تعیین قیمت دستی (خارج از پله‌های استاندارد در صورت نیاز)، افزودن هزینه‌های جانبی، و صدور لینک پرداخت برای مشتری.

---

## ۳. منطق موجودی و نمایش در کاتالوگ

سایت فقط چیزی را نمایش می‌دهد که واقعاً و به‌صورت آنلاین قابل خرید است. مدیریت کمبود یا نبود موجودی کاملاً به‌صورت تلفنی و خارج از سایت انجام می‌شود؛ سایت در این خصوص کاری انجام نمی‌دهد و محصول ناموجود اصلاً نباید در کاتالوگ آنلاین دیده شود یا درصورت صلاح ادمین میتواند در سایت نمایش داده شود ولی با اکشن تماس با کارشناس.

> نکته‌ی مهم برای هماهنگی: نسخه‌های اولیه‌ی این سند (که بر اساس بررسی رقبا نوشته شده بودند) پیشنهاد می‌کردند برای محصولات ناموجود عبارت «تماس بگیرید» نمایش داده شود.

---

## ۴. واحد فروش (کیلوگرم، شاخه، عدد، برگ)

بر خلاف فرض اولیه‌ی «همه‌چیز همیشه با کیلوگرم حساب می‌شود»، واقعیت این است:

- اکثر محصولات خام (میلگرد، ورق، نبشی، ناودانی و...) سایز استاندارد دارند و معمولاً بر اساس وزن (کیلوگرم) قیمت‌گذاری و فاکتور می‌شوند، اما طبق سایز و درخواست مشتری قابل کم‌ و زیاد شدن‌اند.
- محصولات کاملاً تعدادی و بدون سایز ثابت مثل تیرآهن، والپست، بولت بر اساس نیاز پروژه‌ی ساختمانی متغیرند و باید با مشتری هماهنگ (تماس گرفته) شوند. تیرآهن استثنائاً می‌تواند به‌صورت شاخه‌ی ۱۲ متری استاندارد، به‌صورت تعدادی، مستقیم در سایت قیمت‌گذاری و فاکتور شود.

> نتیجه‌ی طراحی: واحد فروش نباید یک فرض ثابت سراسری (فقط کیلوگرم) در سیستم باشد. باید در سطح محصول/دسته مشخص شود که آن محصول با «وزن»، «تعداد شاخه»، یا هر دو قابل سفارش است، و فاکتور باید بتواند هر دو نوع واحد را همزمان در کنار هم نمایش دهد (دقیقاً مانند فاکتورهای واقعی نمونه که هم ردیف‌های کیلویی و هم ردیف‌های تعدادی داشتند).

---

## ۵. هزینه‌های جانبی (کرایه حمل و مشابه)

کرایه حمل به‌صورت یک ردیف مستقل در فاکتور ثبت می‌شود (نه اینکه داخل قیمت کالا مخفی شود). بخشی از سود فروش خرده در همین کرایه حمل نهفته است.

> ابهام باز: نحوه‌ی دقیق محاسبه‌ی کرایه حمل (بر چه اساسی تعیین می‌شود — فاصله، وزن کل بار، منطقه‌ی مقصد؟) هنوز مشخص نشده و باید در جلسه‌ای جداگانه تصمیم‌گیری شود. تا آن زمان، سیستم باید امکان درج دستی مبلغ کرایه توسط ادمین به‌عنوان یک ردیف جدا را داشته باشد، بدون فرمول خودکار. که این ماژول میتواند به صورت پیشفرض در بکند عدد ثابتی برگرداند.

---

## ۶. مشخصات و متغیرهای محصول

### ۶-۱. متغیرهای اصلی و قیمت‌ساز
سایز، ضخامت، طول، گرید و کارخانه‌ی سازنده — این‌ها فیلدهای اصلی هستند که باید به‌صورت ساختاریافته (نه متن آزاد) ثبت شوند، چون مستقیماً پایه‌ی قیمت‌گذاری و امکان فیلتر/جست‌وجو هستند.

### ۶-۲. متغیرهای بازاری/کیفی
در بررسی سفارش‌های واقعی مشتریان، کلمات و مفاهیم زیر مشاهده شد که باید در طراحی محصول لحاظ شوند:

| اصطلاح بازاری | توضیح |
|---|---|
| فابریک | یکی از دو نوع مجزای محصول (مثلاً ورق آجدار فابریک)؛ یک محصول کاملاً جدا از نوع «پرسی»، نه یک ویژگی روی محصول مشترک |
| پرسی | محصول جدا و مستقل از «فابریک» |
| سبک / سنگین | دسته‌بندی وزنی محصول (مثلاً «ناودانی ۸ - سنگین») که در فاکتورهای واقعی به‌عنوان بخشی از نام کالا ثبت می‌شود |
| نیمه | نسخه‌ای با ضخامت یا وزن کمتر از حالت استاندارد (مثلاً «نبشی نیمه ۵») |
| لقمه | محصول برش‌خورده (تکه‌تکه‌شده) از یک پروفیل استاندارد؛ عمدتاً وزنی، ولی گاهی تعدادی حساب می‌شود. این یک محصول مشتق از محصول خام است، نه یک ماده‌ی اولیه‌ی جدید |

> نتیجه‌ی طراحی: «فابریک» و «پرسی» باید به‌عنوان دو محصول/واریانت کاملاً جدا در کاتالوگ ثبت شوند، نه یک فیلد مشترک. سایر اصطلاحات بازاری (سبک/سنگین، نیمه) در حال حاضر می‌توانند در نام قابل‌نمایش محصول (Display Name) گنجانده شوند؛ در صورتی که در آینده معلوم شود اثر مستقیم و قابل‌فرمول‌بندی روی قیمت دارند، باید به فیلد ساختاریافته‌ی جداگانه ارتقا یابند.

> ابهام باز (کم‌اهمیت): فهرست کامل و جامع همه‌ی اصطلاحات بازاری مشابه (غیر از موارد بالا) فعلاً مشخص نیست. طبق تأیید صاحب کسب‌وکار، این موضوع در حال حاضر اولویت پایینی دارد و بعداً تکمیل می‌شود.

---

## ۷. قیمت‌گذاری پلکانی (حجم خرید)

### ۷-۱. اصول کلی (تأییدشده)
- قیمت واحد به میزان خرید (وزن یا تعداد) بستگی دارد: خرید کم = قیمت بالاتر، خرید متوسط = قیمت پایه، خرید عمده = قیمت پایین‌تر.
- پله‌ها برای هر خانواده‌ی محصول (میلگرد، ورق، پروفیل و...) می‌توانند جدا تعریف شوند، اما سیستم باید یک پله‌ی پیش‌فرض سراسری هم داشته باشد که در صورت نیاز توسط ادمین برای هر دسته override (بازنویسی) شود.
- محاسبه‌ی پله بر اساس هر قلم سفارش به‌طور مستقل انجام می‌شود، نه مجموع کل سبد خرید. یعنی اگر مشتری هم‌زمان دو محصول متفاوت بخرد که هرکدام کمتر از حد نصاب عمده باشند، تخفیف عمده به هیچ‌کدام تعلق نمی‌گیرد؛ باید هر قلم به‌تنهایی از حد نصاب رد شود.
- اگر مشتری مقدار سفارش خود را در سبد خرید افزایش دهد (مثلاً از پله‌ی خرده به پله‌ی عمده برسد)، قیمت واحد باید به‌صورت زنده و خودکار به نرخ جدید (عمده) تغییر کند — قیمت پله قفل‌شدنی نیست، برخلاف قیمت پایه‌ی بازار که برای جلوگیری از نوسان لحظه‌ای قفل می‌شود.

### ۷-۲. محدودیت حداقل سفارش
برخی محصولات دارای حداقل مقدار قابل‌فروش هستند (چه بر اساس وزن، چه تعداد) — یعنی زیر یک سقف مشخص، اصلاً قابل سفارش نیستند.

> ابهام باز (اولویت بالا): قواعد دقیق پله‌های تخفیف (مرزهای وزنی/تعدادی هر پله و درصد یا مبلغ تغییر قیمت در هر پله، برای هر خانواده‌ی محصول) هنوز نهایی نشده و به گفته‌ی صاحب کسب‌وکار «جزو خلاقیت و مزیت رقابتی ماست» — باید در یک جلسه‌ی اختصاصی تعیین شود. تا آن زمان، سیستم باید طوری طراحی شود که این پله‌ها به‌سادگی از پنل ادمین (بدون نیاز به تغییر کد) قابل تعریف و ویرایش باشند.

---

## ۸. مالیات بر ارزش افزوده
- تمام قیمت‌های نمایش‌داده‌شده به مشتری، شامل ۱۰٪ ارزش‌افزوده هستند و دو قیمت (با/بدون مالیات) در کنار هم نمایش داده نمی‌شود.
- در پشت سیستم، قیمت پایه (بدون مالیات) ذخیره می‌شود و مالیات فقط در محاسبه‌ی نهایی اعمال می‌گردد.
- یک متن ثابت راهنما باید در همه‌ی صفحات مرتبط نمایش داده شود: «همه قیمت‌های اعلامی با احتساب ۱۰٪ ارزش افزوده می‌باشد و هزینه جانبی دیگری ندارد.»

---

## ۹. تاریخچه‌ی قیمت‌ها
هر تغییر قیمت به‌عنوان یک رکورد جدید ثبت می‌شود؛ رکوردهای قبلی هرگز حذف یا ویرایش نمی‌شوند. سیستم همیشه از آخرین رکورد برای نمایش و محاسبه استفاده می‌کند، اما مدیران باید بتوانند گزارش تاریخی قیمت هر محصول را مشاهده کنند. اما رکورد های قیمت میتوانند منسوخ شوند تا در صورت گرفتن گزارش در آن گنجانده نشوند.

---

## ۱۰. جمع‌بندی ابهامات باز (نیازمند تصمیم‌گیری در جلسه)

| # | موضوع | اولویت | وضعیت |
|---|---|---|---|
| ۱ | فرمول/قاعده‌ی دقیق محاسبه‌ی کرایه حمل | بالا | باز |
| ۲ | مرزهای دقیق پله‌های تخفیف حجمی برای هر خانواده‌ی محصول | بالا | باز |
| ۳ | تناقض «تماس بگیرید برای ناموجودی» در برابر «عدم نمایش کامل محصول ناموجود» | متوسط | نیازمند تأیید نهایی |
| ۴ | فهرست کامل اصطلاحات بازاری مشابه فابریک/پرسی/لقمه/نیمه/سنگین | پایین | باز، غیر فوری |

---

## ۱۱. نکات کلیدی برای تیم فنی (خلاصه‌ی اجرایی)

این بخش صرفاً جمع‌بندی implication های معماری از بندهای بالاست، برای استفاده‌ی مستقیم تیم توسعه:

۱. هر محصول باید یک مشخصه‌ی «نحوه‌ی فروش» داشته باشد: کاملاً آنلاین / نمایش آنلاین با تکمیل تلفنی / کاملاً سفارشی-تماسی.
۲. پنل ادمین باید امکان ساخت سفارش از صفر (نه فقط مدیریت سفارش‌های ثبت‌شده) را داشته باشد، شامل قیمت‌گذاری دستی و افزودن هزینه‌ی جانبی.
۳. واحد فروش باید در سطح محصول قابل تعریف باشد (وزن/تعداد/هر دو)، نه یک فرض ثابت سراسری.
۴. کرایه حمل و مشابه آن باید به‌صورت ردیف مستقل (غیرمرتبط با یک محصول واقعی) در سفارش/فاکتور قابل ثبت باشند.
۵. فابریک/پرسی باید محصولات جدا باشند؛ سایر اصطلاحات بازاری فعلاً در نام نمایشی محصول کافی است.
۶. پله‌های تخفیف باید per-category قابل تعریف باشند با امکان override از یک پیش‌فرض سراسری، و باید کاملاً از پنل ادمین (بدون تغییر کد) قابل مدیریت باشند.
۷. محاسبه‌ی پله per-item است، نه per-cart؛ و باید با تغییر مقدار در سبد خرید، به‌صورت زنده بازمحاسبه شود.
۸. برخی محصولات نیاز به حداقل مقدار سفارش دارند که باید در سطح محصول قابل تعریف باشد.
۹. برای پیشفاکتور و فاکتور از stimulsoft استفاده شود. به این صورت که در بکند فایل خروجی فاکتور و پیش فاکتور به عکس و سپس base64 تبدیل شده سمت فرانت ارسال میشود و فرانت با تبدیل آن به عکس یا pdf آن را نمایش میدهد یا قابل دانلود میکند.

(Note: item 9 above is an explicitly volunteered TECHNICAL decision by the business owner, embedded in an otherwise business-focused document. Per working method, this was logged as a pre-existing technical decision and explicitly excluded from domain modeling until the technical design phase.)

===========================================================
PART 2 BEGINS BELOW
===========================================================

===========================================================
ORIGINAL SOURCE DOCUMENTS B-G (COMPETITOR REFERENCE DATA)
Extracted from AhanOnline, MarkazAhan, EsfahanAhan
NON-AUTHORITATIVE for Foolad Shahyar Sepand business rules, EXCEPT where
explicitly promoted to a confirmed fact (see Decision D-14 in the Decision Log:
overall product-domain breadth was explicitly confirmed to match this data).
Reproduced verbatim as submitted in the original conversation, in the order submitted.
===========================================================


###########################################################
DOCUMENT B (submitted as document index 3, first of two concatenated docs)
###########################################################

# سند مرجع کاتالوگ و جزئیات محصولات آهن‌آلات

**منبع داده‌ها:** سایت‌های AhanOnline (ahanonline.com)، MarkazAhan (markazeahan.com) و منابع مرتبط با جداول وزن و مشخصات فنی منتشرشده در این پلتفرم‌ها.

---

## ۱. دسته‌بندی اصلی محصولات آهن‌آلات

بر اساس ساختار کاتالوگ AhanOnline و MarkazAhan، محصولات نهایی به دو دسته اصلی **طویل** و **تخت** تقسیم می‌شوند.

### ۱.۱. محصولات طویل
- **میلگرد**: میلگرد ساده، میلگرد آجدار، کلاف.
- **تیرآهن**: تیرآهن I، تیرآهن H (هاش)، ریل.
- **پروفیل (قوطی)**: پروفیل بسته (مربع و مستطیل)، پروفیل باز، لوله.
- **نبشی و ناودانی**.
- **محصولات مفتولی**: سیم مفتول، توری، مش، رابیتس.

### ۱.۲. محصولات تخت
- **ورق گرم (ورق سیاه)**: سیاه، A516، A283، ST52، ضد سایش، آجدار، اسیدشویی، اسلب.
- **ورق سرد**: روغنی.
- **ورق‌های پوششی**: گالوانیزه، رنگی، استنلس استیل.
- **ورق‌های فرمینگ**: کرکره، شادولاین، ذوزنقه، سینوسی، تایل.
- **سایر**: ورق پانچ، تسمه، چهارپهلو، گریتینگ.

### ۱.۳. محصولات میانی و اولیه
- شمش، اسلب، بیلت.
- آهن اسفنجی، قراضه.

---

## ۲. جزئیات محصولات کلیدی (ورق و پروفیل)

### ۲.۱. ورق (Sheet)
ورق‌ها بر اساس فرآیند تولید، پوشش و کاربرد دسته‌بندی می‌شوند.

**مشخصات فنی رایج در کاتالوگ‌ها:**
- ضخامت: از ۰.۴ میلی‌متر تا ۱۰۰ میلی‌متر (برای ورق سیاه تا ۱۰۰ میلی‌متر گزارش شده).
- ابعاد استاندارد: عرض ۱ متر، ۱.۲۵ متر، ۱.۵ متر؛ طول ۲ متر، ۶ متر یا طول درخواستی.
- حالت عرضه: رول، شیت (برش‌خورده)، فابریک.
- محل بارگیری: بنگاه تهران، بنگاه اصفهان، کارخانه.
- واحد قیمت: کیلوگرم.

**انواع اصلی و چگالی مورد استفاده برای محاسبه وزن:**

| نوع ورق              | چگالی (g/cm³) | توضیح |
|----------------------|---------------|-------|
| فولاد کربنی / ورق سیاه | ۷.۸۵         | استاندارد رایج |
| ورق گالوانیزه         | ۷.۸۵         | پوشش روی تأثیر ناچیز دارد |
| ورق روغنی             | ۷.۸۵         | - |
| ورق رنگی              | ۷.۸۵         | - |
| ورق آجدار             | ۷.۸۵         | - |
| استنلس استیل          | ۷.۹۳ – ۸.۰۰  | - |
| آلومینیوم             | ۲.۷۰         | - |
| مس                    | ۸.۹۶         | - |

**فرمول محاسبه وزن ورق (استاندارد مورد استفاده در سایت‌های مرجع):**
```
وزن (کیلوگرم) = طول (متر) × عرض (متر) × ضخامت (میلی‌متر) × چگالی (۷.۸۵ برای فولاد کربنی)
```
یا معادل:
```
وزن هر مترمربع (kg/m²) ≈ ضخامت (mm) × ۷.۸۵
```

**جدول نمونه وزن هر مترمربع ورق فولادی (بر اساس چگالی ۷.۸۵):**

| ضخامت (mm) | وزن هر مترمربع (kg) |
|------------|---------------------|
| ۰.۵        | ۳.۹۳                |
| ۰.۷        | ۵.۵۰                |
| ۱.۰        | ۷.۸۵                |
| ۱.۵        | ۱۱.۷۸               |
| ۲.۰        | ۱۵.۷۰               |
| ۳.۰        | ۲۳.۵۵               |
| ۴.۰        | ۳۱.۴۰               |
| ۵.۰        | ۳۹.۲۵               |
| ۶.۰        | ۴۷.۱۰               |
| ۸.۰        | ۶۲.۸۰               |
| ۱۰.۰       | ۷۸.۵۰               |

### ۲.۲. پروفیل (Profile / قوطی)
پروفیل‌ها به دو گروه ساختمانی و صنعتی تقسیم می‌شوند. قیمت‌گذاری بر اساس ضخامت یکسان برای ابعاد مختلف انجام می‌شود.

**مشخصات فنی رایج:**
- شکل مقطع: مربع، مستطیل.
- ابعاد: از ۲۰×۲۰ میلی‌متر تا ۱۴۰×۱۴۰ میلی‌متر و بالاتر (تا ۵۰۰×۵۰۰ در برخی منابع).
- ضخامت: از ۰.۵ میلی‌متر تا ۸ میلی‌متر و بالاتر.
- طول استاندارد: ۶ متر یا ۱۲ متر.
- واحد قیمت: کیلوگرم.
- محل بارگیری: کارخانه یا بنگاه.

**فرمول محاسبه وزن پروفیل توخالی (قوطی مربع/مستطیل) مورد استفاده در منابع:**
```
وزن هر متر (کیلوگرم) = [(H + W − ۲t) × ۲ × t] × ۷.۸۶
```
که در آن:
- H = ارتفاع (متر)
- W = عرض (متر)
- t = ضخامت (متر)
- ۷.۸۶ = چگالی فولاد

فرمول ساده‌شده معادل:
```
وزن = طول × ضخامت × (طول مقطع + عرض مقطع) × ۲ × ۷.۸۵
```

**جدول نمونه وزن پروفیل مربع (kg/m) بر اساس داده‌های MarkazAhan و منابع مرتبط:**

| سایز (mm) | ضخامت ۱ mm | ضخامت ۲ mm | ضخامت ۲.۵ mm | ضخامت ۳ mm |
|-----------|------------|------------|--------------|------------|
| ۲۰×۲۰     | ۰.۶۲       | ۱.۱۸       | -            | -          |
| ۲۵×۲۵     | ۰.۷۸       | ۱.۵۳       | ۱.۸۸         | -          |
| ۳۰×۳۰     | ۰.۹۶       | ۱.۷۸       | ۲.۳۱         | ۲.۷۳       |
| ۴۰×۴۰     | ۱.۲۵       | ۲.۴۵       | ۳.۰۳         | ۳.۶۰       |
| ۵۰×۵۰     | -          | ۳.۱۱       | ۳.۴۸         | ۴.۶۲       |
| ۶۰×۶۰     | -          | -          | ۴.۶۳         | ۵.۵۴       |
| ۸۰×۸۰     | -          | ۴.۹۷       | ۶.۲۵         | ۷.۴۴       |
| ۱۰۰×۱۰۰   | -          | -          | ۷.۸۱         | ۹.۳۸       |

وزن شاخه ۶ متری = وزن هر متر × ۶.

### ۲.۳. میلگرد
- انواع: ساده، آجدار (A2، A3، A4)، کلاف.
- سایز رایج: ۸ تا ۳۲ میلی‌متر و بالاتر.
- طول استاندارد: ۱۲ متر.
- واحد فروش: شاخه یا وزن (کیلوگرم/تن).
- قیمت‌گذاری: بر اساس کیلوگرم؛ سایزهای ۱۴ تا ۲۸ میلی‌متر اغلب قیمت یکسان دارند؛ سایزهای خیلی ریز یا درشت قیمت متفاوت.

در پیش‌فاکتور و فاکتور نهایی، مقدار بر اساس وزن (کیلوگرم) ثبت می‌شود حتی اگر سفارش به صورت تعداد شاخه باشد.

---

## ۳. نحوه نمایش موجودی و قیمت در کاتالوگ

بر اساس رفتار سایت‌های مرجع:
- محصولات موجود: قیمت به تومان (یا ریال) همراه با نمودار تغییرات قیمت نمایش داده می‌شود.
- محصولات ناموجود یا نیازمند تأیید: عبارت «تماس بگیرید» نمایش داده می‌شود.
- قیمت‌ها به‌صورت روزانه یا لحظه‌ای به‌روزرسانی می‌شوند.
- محل بارگیری (بنگاه تهران / اصفهان / کارخانه) و کارخانه تولیدکننده ذکر می‌شود.
- قیمت اعلامی شامل ۱۰٪ ارزش افزوده است یا به‌صورت جداگانه اعلام می‌شود؛ در بسیاری از موارد قیمت نهایی با احتساب ارزش افزوده نمایش داده می‌شود.

---

## ۴. واحدهای سنجش و تبدیل

- واحد پایه قیمت: کیلوگرم.
- واحدهای ورودی کاربر: شاخه، کیلوگرم، تن، متر طول، مترمربع.
- تبدیل نهایی برای محاسبه قیمت و ثبت در پیش‌فاکتور/فاکتور: همیشه به کیلوگرم انجام می‌شود.
- طول استاندارد شاخه: ۶ متر یا ۱۲ متر.

---

## ۵. پارامترهای ثبت محصول در کاتالوگ

هر محصول در کاتالوگ‌های مرجع با پارامترهای زیر تعریف می‌شود:
- نام محصول / دسته / زیر‌دسته
- ابعاد (طول × عرض یا سایز مقطع)
- ضخامت
- طول شاخه
- وزن نظری (kg/m یا kg/شاخه)
- جنس / آلیاژ / استاندارد (ST37، ST52، A3 و ...)
- کارخانه تولیدکننده
- محل بارگیری
- حالت عرضه (رول، شیت، شاخه)
- قیمت واحد (کیلوگرم)
- وضعیت موجودی (موجود / تماس بگیرید)

---

## ۶. خلاصه فرمول‌های وزن مورد استفاده در ماشین‌حساب‌های مرجع

**ورق:**
```
وزن (kg) = طول (m) × عرض (m) × ضخامت (mm) × ۷.۸۵
```

**پروفیل قوطی:**
```
وزن هر متر (kg/m) = ۲ × t × (H + W − ۲t) × ۷.۸۶
```
(با واحدهای متر)

این فرمول‌ها و جداول وزن در ماشین‌حساب‌ها و جداول منتشرشده در سایت‌های AhanOnline، MarkazAhan و منابع مرتبط با آن‌ها استفاده می‌شوند.



# سند مرجع تفصیلی کاتالوگ و جزئیات محصولات آهن‌آلات

**منابع داده‌ها:**
- ahanonline.com (آهن آنلاین)
- markazeahan.com (مرکزآهن)
- جداول وزن و مشخصات فنی منتشرشده در این دو پلتفرم و منابع مرتبط مستقیم با آن‌ها


###########################################################
DOCUMENT C (submitted as document index 4)
###########################################################

## ۱. ساختار کلی کاتالوگ محصولات در سایت‌های مرجع

### ۱.۱. دسته‌بندی سطح اول (AhanOnline)
محصولات به سه سطح اصلی تقسیم می‌شوند:

**الف) مواد اولیه**
- آهن اسفنجی
- قراضه (ضایعات)

**ب) محصولات میانی فولادی**
- شمش
- اسلب
- بیلت

**ج) محصولات نهایی** (دو گروه اصلی)

**گروه طویل:**
- تیرآهن (I، H/هاش، ریل)
- میلگرد (ساده، آجدار، کلاف)
- محصولات مفتولی (سیم مفتول، توری، مش، رابیتس)
- پروفیل بسته و باز + لوله
- نبشی و ناودانی

**گروه تخت:**
- ورق گرم (سیاه، A516، A283، ST52، ضد سایش، آجدار، اسیدشویی، اسلب)
- ورق سرد (روغنی)
- ورق‌های پوششی (گالوانیزه، رنگی، استنلس استیل)
- ورق فرمینگ (کرکره، شادولاین، ذوزنقه، سینوسی، تایل)
- ورق پانچ، تسمه، چهارپهلو، گریتینگ

### ۱.۲. دسته‌بندی عملیاتی در MarkazAhan
محصولات بر اساس نوع مقطع و کاربرد به صفحات جداگانه تقسیم می‌شوند:
- قوطی پروفیل (ساختمانی و صنعتی)
- پروفیل گالوانیزه
- ورق سیاه، ورق روغنی، ورق گالوانیزه، ورق رنگی، ورق فرمینگ
- میلگرد آجدار و ساده
- تیرآهن و هاش
- نبشی و ناودانی
- لوله (درزدار، مانیسمان، گالوانیزه)

هر صفحه محصول شامل جدول با ستون‌های زیر است:
ابعاد / ضخامت / طول / وزن / محل بارگیری / قیمت (تومان) / نمودار تغییرات / وضعیت خرید

---

## ۲. جزئیات کامل محصول ورق

### ۲.۱. انواع ورق و مشخصات فنی

| نوع ورق              | فرآیند تولید     | ضخامت رایج (mm)     | عرض رایج (m)      | حالت عرضه          | چگالی محاسباتی |
|----------------------|------------------|---------------------|-------------------|--------------------|----------------|
| ورق سیاه (گرم)       | نورد گرم         | ۱ تا ۱۰۰            | ۱ / ۱.۲۵ / ۱.۵   | رول / شیت / فابریک | ۷.۸۵           |
| ورق روغنی (سرد)      | نورد سرد         | ۰.۳ تا ۳            | ۱ / ۱.۲۵         | رول                | ۷.۸۵           |
| ورق گالوانیزه        | پوشش روی         | ۰.۳ تا ۳            | ۱ / ۱.۲۵         | رول                | ۷.۸۵           |
| ورق رنگی             | پوشش رنگی        | ۰.۴ تا ۰.۷          | ۱ / ۱.۲۵         | رول                | ۷.۸۵           |
| ورق آجدار            | نورد گرم + آج    | ۲ تا ۱۰             | ۱.۲۵ / ۱.۵       | شیت                | ۷.۸۵           |
| ورق اسیدشویی         | اسیدشویی         | ۱ تا ۶              | ۱.۲۵ / ۱.۵       | رول / شیت          | ۷.۸۵           |
| ورق ضد سایش          | آلیاژی           | ۴ تا ۵۰             | ۱.۵ / ۲          | شیت                | ۷.۸۵           |
| ورق ST52 / A516      | آلیاژی           | ۶ تا ۱۰۰            | ۱.۵ / ۲          | شیت / فابریک       | ۷.۸۵           |
| استنلس استیل         | نورد + آلیاژ     | ۰.۳ تا ۱۰           | ۱ / ۱.۲۵         | رول / شیت          | ۷.۹۳–۸.۰۰      |
| آلومینیوم            | نورد             | ۰.۳ تا ۶            | ۱ / ۱.۲۵         | رول / شیت          | ۲.۷۰           |

### ۲.۲. فرمول محاسبه وزن ورق (استاندارد مورد استفاده)

**فرمول اصلی:**
```
وزن (کیلوگرم) = طول (متر) × عرض (متر) × ضخامت (میلی‌متر) × چگالی
```

- چگالی فولاد کربنی و گالوانیزه و روغنی و رنگی: **۷.۸۵**
- چگالی استنلس استیل: **۷.۹۳ تا ۸.۰۰**
- چگالی آلومینیوم: **۲.۷۰**
- چگالی مس: **۸.۹۶**

**فرمول میان‌بر برای هر مترمربع:**
```
وزن هر مترمربع (kg/m²) = ضخامت (mm) × ۷.۸۵
```

### ۲.۳. جدول وزن هر مترمربع ورق فولادی (چگالی ۷.۸۵)

| ضخامت (mm) | وزن هر مترمربع (kg) | ضخامت (mm) | وزن هر مترمربع (kg) |
|------------|---------------------|------------|---------------------|
| ۰.۴        | ۳.۱۴                | ۶          | ۴۷.۱۰               |
| ۰.۵        | ۳.۹۳                | ۸          | ۶۲.۸۰               |
| ۰.۶        | ۴.۷۱                | ۱۰         | ۷۸.۵۰               |
| ۰.۷        | ۵.۵۰                | ۱۲         | ۹۴.۲۰               |
| ۰.۸        | ۶.۲۸                | ۱۵         | ۱۱۷.۷۵              |
| ۱.۰        | ۷.۸۵                | ۲۰         | ۱۵۷.۰۰              |
| ۱.۲        | ۹.۴۲                | ۲۵         | ۱۹۶.۲۵              |
| ۱.۵        | ۱۱.۷۸               | ۳۰         | ۲۳۵.۵۰              |
| ۲.۰        | ۱۵.۷۰               | ۴۰         | ۳۱۴.۰۰              |
| ۲.۵        | ۱۹.۶۳               | ۵۰         | ۳۹۲.۵۰              |
| ۳.۰        | ۲۳.۵۵               | ۶۰         | ۴۷۱.۰۰              |
| ۴.۰        | ۳۱.۴۰               | ۸۰         | ۶۲۸.۰۰              |
| ۵.۰        | ۳۹.۲۵               | ۱۰۰        | ۷۸۵.۰۰              |

### ۲.۴. ابعاد استاندارد رایج ورق در بازار ایران (بر اساس جداول MarkazAhan و AhanOnline)

- عرض: ۱۰۰۰، ۱۲۵۰، ۱۵۰۰، ۲۰۰۰ میلی‌متر
- طول شیت: ۲۰۰۰، ۲۵۰۰، ۶۰۰۰ میلی‌متر
- طول رول: نامحدود (برش درخواستی)
- ضخامت ورق سیاه فابریک: تا ۱۰۰ میلی‌متر
- ضخامت ورق رنگی و گالوانیزه: عمدتاً ۰.۴ تا ۰.۷ میلی‌متر

---

## ۳. جزئیات کامل محصول پروفیل (قوطی)

### ۳.۱. انواع پروفیل

- **پروفیل ساختمانی**: قوطی مربع و مستطیل، سپری، لنگه‌دری، چارچوب فرانسوی، داربستی
- **پروفیل صنعتی**: ضخامت بالاتر، ابعاد بزرگ‌تر
- **پروفیل گالوانیزه**: پوشش روی، ضخامت ۲ و ۲.۵ میلی‌متر رایج
- **پروفیل زد (Z)**: ارتفاع ۱۸ تا ۲۴ میلی‌متر، ضخامت ۲ تا ۳ میلی‌متر

### ۳.۲. مشخصات فنی

- طول استاندارد شاخه: **۶ متر** و **۱۲ متر**
- ضخامت رایج: ۰.۵، ۱، ۱.۲۵، ۱.۵، ۲، ۲.۵، ۳، ۴، ۵، ۶، ۸ میلی‌متر
- ابعاد مربع رایج: ۲۰×۲۰ تا ۱۴۰×۱۴۰ میلی‌متر (و بالاتر تا ۵۰۰×۵۰۰)
- ابعاد مستطیل رایج: ۲۰×۱۰ تا ۳۰۰×۱۳۵ میلی‌متر

### ۳.۳. فرمول محاسبه وزن پروفیل قوطی (مورد استفاده در منابع)

**فرمول استاندارد:**
```
وزن هر متر (kg/m) = [(H + W − ۲ × t) × ۲ × t] × ۷.۸۶
```
که در آن:
- H = ارتفاع مقطع (متر)
- W = عرض مقطع (متر)
- t = ضخامت (متر)
- ۷.۸۶ = چگالی فولاد

**فرمول ساده‌شده رایج:**
```
وزن هر متر = ۲ × ضخامت (mm) × (طول مقطع + عرض مقطع − ۲ × ضخامت) × ۰.۰۰۷۸۵
```

### ۳.۴. جدول وزن پروفیل مربع (kg/m) – داده‌های MarkazAhan

| سایز (mm)   | ضخامت ۱ mm | ضخامت ۱.۲۵ mm | ضخامت ۱.۵ mm | ضخامت ۲ mm | ضخامت ۲.۵ mm | ضخامت ۳ mm | ضخامت ۴ mm |
|-------------|------------|---------------|--------------|------------|--------------|------------|------------|
| ۲۰×۲۰       | ۰.۶۲       | ۰.۷۵          | ۰.۹۱         | ۱.۱۸       | -            | -          | -          |
| ۲۵×۲۵       | ۰.۷۸       | ۰.۹۸          | ۱.۱۷         | ۱.۵۳       | ۱.۸۸         | -          | -          |
| ۳۰×۳۰       | ۰.۹۶       | ۱.۱۹          | ۱.۴۲         | ۱.۷۸       | ۲.۳۱         | ۲.۷۳       | -          |
| ۳۵×۳۵       | ۱.۱۲       | ۱.۳۹          | ۱.۶۴         | ۲.۱۶       | ۲.۶۸         | ۳.۱۹       | -          |
| ۴۰×۴۰       | ۱.۲۵       | ۱.۵۵          | ۱.۸۶         | ۲.۴۵       | ۳.۰۳         | ۳.۶۰       | -          |
| ۴۵×۴۵       | -          | -             | ۲.۱۲         | ۲.۸۱       | ۳.۴۷         | ۴.۲۵       | -          |
| ۵۰×۵۰       | -          | -             | ۲.۳۳         | ۳.۱۱       | ۳.۴۸         | ۴.۶۲       | ۶.۰۶       |
| ۶۰×۶۰       | -          | -             | ۲.۸۲         | -          | ۴.۶۳         | ۵.۵۴       | ۷.۳۲       |
| ۷۰×۷۰       | -          | -             | -            | ۴.۳۸       | ۵.۴۶         | ۶.۵۱       | ۸.۵۸       |
| ۸۰×۸۰       | -          | -             | -            | ۴.۹۷       | ۶.۲۵         | ۷.۴۴       | ۹.۸۹       |
| ۹۰×۹۰       | -          | -             | -            | ۵.۶۸       | ۶.۹۸         | ۸.۳۸       | ۱۱.۱۸      |
| ۱۰۰×۱۰۰     | -          | -             | -            | -          | ۷.۸۱         | ۹.۳۸       | ۱۲.۴۲      |
| ۱۱۰×۱۱۰     | -          | -             | -            | -          | ۸.۶۲         | ۱۰.۳۶      | ۱۳.۷۲      |
| ۱۴۰×۱۴۰     | -          | -             | -            | -          | ۱۱.۰۲        | ۱۳.۱۹      | ۱۷.۴۸      |

وزن شاخه ۶ متری = وزن هر متر × ۶
وزن شاخه ۱۲ متری = وزن هر متر × ۱۲

### ۳.۵. قیمت‌گذاری پروفیل در سایت‌های مرجع
- پروفیل‌های با **ضخامت یکسان** قیمت کیلویی یکسانی دارند (مستقل از ابعاد مقطع).
- مثال: تمام پروفیل‌های ضخامت ۲ میلی‌متر یک قیمت واحد دارند.
- قیمت بر اساس کیلوگرم اعلام می‌شود.
- وضعیت «تماس بگیرید» برای موجودی‌های نامشخص یا سفارشی استفاده می‌شود.

---

## ۴. جزئیات محصول میلگرد

### ۴.۱. انواع و استانداردها
- میلگرد ساده
- میلگرد آجدار A2، A3، A4
- کلاف

### ۴.۲. سایزهای رایج و رفتار قیمتی
- سایزهای خیلی ریز (۸ و ۱۰ میلی‌متر): قیمت بالاتر
- سایزهای ۱۰ و ۱۲ میلی‌متر: قیمت متوسط
- سایزهای ۱۴ تا ۲۸ میلی‌متر: قیمت پایه یکسان
- سایزهای ۳۲ و بالاتر: قیمت متفاوت

### ۴.۳. واحد فروش و ثبت در پیش‌فاکتور
- مشتری می‌تواند تعداد شاخه یا وزن (کیلوگرم / تن) سفارش دهد.
- سیستم وزن شاخه را محاسبه می‌کند.
- در پیش‌فاکتور و فاکتور نهایی مقدار **همیشه به کیلوگرم** ثبت می‌شود.

---

## ۵. پارامترهای اجباری ثبت هر محصول در کاتالوگ

هر ردیف محصول در جداول سایت‌های مرجع شامل فیلدهای زیر است:

۱. نام کامل محصول
۲. دسته و زیر‌دسته
۳. ابعاد مقطع یا ورق (طول × عرض یا سایز)
۴. ضخامت
۵. طول شاخه / رول
۶. وزن نظری (kg/m یا kg/شاخه)
۷. جنس / آلیاژ / استاندارد
۸. کارخانه تولیدکننده
۹. محل بارگیری (بنگاه تهران / اصفهان / کارخانه)
۱۰. حالت عرضه (رول / شیت / شاخه / فابریک)
۱۱. قیمت واحد (کیلوگرم)
۱۲. وضعیت موجودی (موجود / تماس بگیرید)
۱۳. تاریخ آخرین به‌روزرسانی قیمت

---

## ۶. نحوه نمایش موجودی و قیمت

- محصولات موجود: قیمت عددی + نمودار تغییرات
- محصولات ناموجود یا نیازمند تأیید موجودی: عبارت ثابت **«تماس بگیرید»**
- قیمت‌ها به‌صورت روزانه به‌روزرسانی می‌شوند.
- در بسیاری از موارد قیمت اعلامی شامل ۱۰٪ ارزش افزوده است یا به‌صورت جداگانه اعلام می‌شود.
- محل بارگیری و کارخانه تولیدکننده همیشه در جدول نمایش داده می‌شود.

---

## ۷. خلاصه فرمول‌های وزن مورد استفاده در ماشین‌حساب‌های مرجع

**ورق:**
```
وزن (kg) = طول (m) × عرض (m) × ضخامت (mm) × ۷.۸۵
```

**پروفیل قوطی:**
```
وزن هر متر (kg/m) = [(H + W − ۲t) × ۲ × t] × ۷.۸۶
```
(واحدها به متر)

این فرمول‌ها و جداول وزن مستقیماً از ماشین‌حساب‌ها و جداول منتشرشده در ahanonline.com و markazeahan.com استخراج شده‌اند.


###########################################################
DOCUMENT D (submitted as document index 5)
###########################################################

# مستند داده‌ای کاتالوگ محصولات آهن‌آلات
**منابع استخراج داده:** آهن‌آنلاین (AhanOnline)، مرکزآهن (MarkazAhan)، اصفهان آهن (EsfahanAhan)

---

## ۱. ساختار سلسله‌مراتبی دسته‌بندی‌ها (Category Hierarchy)
در پلتفرم‌های مرجع، محصولات به دسته‌های کاملاً مجزا و مستقل تقسیم شده‌اند. هر دسته‌بندی اصلی، یک مسیر (URL) اختصاصی دارد و ساختار داده‌های آن با دسته دیگر متفاوت است.

لیست دسته‌بندی‌های اصلی عبارتند از:
* میلگرد (Rebar)
* ورق (Sheet)
* پروفیل / قوطی (Profile)
* تیرآهن (I-Beam)
* نبشی (Angle Iron)
* لوله (Pipe)
* شمش و بیلت (Bloom/Billet)

---

## ۲. متغیرها و ویژگی‌های محصولات (Product Attributes)
هر خانواده محصولی، مجموعه‌ای از متغیرهای مشخص را به عنوان فیلترهای اصلی (Filter Options) و ستون‌های جدول قیمت‌ها نمایش می‌دهد. این متغیرها در واقع ویژگی‌های فیزیکی کالا هستند:

### ۲.۱. میلگرد
* **سایز (قطر):** اعداد صحیح (۸، ۱۰، ۱۲، ۱۴، ۱۶، ۱۸، ۲۰، ۲۲، ۲۵، ۲۸، ۳۰، ۳۲). واحد اندازه‌گیری به طور ضمنی میلی‌متر است اما در کنار عدد نوشته نمی‌شود.
* **گرید (استاندارد مقاومت):** A2، A3، A4.
* **کارخانه (برند):** کاوه، ذوب آهن، فولاد خوزستان، نیشابور و...

### ۲.۲. ورق
* **نوع ورق:** سیاه، روغنی، گالوانیزه، رنگی، اسیدشویی، کروم.
* **ضخامت:** به میلی‌متر (مثلاً ۰.۵، ۱، ۱.۵، ۲، ۳، ۱۰، ۲۰).
* **ابعاد (عرض و طول):** معمولاً به صورت استاندارد شده نمایش داده می‌شود (مثلاً ۱۰۰*۲۰۰، ۱۵۰*۶۰۰، ۲*۶). واحد سانتی‌متر یا میلی‌متر است.

### ۲.۳. پروفیل (قوطی)
* **نوع پروفیل:** قوطی مبلی، قوطی صنعتی، پروفیل زد یا فیکس.
* **ابعاد (سایز):** دو عدد ضربدر هم (مثلاً ۲۰*۲۰، ۴۰*۴۰، ۴۰*۸۰، ۶۰*۸۰). واحد به طور ضمنی میلی‌متر است.
* **ضخامت:** به میلی‌متر (مثلاً ۰.۸، ۱، ۱.۲۵، ۱.۵، ۲، ۲.۵، ۳).

### ۲.۴. تیرآهن
* **شماره تیرآهن:** اعداد (۱۰، ۱۲، ۱۴، ۱۶، ۱۸، ۲۰، ۲۲، ۲۴، ۲۷، ۳۰، ۳۶).
* **نوع تیرآهن:** بال‌پهن (IPE)، بال‌نازک (INP).

### ۲.۵. نبشی
* **سایز (بال):** معمولاً با یک یا دو عدد بیان می‌شود (مثلاً ۳، ۴، ۵ برای بال مساوی / ۳*۴، ۴*۶ برای بال نامساوی).
* **ضخامت:** به میلی‌متر.
* **واحد نمایش سایز:** در جدول قیمت‌ها و فیلترها، سایز نبشی اغلب با واحد **اینچ** نمایش داده می‌شود (مثلاً سایز ۲ اینچ، ۲.۵ اینچ) و معادل میلی‌متری آن در پرانتز قرار می‌گیرد (مثلاً ۵۰*۵۰).

### ۲.۶. لوله
* **نوع لوله:** مانیسمان (بدون درز)، درزدار (لوله سیاه، لوله گالوانیزه، لوله مبلی).
* **سایز (قطر):** در فیلترها و جداول قیمت، قطر لوله تقریباً همیشه با واحد **اینچ** نمایش داده می‌شود (مثلاً ۱/۲ اینچ، ۱ اینچ، ۲ اینچ، ۴ اینچ، ۶ اینچ).
* **ضخامت (لاستیک):** به میلی‌متر (مثلاً ۱.۵، ۲، ۳).

---

## ۳. منطق قیمت‌گذاری و واحدهای اندازه‌گیری
* **واحد پایه قیمت:** در تمامی پلتفرم‌های مرجع، قیمت تمامی محصولات آهنی منحصراً بر اساس **کیلوگرم** اعلام می‌شود. ستون قیمت در جداول، همیشه "قیمت هر کیلوگرم" است.
* **تغییرات قیمت بر اساس متغیرها:**
    * **در پروفیل:** قیمت کیلویی مستقیماً به "ضخامت" گره خورده است. تمامی ابعاد پروفیل با ضخامت مشابه (مثلاً تمامی قوطی‌های ضخامت ۲ میل با هر ابعادی) یک قیمت کیلویی واحد دارند.
    * **در میلگرد:** قیمت کیلویی مستقیماً به "سایز (قطر)" گره خورده است. سایزهای ریز (۸ و ۱۰) قیمت متفاوتی با سایزهای متوسط (۱۴ تا ۲۸) دارند.
* **واحد فروش در پیش‌فاکتور:** با وجود اینکه کاربر ممکن است به صورت "شاخه" یا "عدد" سفارش دهد، در پیش‌فاکتورها و فاکتورهای رسمی این سایت‌ها، میزان کالا به صورت **وزن (کیلوگرم یا تن)** محاسبه و درج می‌شود.

---

## ۴. مدیریت و نمایش موجودی
در جدول قیمت‌های پلتفرم‌های مرجع، دو وضعیت برای سلولِ قیمت وجود دارد:
۱. **موجودی با قیمت:** عدد ریالی به ازای هر کیلوگرم درج شده است.
۲. **بدون قیمت/موجودی:** به جای عدد، عبارت **"تماس بگیرید"** یا یک خط تیره (-) قرار دارد. در این پلتفرم‌ها از عبارت "عدم موجودی" در جدول اصلی استفاده نمی‌شود.

---

## ۵. ابزارهای محاسبه وزن (Weight Calculators)
پلتفرم‌های مرجع دارای ابزارهای محاسبه‌گر وزن هستند که به دو دسته تقسیم می‌شوند:

### ۵.۱. ماشین حساب داینامیک (فرم‌محور)
این ابزار **فقط** برای دو دسته "ورق" و "پروفیل" پیاده‌سازی شده است.
* **ورودی‌ها:** کاربر باید ابعاد (طول، عرض، ضخامت برای ورق / طول، ابعاد، ضخامت برای پروفیل) را به صورت عددی وارد کند.
* **خروجی:** محاسبه وزن نهایی بر حسب کیلوگرم.
* *(نکته: در برخی از این پلتفرم‌ها، در ماشین حساب ورق امکان انتخاب "جنس متریال" وجود دارد که بر اساس چگالی فلزات مختلف، وزن را تغییر می‌دهد).*

### ۵.۲. جدول استاتیک وزنی
برای سایر محصولات (میلگرد، تیرآهن، نبشی، لوله)، ماشین حساب فرمی وجود ندارد. در عوض، جدولی استاتیک شامل ستون‌های "سایز"، "وزن هر متر طول" و "وزن هر شاخه (معمولاً ۱۲ متری)" درج شده است که کاربر باید به صورت دستی وزن مورد نیاز خود را از روی آن محاسبه کند.


###########################################################
DOCUMENT E (submitted as document index 6)
###########################################################

# مستند تفصیلی داده‌ای کاتالوگ آهن‌آلات (سطح عمیق)
**منابع استخراج داده:** آهن‌آنلاین (AhanOnline)، مرکزآهن (MarkazAhan)، اصفهان آهن (EsfahanAhan)

---

## ۱. جزئیات ساختاری ورق‌ها (مثال پیچیده کاتالوگ)
دسته ورق‌ها در سایت‌های مرجع دارای پیچیده‌ترین حالت نمایش و قیمت‌گذاری هستند و به دو زیرگروه رفتار متفاوت تقسیم می‌شوند:

* **ورق‌های رند بازار (برش خورده استاندارد):**
    * ابعاد ثابت و مشخصی دارند که در صنعت شناخته شده هستند (مثل ۱۰۰×۲۰۰ سانتی‌متر، ۱۵۰×۳۰۰، ۱۲۵×۲۵۰، ۲×۶ متر).
    * در جدول قیمت‌ها، برای هر ضخامت (مثلاً ضخامت ۱.۵ میل)، ردیف‌های مجزایی برای هر "ابعاد رند" وجود دارد.
    * قیمت نمایش داده شده برای این ورق‌ها معمولاً "قیمت هر برگ" یا "قیمت هر کیلوگرم بر اساس وزن ثابت آن برش" است.
* **ورق‌های پالتینگ / شیت (برش آزاد):**
    * ابعاد مشخصی ندارند.
    * در سایت‌ها به صورت یک ردیف با عنوان "ورق برش آزاد" یا "پالتینگ" Listed می‌شوند.
    * قیمت آن‌ها صرفاً بر اساس کیلوگرم اعلام می‌شود و خریدار باید ابعاد مورد نیاز خود را محاسبه کرده و ضربدر قیمت کیلویی کند.
* **زیرنوع‌های تعریف شده:** ورق سیاه (ST37، ST52)، ورق روغنی (رنگی، ساده، پرس‌رنگی)، ورق گالوانیزه (رنگی، سفید/مات).

---

## ۲. جزئیات ساختاری پروفیل‌ها (قوطی)
در این دسته، متغیر "نوع تولید" به عنوان یک ویژگی حیاتی در کنار ابعاد و ضخامت کاتالوگ می‌شود:

* **تفاوت قوطی مبلی و صنعتی:** سایت‌های مرجع این دو را به عنوان دو زیردسته مجزا با قیمت‌های متفاوت لیست می‌کنند.
    * *قوطی مبلی:* تولید شده از نوارگالوانیزه، لبه‌های گرد شده دارند، ضخامت‌های معمولاً زیر ۱.۵ میل.
    * *قوطی صنعتی:* تولید شده از ورق سیاه یا گالوانیزه با لبه‌های ۹۰ درجه تیز، ضخامت‌های بالا (بالای ۱.۵ میل).
* **قانون یکپارچگی قیمت بر اساس ضخامت (مثال واقعی از جداول):**
    * قوطی صنعتی ۴۰×۴۰ ضخامت ۲ میل = قیمت X
    * قوطی صنعتی ۸۰×۴۰ ضخامت ۲ میل = قیمت X
    * قوطی صنعتی ۶۰×۶۰ ضخامت ۲ میل = قیمت X
    * (تغییر اعداد ابعاد در داخل یک ضخامت مشخص، هیچ تاثیری روی عدد قیمت کیلویی در جدول ندارد).

---

## ۳. جزئیات ساختاری میلگرد و گریدبندی سایزها
میلگردها بر اساس رفتار قیمت‌گذاری بازار به شکل خاصی در فیلترها و جداول دسته‌بندی می‌شوند:

* **خوشه‌بندی سایزها:** در پنل‌های فیلتر سایدبار سایت‌های مرجع، سایزها به صورت زیر چیده می‌شوند تا با منطق پلکانی همخوانی داشته باشند:
    * سایزهای ریز: ۸، ۱۰، ۱۲
    * سایزهای نرمال (استاندارد): ۱۴، ۱۶، ۱۸، ۲۰، ۲۲، ۲۵، ۲۸
    * سایزهای درشت: ۳۰، ۳۲، ۳۶
* **معرفی برند به عنوان یک ویژگی قیمت‌ساز:** در میلگرد، "کارخانه تولیدی" فقط یک برند نمایشی نیست؛ بلکه در جداول مرجع، برای یک سایز واحد (مثلاً میلگرد ۱۸)، قیمت کارخانه کاوه با قیمت کارخانه ذوب آهن یا فولاد خوزستان متفاوت Listed می‌شود. بنابراین برند یک فیلتر و متغیر موثر روی قیمت است.
* **واحد شمارش استاندارد:** در جداول وزنی استاتیک میلگرد، واحد سنجش همیشه "وزن یک شاخه ۱۲ متری" بیان می‌شود (مثلاً میلگرد ۱۸، هر شاخه ۲۴ کیلوگرم).

---

## ۴. جزئیات ساختاری لوله‌ها و نبشی‌ها (مدیریت دوگانه واحدها)
این دو دسته شفاف‌ترین مثال‌ها برای نحوه مدیریت اختلاف واحد بازار و واحد مهندسی در دیتابیس هستند:

* **لوله‌ها:**
    * *ستون سایز در جدول:* همیشه با اینچ نوشته می‌شود (مثلاً ۱/۲، ۳/۴، ۱، ۱.۲۵، ۱.۵، ۲، ۲.۵، ۳، ۴).
    * *ستون ضخامت:* همیشه با میلی‌متر نوشته می‌شود (مثلاً ۱.۵، ۲، ۲.۵، ۳).
    * *زیردسته‌ها:* لوله مانیسمان (بدون درز) و لوله درزدار (لوله سیاه، لوله گالوانیزه، لوله سفید). قیمت لوله درزدار با لوله مانیسمان با قطر و ضخامت یکسان کاملاً متفاوت است.
* **نبشی‌ها:**
    * *ستون سایز در جدول:* با اینچ و میلی‌متر ترکیبی نوشته می‌شود. مثال واقعی از جداول: "نبشی ۲ * ۲ * ۴ (۲ اینچ)" یا "نبشی ۳ * ۳ * ۳ (۱.۵ اینچ)".
    * *ساختار:* بال مساوی (مثلاً ۳*۳) و بال نامساوی (مثلاً ۳*۴ یا ۴*۵).

---

## ۵. معماری ماشین حساب‌های وزنی موجود در سایت‌ها
ابزار محاسبه وزن در این سایت‌ها اطلاعات را به این شکل دریافت و پردازش می‌کنند:

* **ورودی‌های ماشین حساب ورق:**
    ۱. جنس متریال (فقط در یک مرجع وجود دارد): آهن/فولاد، آلومینیوم، مس، برنج، استیل. (با تغییر هر کدام، ضریب چگالی در فرمول عوض می‌شود).
    ۲. ضخامت (عدد، به طور ضمنی میلی‌متر).
    ۳. عرض (عدد، به طور ضمنی میلی‌متر).
    ۴. طول (عدد، به طور ضمنی میلی‌متر یا متر، بسته به رابط کاربری).
* **ورودی‌های ماشین حساب پروفیل:**
    ۱. نوع مقطع: قوطی مربعی، قوطی مستطیلی، پروفیل استیل، لوله.
    ۲. ضخامت (عدد).
    ۳. ابعاد خارجی (عدد).
* **محصولات فاقد ماشین حساب فرمی:** تیرآهن، میلگرد، شمش، نبشی. برای این موارد فقط یک جدول متنی (Markdown Table) قرار دارد که در آن "سایز" در مقابل "وزن متر طولی" و "وزن شاخه ۱۲ متری" قرار گرفته است.

---

## ۶. ماتریس نمایش قیمت و ارزش افزوده در جداول
نحوه نمایش قیمت در کاتالوگ این سایت‌ها از یک الگو مشخص پیروی می‌کند:

* **قیمت پایه:** عدد به ازای هر کیلوگرم (با احتساب ۱۰٪ ارزش افزوده در تمامی سایت‌های مذکور).
* **سرستون‌های جدول قیمت (بسیار مهم):** جدول قیمت‌ها معمولاً دو یا سه سرستون اصلی دارند که ترتیب آن‌ها در سایت‌ها این است:
    ۱. ستون مشخصات محصول (سایز، ابعاد، ضخامت).
    ۲. ستون "قیمت خرده" (برای خریدهای زیر یک تن یا زیر ۵۰۰ کیلو - عدد بالاتر).
    ۳. ستون "قیمت نیمه‌فروشی" یا "قیمت عمده" (برای خریدهای بالای یک تن یا بالای ۱۰ تن - عدد پایین‌تر).
* **بدون تفکیک مالیاتی:** در هیچ‌کدام از این سایت‌ها در جدول اصلی کاتالوگ، ستونی به نام "قیمت بدون ارزش افزوده" وجود ندارد. فقط یک عدد نهایی نمایش داده می‌شود.


###########################################################
DOCUMENT F (submitted as document index 7)
###########################################################

# سند مرجع ماشین‌حساب‌های محاسبه وزن در سایت‌های آهن آنلاین و مرکزآهن

**منابع:**
- ahanonline.com (صفحات جدول وزن ورق سیاه و جدول وزن آهن‌آلات)
- markazeahan.com (صفحات جدول وزن پروفیل و جدول وزن ورق)

---

## ۱. ماشین‌حساب و جداول وزن ورق

### ۱.۱. محل قرارگیری در سایت‌ها
- **آهن آنلاین:** صفحه «جدول وزن ورق سیاه + فرمول محاسبه وزن ورق آهن» و بخش «جدول وزن آهن‌آلات».
- **مرکزآهن:** صفحه «جدول وزن ورق آهن + فرمول محاسبه وزن ورق».

### ۱.۲. ورودی‌های مورد نیاز ماشین‌حساب
- طول ورق (متر)
- عرض ورق (متر)
- ضخامت ورق (میلی‌متر)
- چگالی (عدد ثابت یا قابل انتخاب)

### ۱.۳. فرمول مورد استفاده
وزن ورق (کیلوگرم) = طول (متر) × عرض (متر) × ضخامت (میلی‌متر) × چگالی

**چگالی‌های اعلام‌شده در سایت‌ها:**
- آهن / فولاد کربنی / ورق سیاه / گالوانیزه / روغنی / رنگی: **۷.۸۵** یا **۷.۸۶**
- استیل: **۷.۸۴**
- آلومینیوم: **۲.۷**
- گاهی برای سهولت محاسبات عدد **۸** در نظر گرفته می‌شود.

### ۱.۴. خروجی ماشین‌حساب
- وزن تقریبی واحد (کیلوگرم)
- وزن تقریبی نهایی (کیلوگرم) در صورت وارد کردن تعداد

### ۱.۵. جدول نمونه وزن ورق سیاه (آهن آنلاین – فولاد مبارکه)

| طول (mm) | عرض (mm) | ضخامت (mm) | وزن هر شیت (kg) |
|----------|----------|------------|-----------------|
| ۲۰۰۰     | ۱۰۰۰     | ۲          | ۳۲              |
| ۲۵۰۰     | ۱۲۵۰     | ۲          | ۵۰              |
| ۶۰۰۰     | ۱۵۰۰     | ۳          | ۲۱۲             |
| ۶۰۰۰     | ۱۵۰۰     | ۴          | ۲۸۳             |
| ۶۰۰۰     | ۱۵۰۰     | ۵          | ۳۵۳             |
| ۶۰۰۰     | ۱۵۰۰     | ۶          | ۴۲۴             |
| ۶۰۰۰     | ۱۵۰۰     | ۸          | ۵۶۵             |
| ۶۰۰۰     | ۱۵۰۰     | ۱۰         | ۷۰۶             |

### ۱.۶. نکته اعلام‌شده در سایت‌ها
وزن به‌دست‌آمده از فرمول تقریبی است. وزن واقعی پس از تحویل و قرار گرفتن محصول روی باسکول مشخص می‌شود.

---

## ۲. ماشین‌حساب و جداول وزن پروفیل (قوطی)

### ۲.۱. محل قرارگیری در سایت‌ها
- **مرکزآهن:** صفحه «جدول وزن پروفیل آهن + محاسبه آنلاین وزن هر شاخه قوطی».
- **آهن آنلاین:** بخش جدول وزن آهن‌آلات شامل فرمول پروفیل.

### ۲.۲. ورودی‌های مورد نیاز ماشین‌حساب
- ابعاد مقطع (طول و عرض به میلی‌متر یا متر)
- ضخامت (میلی‌متر)
- طول شاخه (متر – معمولاً ۶ یا ۱۲)
- تعداد شاخه (اختیاری)

### ۲.۳. فرمول مورد استفاده در مرکزآهن
وزن پروفیل = ۲ × (طول مقطع + عرض مقطع) × ضخامت × طول شاخه × چگالی ۷.۸۶

**فرمول دقیق‌تر (با در نظر گرفتن ضخامت داخلی):**
وزن هر متر (kg/m) = [(H + W − ۲ × t) × ۲ × t] × ۷.۸۶

که در آن:
- H = ارتفاع مقطع (متر)
- W = عرض مقطع (متر)
- t = ضخامت (متر)

### ۲.۴. خروجی ماشین‌حساب
- وزن هر متر (kg/m)
- وزن هر شاخه ۶ متری یا ۱۲ متری
- وزن کل در صورت وارد کردن تعداد شاخه

### ۲.۵. جدول نمونه وزن پروفیل مربع ضخامت ۲ میلی‌متر (مرکزآهن)

| سایز (mm) | وزن هر متر (kg/m) | وزن شاخه ۶ متری (kg) |
|-----------|-------------------|----------------------|
| ۲۰×۲۰     | ۱.۱۸              | ۷.۰۸                 |
| ۲۵×۲۵     | ۱.۵۳              | ۹.۱۸                 |
| ۳۰×۳۰     | ۱.۷۸              | ۱۰.۶۸                |
| ۳۵×۳۵     | ۲.۱۶              | ۱۲.۹۶                |
| ۴۰×۴۰     | ۲.۴۵              | ۱۴.۷۰                |
| ۴۵×۴۵     | ۲.۸۱              | ۱۶.۸۶                |
| ۵۰×۵۰     | ۳.۱۱              | ۱۸.۶۶                |
| ۶۰×۶۰     | ۳.۷۴              | ۲۲.۴۴                |
| ۷۰×۷۰     | ۴.۳۸              | ۲۶.۲۸                |
| ۸۰×۸۰     | ۴.۹۷              | ۲۹.۸۲                |
| ۹۰×۹۰     | ۵.۶۸              | ۳۴.۰۸                |

### ۲.۶. جدول نمونه وزن پروفیل ۵۰×۵۰ با ضخامت‌های مختلف

| ضخامت (mm) | وزن هر متر (kg/m) | وزن شاخه ۶ متری (kg) |
|------------|-------------------|----------------------|
| ۱.۵        | ۲.۳۳              | ۱۳.۹۸                |
| ۲.۰        | ۳.۱۱              | ۱۸.۶۶                |
| ۲.۵        | ۳.۸۴              | ۲۳.۰۴                |
| ۳.۰        | ۴.۶۲              | ۲۷.۷۲                |
| ۳.۵        | ۵.۳۳              | ۳۱.۹۸                |
| ۴.۰        | ۶.۰۶              | ۳۶.۳۶                |

---

## ۳. ماشین‌حساب وزن پروفیل زد (Z)

### ۳.۱. محل قرارگیری
- **مرکزآهن:** صفحه «جدول وزن پروفیل زد + محاسبه وزن انواع پروفیل Z».

### ۳.۲. فرمول مورد استفاده
وزن پروفیل زد (کیلوگرم) = (طول × عرض × ضخامت × چگالی) / ۱۰۰۰
(واحدها: سانتی‌متر برای طول و عرض، میلی‌متر برای ضخامت)

### ۳.۳. نمونه وزن واحد طول پروفیل زد

| ارتفاع | ضخامت ۲ mm | ضخامت ۲.۵ mm | ضخامت ۳ mm |
|--------|------------|--------------|------------|
| Z16    | ۴.۹ kg/m   | ۶.۰ kg/m     | ۷.۲ kg/m   |
| Z18    | ۵.۲ kg/m   | ۶.۵ kg/m     | ۷.۷ kg/m   |
| Z20    | ۵.۵ kg/m   | ۶.۸ kg/m     | ۸.۰ kg/m   |

---

## ۴. ویژگی‌های مشترک ماشین‌حساب‌ها در سایت‌های مرجع

- محاسبات بر اساس چگالی استاندارد فولاد (۷.۸۵ یا ۷.۸۶) انجام می‌شود.
- وزن به‌دست‌آمده **نظری** است و ممکن است با وزن باسکول تفاوت جزئی داشته باشد.
- امکان محاسبه برای تعداد شاخه یا تعداد شیت وجود دارد.
- جداول وزن به‌صورت ثابت در کنار ماشین‌حساب نمایش داده می‌شوند.
- برای محصولات ناموجود یا سفارشی، ماشین‌حساب همچنان وزن نظری را محاسبه می‌کند اما قیمت با عبارت «تماس بگیرید» اعلام می‌شود.
- واحد نهایی خروجی همیشه کیلوگرم است.

---

## ۵. خلاصه فرمول‌های اصلی مورد استفاده

**ورق:**
وزن (kg) = طول (m) × عرض (m) × ضخامت (mm) × ۷.۸۵

**پروفیل قوطی:**
وزن هر متر (kg/m) = [(H + W − ۲t) × ۲ × t] × ۷.۸۶
(واحدها به متر)

**پروفیل زد:**
وزن (kg) = (طول × عرض × ضخامت × چگالی) / ۱۰۰۰

این فرمول‌ها و ساختار ماشین‌حساب‌ها مستقیماً از صفحات جدول وزن و محاسبه آنلاین در ahanonline.com و markazeahan.com استخراج شده‌اند.


###########################################################
DOCUMENT G (submitted as document index 8)
###########################################################

**سند مرجع قیمت‌گذاری پروفیل (قوطی) بر اساس سایت‌های آهن آنلاین و مرکزآهن**

**منابع:**
- markazeahan.com (صفحات قیمت پروفیل، قوطی پروفیل، پروفیل صنعتی، پروفیل زد، پروفیل گالوانیزه و کارخانه‌های مختلف)
- ahanonline.com (صفحات قیمت پروفیل و پروفیل صنعتی)

---

### ۱. واحد قیمت‌گذاری
- قیمت پروفیل در هر دو سایت **بر اساس کیلوگرم** اعلام می‌شود.
- قیمت نهایی هر شاخه یا هر سفارش = قیمت کیلویی × وزن محاسبه‌شده (نظری یا باسکول).
- طول استاندارد شاخه در جداول معمولاً **۶ متر** است (گاهی ۱۲ متر برای پروفیل صنعتی).

### ۲. منطق اصلی قیمت‌گذاری بر اساس ضخامت
در جداول مرکزآهن و آهن آنلاین:
- **همه پروفیل‌های با ضخامت یکسان**، قیمت کیلویی یکسانی دارند (مستقل از ابعاد مقطع).
- مثال واقعی از جداول مرکزآهن:
  - تمام پروفیل‌های ضخامت ۲ میلی‌متر یک کارخانه → قیمت کیلویی یکسان
  - تمام پروفیل‌های ضخامت ۲.۵ میلی‌متر همان کارخانه → قیمت کیلویی یکسان (معمولاً کمی متفاوت از ضخامت ۲)
  - تمام پروفیل‌های ضخامت ۳ میلی‌متر → قیمت کیلویی یکسان

ابعاد مختلف (۲۰×۲۰، ۴۰×۴۰، ۶۰×۶۰، ۱۰۰×۱۰۰ و ...) فقط وزن شاخه را تغییر می‌دهند، نه قیمت کیلویی را.

### ۳. ساختار جداول قیمت در سایت‌های مرجع
هر ردیف جدول معمولاً شامل این ستون‌ها است:
- ابعاد (mm)
- ضخامت (mm)
- طول (m)
- وزن هر شاخه (kg)
- محل بارگیری (کارخانه / بنگاه تهران / بنگاه اصفهان)
- قیمت (تومان) – گاهی با ذکر «بدون ارزش افزوده» یا «با ارزش افزوده»
- نمودار تغییرات قیمت
- وضعیت خرید (موجود یا «تماس بگیرید»)

### ۴. نمونه واقعی قیمت‌گذاری بر اساس ضخامت (مرکزآهن)

**پروفیل ضخامت ۲ میلی‌متر (یک کارخانه):**
- همه سایزها (۲۰×۲۰ تا ۹۰×۹۰ و ...) → قیمت کیلویی یکسان (مثلاً ۶۷٬۹۴۵ تومان یا ۷۰٬۰۰۰ تومان بسته به تاریخ و کارخانه)

**پروفیل ضخامت ۲.۵ میلی‌متر:**
- همه سایزها → قیمت کیلویی یکسان (معمولاً کمی بالاتر یا پایین‌تر از ضخامت ۲)

**پروفیل ضخامت ۳ میلی‌متر:**
- همه سایزها → قیمت کیلویی یکسان

**پروفیل صنعتی (ضخامت بالاتر ۴، ۵، ۶ میلی‌متر و بیشتر):**
- قیمت کیلویی جداگانه و معمولاً متفاوت از پروفیل ساختمانی سبک

### ۵. تفاوت قیمت بر اساس کارخانه و محل بارگیری
- قیمت هر کارخانه جداگانه اعلام می‌شود (یزد، مهر سهند تبریز، اصفهان، تهران شرق، کالوپ، نیکان، صابری و ...).
- محل بارگیری (کارخانه یا بنگاه) در جدول مشخص است و بر قیمت تأثیر دارد.
- برخی ردیف‌ها با عبارت **«تماس بگیرید»** نمایش داده می‌شوند (موجودی نامشخص یا قیمت سفارشی).

### ۶. پروفیل‌های خاص
- **پروفیل گالوانیزه:** قیمت کیلویی جداگانه و معمولاً بالاتر از پروفیل سیاه.
- **پروفیل زد (Z):** قیمت بر اساس ضخامت و ارتفاع اعلام می‌شود و اغلب قیمت کیلویی یکسان برای ضخامت مشابه دارد.
- **پروفیل صنعتی سنگین:** قیمت کیلویی بر اساس ضخامت و گاهی سایز بزرگ‌تر متفاوت است.
- **پروفیل مبلی / سبک:** ضخامت پایین‌تر (۱ تا ۱.۵ میلی‌متر) و قیمت کیلویی معمولاً بالاتر.

### ۷. ارزش افزوده
- در بسیاری از جداول مرکزآهن قیمت به‌صورت «بدون ارزش افزوده» یا «با ارزش افزوده» مشخص می‌شود.
- در برخی صفحات قیمت نهایی با احتساب ۱۰٪ ارزش افزوده نمایش داده می‌شود یا اعلام می‌شود که قیمت‌ها شامل ارزش افزوده است.

### ۸. نحوه محاسبه قیمت نهایی در عمل
۱. مشتری ابعاد و ضخامت را مشخص می‌کند.
۲. وزن نظری شاخه از جدول یا فرمول محاسبه می‌شود.
۳. قیمت کیلویی مربوط به آن ضخامت و کارخانه اعمال می‌شود.
۴. قیمت نهایی = قیمت کیلویی × وزن.
۵. در پیش‌فاکتور مقدار بر اساس وزن (کیلوگرم) ثبت می‌شود.

### ۹. نکات ثابت مشاهده‌شده در سایت‌های مرجع
- قیمت‌ها روزانه به‌روزرسانی می‌شوند.
- تغییرات قیمت با عبارت‌هایی مثل «بدون تغییر»، «افزایش X تومان» یا نمودار نشان داده می‌شود.
- قیمت پروفیل ساختمانی سبک (ضخامت ۲ و ۲.۵) معمولاً نزدیک به هم است.
- پروفیل‌های خیلی سبک یا خیلی سنگین ممکن است قیمت کیلویی متفاوتی داشته باشند.
- برای موجودی‌های نامشخص یا ابعاد غیراستاندارد، به‌جای قیمت عددی عبارت «تماس بگیرید» درج می‌شود.

این ساختار قیمت‌گذاری (قیمت بر اساس ضخامت، واحد کیلوگرم، وزن نظری در جدول، و وضعیت موجودی) مستقیماً از جداول قیمت پروفیل در markazeahan.com و ahanonline.com استخراج شده است.

===========================================================
END OF COMPETITOR REFERENCE DOCUMENTS (B-G).
===========================================================

===========================================================
PART 3 BEGINS BELOW (PROJECT KNOWLEDGE BASE, FULL, UNABRIDGED)
===========================================================

PROJECT KNOWLEDGE BASE
HyperAhan - Catalog and Pricing Modules
Business Domain Analysis - Full Extraction
Status: Phase 3 in progress, through Step 6 of 11. Steps 7-11 not yet executed.
Scope lock: Only Catalog and Pricing modules are in active design scope. Order, Discount, Auth, Notification, Blog modules exist in the overall system but are deferred to later, separate decisions. System is a Modular Monolith, Clean Architecture.

===========================================================
1. SOURCE DOCUMENTS OVERVIEW
===========================================================

Document A (primary, authoritative): Internal business document from Foolad Shahyar Sepand (steel trading company). Version 1.0, dated 1405/05/01. This is the ONLY document that represents the actual business. It explicitly flags open ambiguities.

Documents B-F (competitor reference data): Extracted catalog/pricing/weight-calculator structures from AhanOnline, MarkazAhan, EsfahanAhan. These are NOT business rules for Foolad Shahyar Sepand by default. They only became authoritative facts when the business owner explicitly confirmed adoption of a specific point (see Section 3, item on product domain scope).

Rule applied throughout: competitor data = reference knowledge only, never auto-promoted to business rule without explicit confirmation.

===========================================================
2. TERMINOLOGY (Ubiquitous Language)
===========================================================

- Sale Mode (نحوه فروش): A property of a product (or product category) with three known values:
  1. Fully online - price shown, "add to cart" available, no phone contact needed.
  2. Semi-custom (e.g., I-beam) - default standard size is fully online/purchasable; a custom variation requires phone contact.
  3. Fully custom/contact-only (e.g., wall-post, bolts) - price is not predetermined; requires phone call. Can still appear in public catalog without a price and with a "contact expert" button.
  Open minor question (unresolved, low priority): whether Sale Mode is a closed 3-value enum or extensible in the future.

- Inventory Status (وضعیت موجودی): Independent of Sale Mode. Represents real-time stock availability. Confirmed to be a per-product configurable setting (see Section 5).

- Base Size (سایز پایه): For semi-custom products like I-beam, the standard size (e.g., 12-meter bar) on which default online pricing is based. Any deviation from Base Size is custom and requires phone contact.

- Call Path A / Call Path B (مسیر الف / مسیر ب): Two paths for phone-based orders.
  - Path A: Admin builds the order in the system and issues a payment link/notification to the customer.
  - Path B: Entire transaction is manual/offline (agreement and payment outside the system); admin later enters the order into the system only for reporting/statistics.

- Fabrik / Persi (فابریک / پرسی): Two fully independent products/variants, NOT a shared field on one product. Confirmed explicitly.

- Market slang terms - سبک/سنگین (light/heavy), نیمه (half), لقمه (cut piece): Currently stored only in the product's display name, not as structured fields, unless a direct, formulable effect on price is proven later. لقمه (Loghme) specifically is a derived/cut product from a raw product, not a new raw material.

- Tiered/Volume Pricing (قیمت‌گذاری پلکانی): Discount based on purchase volume (weight or quantity). Calculated per-item (not per-cart). Live/unlocked - recalculates automatically as cart quantity changes. Exact tier boundaries are unresolved and explicitly called out by the business owner as "part of our creativity and competitive advantage."

- Base Market Price: The confirmed-final term is: there is NO price lock anywhere in the system (see Section 7, this superseded an earlier reading of Document A). Price is always read live from backend up through checkout.

- Obsoleted Price Record (رکورد قیمت منسوخ): A price history record can be marked obsolete so it's excluded from reporting, but it is NEVER deleted or edited. Append-only history.

- Ancillary Cost (هزینه جانبی): E.g., freight/shipping cost. Recorded as an independent line item in the order/invoice, never folded into the product price. Currently manual entry by admin only (no automatic formula yet); backend may return a default fixed number.

- Registration Unit / Sale Unit / Display Unit / Pricing Unit (واحد ثبت / واحد فروش / واحد نمایش / واحد قیمت‌گذاری): Four independent unit concepts (see Section 6). A fixed numeric Conversion Factor relates them where needed.

- Formula (فرمول قیمت‌گذاری اختصاصی): An admin-defined formula, per product, that computes the effective quantity/multiplier from the product's Specification values. This same formula engine also powers the public-facing weight calculator toolbox. Confirmed: the formula NEVER outputs final price directly - see Section 7.

- Price Entry (working name, not finalized): The pricing record tied to one specific full combination of a product's Specification values (including but not limited to factory/brand). Formerly assumed to be keyed only by factory; corrected in Step 6 (see Section 9).

===========================================================
3. CONFIRMED BUSINESS FACTS - CATALOG DOMAIN
===========================================================

3.1 Product classification by sale behavior (Document A, Section 1)
Three categories of how steel products are sold:
- Category 1: Standard products, fully online (rebar, sheet, angle iron, channel, profile, etc.). Price shown directly, cart + online payment supported.
- Category 2: Semi-custom (e.g., I-beam). Standard base size has default online price and is purchasable online. Custom variation (different length, special cut) requires phone contact. CONFIRMED: base/standard size is priced and sold online; only deviations from standard size require a phone call. This pattern (base size online, custom variant phone-only) applies at minimum to I-beam and is treated as a general pattern for any product with a recognized standard/base size.
- Category 3: Fully custom (e.g., wall-post, bolts). Made from raw materials plus ancillary costs (welding, bending, labor). Price is not predetermined; fully phone/order dependent. These products DO appear in the public catalog, but without price and with a "contact expert" button (this directly overturned an earlier statement in the same document that said these products don't necessarily need public catalog display - the later statement supersedes it and is the confirmed rule).

Design implication (confirmed): Every product (or product category) must carry a Sale Mode attribute determining whether the UI shows "add to cart," "contact/request quote," or is admin-panel-only with no public catalog listing.

3.2 Product domain scope (CONFIRMED, upgraded from inference to fact)
- Current focus: iron/steel products.
- Future expansion (explicitly planned by business owner): construction materials, nails, screws, and similar - i.e., NOT limited to steel.
- Business owner explicitly stated twice: expects the system to register and price a wide variety of products.
- CONCLUSION (confirmed, high confidence): Product domain breadth = same breadth as competitors (AhanOnline/MarkazAhan/EsfahanAhan reference catalogs), PLUS future non-steel categories. This is the one place where competitor reference data was explicitly promoted to a confirmed business fact for Foolad Shahyar Sepand.

Competitor reference catalog breadth (context data, now relevant because scope was confirmed to match it):
- Long products: rebar (simple, ribbed/A2-A3-A4, coil), I-beam (I, H/INP, rail), profile/square-rectangular tube, angle iron, channel, wire products (wire, mesh, rabitz).
- Flat products: hot-rolled sheet (black, A516, A283, ST52, wear-resistant, ribbed, pickled, slab), cold-rolled (oiled), coated sheet (galvanized, colored, stainless), forming sheet (corrugated, shadoline, trapezoidal, sinusoidal, tile), punched sheet, strip, square bar, grating.
- Intermediate/raw: billet, slab, bloom, sponge iron, scrap.
- Pipe: seamless (manesman), seamed (black pipe, galvanized pipe, furniture pipe).

3.3 Fabrik/Persi and other market terms (Document A, Section 6)
- Fabrik and Persi MUST be two fully separate products/variants in the catalog, not one shared field.
- Other market terms (light/heavy, half, cut-piece) currently go in display name only; upgrade to structured field only if proven to have a direct, formulable price effect.
- Low-priority open item: complete list of all similar market terms is not yet known; business owner confirmed this is low priority and will be completed later.

3.4 Structured (price-making) variables (Document A, Section 6)
Size, thickness, length, grade, and manufacturing factory MUST be structured fields (not free text), because they are the direct basis for pricing and filter/search.

3.5 Inventory/catalog display logic (RESOLVED CONTRADICTION - see Section 10)
- Original Document A statement (ambiguous/self-contradictory): "Site should only show what is truly purchasable online; out-of-stock products should not appear at all in the catalog" vs. "or at admin's discretion can be shown with a contact-expert action."
- FINAL RESOLUTION (confirmed): This is an admin-configurable, PER-PRODUCT setting, not a system-wide fixed policy. Three possible states for an out-of-stock product:
  1. Fully hidden from catalog.
  2. Shown with an "out of stock" tag, with NO action button at all (neither add-to-cart nor contact).
  3. Shown with a "contact expert" button.
- Business rationale for state 3 (confirmed): allows admin to consult the customer, offer a substitute product, or get a quote from another supplier and relay it to the customer.
- Business rationale for state 2: some out-of-stock products simply have no action at all.
- Sale Mode and Inventory Status/display-on-out-of-stock are CONFIRMED to be two fully independent attributes on the product. The final displayed behavior in the catalog is the combined result of both independent fields; they must not be collapsed into a single field. A "contact" action can originate from EITHER a fully-custom Sale Mode OR from the independent out-of-stock display setting - two different fields can produce the same visible UI action.

3.6 Aggregate/modeling decision for Product (CONFIRMED, Step 3)
- Product is modeled as a SINGLE, GENERIC aggregate type, not one aggregate type per product family (not one class for rebar, another for sheet, another for profile, etc.).
- Reasoning (confirmed by business owner): explicit, repeated statements that the system must support registering diverse products, plus explicit confirmation that future expansion goes beyond steel into construction materials/nails/screws. A per-family aggregate type approach would require code changes/migrations for every new product family, directly conflicting with this requirement.
- Each Product carries a structured, category-dependent Specification (see Section 6.6 for exact shape decision).
- Fabrik and Persi are modeled as two independent instances/variants of this same generic Product aggregate (exact variant-vs-independent-instance mechanics deferred, not yet finalized - open item, low priority, does not block progress).
- Every Product has (at minimum) these two independent top-level attributes, confirmed in Step 5: (a) Sale Mode, (b) Out-of-stock display behavior (one of the three states in 3.5). These are general Product-level fields, NOT part of the category-dependent Specification.

===========================================================
4. CONFIRMED BUSINESS FACTS - ORDER PROCESS (SUPPORTING DOMAIN, OUT OF CURRENT DESIGN SCOPE BUT DOCUMENTED)
===========================================================

Note: Order module is explicitly deferred from current design focus (business owner instruction), but the following facts were established before that scope-narrowing instruction and remain valid business knowledge for whenever that module is designed.

4.1 Phone order process (Document A, Section 2)
1. Customer calls and states request.
2. Admin determines final price (including any ancillary costs).
3. Two possible paths:
   - Path A: Admin builds the entire order in the system; customer receives a link/notification to log in, view order and price, and pay.
   - Path B: Entire transaction is phone-based and manual (agreement, payment outside system); admin enters the order into the system afterward only for reporting/statistics.
4. General principle: whatever CAN be automated should be done on the website. Phone path is used only after negative feedback or when the process is genuinely too complex. Online orders must ultimately be recorded in the same order-management system (not a separate, disconnected path).

Design implication (confirmed): Admin panel must be able to build an order FROM SCRATCH (not just manage customer-submitted orders) - including product selection, manual pricing (outside standard tiers if needed), adding ancillary costs, and issuing a payment link.

4.2 Minimum order quantity (Document A, Section 7-2)
Some products have a minimum sellable quantity (by weight or count) - below a certain threshold, the product cannot be ordered at all.

===========================================================
5. CONFIRMED BUSINESS FACTS - PRICING DOMAIN (CORE)
===========================================================

5.1 VAT / Tax (Document A, Section 8) - CONFIRMED, no contradictions
- All customer-facing prices INCLUDE 10% VAT.
- Two prices (with/without VAT) are NEVER shown side by side.
- Backend stores the base price WITHOUT VAT; VAT is applied only at final calculation.
- A fixed disclaimer text must appear on all relevant pages: "All quoted prices include 10% VAT and there are no other additional costs."
- Confirmed: this matches competitor behavior (no contradiction, just corroboration).

5.2 Price history (Document A, Section 9)
- Every price change is a new record (append-only). Prior records are NEVER deleted or edited.
- System always uses the latest record for display/calculation.
- Admins can view historical price reports.
- Price records CAN be marked obsolete so they are excluded from reports (obsolete != deleted; these are two distinct states and must be modeled separately).

5.3 Tiered/volume pricing (Document A, Section 7) - mechanism confirmed, exact tier values NOT yet confirmed
- Unit price depends on purchase volume (weight or count): smaller purchase = higher unit price, larger = lower.
- Tiers CAN be defined separately per product family, but system must also have a global default tier that can be overridden per category by admin.
- Tier calculation is PER-ITEM (per order line), NOT per-cart-total. If a customer buys two different products each below the wholesale threshold individually, neither gets the wholesale discount even if their sum would exceed it.
- If the customer increases order quantity in the cart (e.g., crossing from retail tier to wholesale tier), unit price updates LIVE and automatically to the new tier rate.
- CONFIRMED (Step 6): Tier discount boundaries/rules are defined completely INDEPENDENTLY of the main pricing Formula. When an admin defines a discount and applies it to a product or product group, it is applied on top of the FINAL price (i.e., on the output of the main pricing Formula, not blended into it).
- Open, high priority per Document A: exact tier boundaries (weight/count thresholds and price change per tier, per product family) not yet finalized - explicitly stated to be "part of our creativity and competitive advantage," to be decided in a dedicated session. Until then, tiers must be admin-panel editable without code changes.

5.4 NO PRICE LOCK (CONFIRMED IN STEP 6 - SUPERSEDES EARLIER READING, see Section 10 for the correction record)
- There is NO price lock anywhere in the order lifecycle, from order creation through payment. Price is always read live from the backend at every stage.
- If a product's price changes during the order process, this is an ACCEPTED, INEVITABLE business event, not a defect.
- If a pre-invoice (پیش‌فاکتور) was issued and the underlying price changes, that pre-invoice becomes EXPIRED. This is stated in the terms/agreement that the customer accepts when placing an order.
- Exact expiration trigger mechanics (e.g., does expiration happen immediately on price change, or only checked at order finalization) are NOT yet resolved - deferred to Step 9 (Invoice/Pre-invoice modeling), still pending as of this knowledge base.

5.5 Base price is keyed by FULL SPECIFICATION COMBINATION, not just factory (CONFIRMED, CORRECTED IN STEP 6)
- Earlier assumption (now superseded): base price varies only by manufacturing factory.
- CORRECTED, CONFIRMED FACT: a base price record exists per full, specific combination of a product's Specification values - factory/brand is just ONE of these dimensions, not the only one. Size, thickness, etc. are equally price-determining dimensions of the SAME price record key.
- Concrete confirmed examples given by business owner:
  - Rebar, Zob-Ahan factory, size 14 -> 73,000 Toman/kg
  - Rebar, Zob-Ahan factory, size 12 -> 74,000 Toman/kg
  - Rebar, Abarkooh factory, size 14 -> 73,500 Toman/kg
  - Nail, size 3, Brand X -> 20 Toman/unit
  - Nail, size 3, Brand Alex -> 2,000,000 Toman/package
  - Nail, size 3, Ashtarkooh -> 25,000 Toman/unit
- CRITICAL CONFIRMED FACT: the PRICING UNIT itself (per kg, per unit/piece, per package, etc.) is part of the price record, NOT a single fixed setting for the whole product. Different combinations of the same product family can be priced in entirely different units (e.g., nails priced per-package for one brand, per-unit for another).
- Working concept name (not finalized, deferred to technical design): something like "Price Entry" or "Product Variant Price," associated with one unique Specification-value combination.

5.6 Pricing Formula mechanics (CONFIRMED, Step 6, no exceptions identified)
- Two-tier formula system:
  1. DEFAULT (system-wide fallback): Final price = base price x order quantity (count or weight) + N% tax.
  2. CUSTOM (admin-defined per product): An admin-defined formula based on that product's Specification values.
- CONFIRMED RULE (no exceptions found as of this knowledge base): the admin-defined Formula NEVER computes final price directly. It ALWAYS computes only the effective consumed QUANTITY or MULTIPLIER (e.g., calculated weight from sheet dimensions) from the Specification. That computed quantity is then multiplied by the Base Price Entry that matches the specific Specification combination (including factory) to get the pre-tax, pre-discount price.
- This same Formula engine is REUSED by the public-facing weight-calculator toolbox on the website (dual consumer: internal order pricing, and public calculator tool). Relationship confirmed as Upstream (Pricing Engine) / Downstream (public calculator) - to be formalized in Step 10 Context Map.
- Technical delivery detail (explicitly NOT part of domain modeling, recorded only as a pre-existing technical decision by the business owner, to be revisited in the technical design phase): frontend fetches calculator form metadata from backend (calculator name, input fields, field types, and the formula itself); results must be cacheable.

5.7 Order of operations for final price calculation (derived/confirmed synthesis from 5.3 + 5.5 + 5.6)
1. Determine the exact Specification combination of the ordered product (including factory/brand).
2. Look up the Base Price Entry for that exact combination (this entry also carries its own pricing unit).
3. Run the pricing Formula (default or custom) to get the effective quantity/multiplier from the Specification.
4. Multiply: quantity/multiplier x base price of that Specification combination = pre-discount, pre-tax price.
5. Apply tiered/volume discount (independent layer, live, per-item) if applicable, on top of the result of step 4.
6. Apply VAT (10%, or admin-configured N%) as the final layer.
7. Store the pre-VAT price separately in the backend at all times (per 5.1).

===========================================================
6. CONFIRMED BUSINESS FACTS - UNIT MODEL (Step 4)
===========================================================

6.1 Four independent unit concepts (all confirmed):
- Registration Unit: the unit the product's Specification is defined in (e.g., kg for rebar).
- Sale Unit: the unit the customer orders in (weight, count, or both - per Document A Section 4).
- Pricing Unit: the unit the base price is defined in (e.g., Toman per kg) - CONFIRMED in Step 6 to be a per-Price-Entry property, not fixed per product.
- Display Unit: the unit shown to the customer, independently configurable by admin in the CATALOG and in the INVOICE (these two display contexts are configured separately).

6.2 Conversion Factor (CONFIRMED, Step 4)
- Always a FIXED NUMBER stored on the product (e.g., weight of one rebar bar = fixed kg value), NEVER the output of the pricing Formula. This keeps Conversion Factor and pricing Formula fully independent concepts.
- Enables converting total order weight into a count of pieces (or vice versa) for products where this applies. Must be admin-configurable.

6.3 Invoice unit display rule (CONFIRMED)
- Maximum of TWO units may be shown simultaneously on an invoice.
- In most cases, only ONE unit is shown.
- Admin controls which unit(s) display on the invoice, independently from the catalog display unit setting.

6.4 Simple vs. complex products (CONFIRMED, Step 4 - resolves earlier open question)
- NOT all products require the full four-unit distinction. Some products can be SIMPLE and use just ONE unit throughout (registration = sale = display = pricing), with NO conversion factor needed.
- Explicit examples given by business owner: box, count/piece, carton, trailer-load, container.
- More complex products (e.g., rebar, sheet, profile) can use the full four-unit distinction with conversion factors.

6.5 System-wide diverse standard units (CONFIRMED, Document A response)
- The system supports a variety of standard measurement units (not just kg and bar-count).
- Admin chooses which unit a product is registered in, and which unit pricing is based on.

6.6 Specification structure decision (CONFIRMED, Step 4)
- Specification is modeled as a TYPED structure (not raw free-form key-value pairs), because price/filter-critical fields (size, thickness, grade, factory, etc.) need structured validation and filterability.
- BUT: fields must be OPTIONAL/lightweight-by-default so a SIMPLE product (Section 6.4) is not forced into unnecessary complexity, while a COMPLEX product can still use the full structured model. This resolves the tension between "must be structured" (Document A) and "must support simple products with one unit" (business owner, Step 4).

===========================================================
7. CORRECTIONS AND SUPERSEDED CONCLUSIONS (EXPLICIT REGRESSION LOG)
===========================================================

CORRECTION 1 (raised and resolved at Step 6):
- WHAT CHANGED: The previously confirmed fact "Base market price is locked to prevent momentary fluctuation, unlike tier discounts which are never locked" (originally drawn directly from Document A Section 7-1) is now INVALID.
- WHY IT CHANGED: Business owner clarified that the "lock" language in Document A referred only to the span from order placement through payment, and that this was a confusing/overcomplicated way to describe it. Business owner explicitly stated: "we don't have a price lock, disregard this."
- WHAT REPLACES IT: No price lock exists anywhere. Price is always read live from backend through checkout. Price changes mid-order are accepted as inevitable; pre-invoices expire if affected prices change (customer has agreed to this in the terms). See Section 5.4.
- STATUS: Fully resolved and confirmed. Not silently overwritten - explicitly logged here per instruction.

CORRECTION 2 (raised and resolved at Step 6):
- WHAT CHANGED: The previously confirmed/derived fact "Base price is a per-factory record" (a Step 3 conclusion, tentatively named ProductFactoryPrice) is now INVALID as stated (too narrow).
- WHY IT CHANGED: Business owner's rebar and nail examples showed that price varies independently by factory AND by size AND (for nails) by pricing unit type (per-unit vs per-package) simultaneously - i.e., price is keyed by the FULL Specification combination, not factory alone.
- WHAT REPLACES IT: Base price ("Price Entry") is keyed by the full, specific combination of Specification values, of which factory is only one dimension. See Section 5.5.
- STATUS: Fully resolved and confirmed.

RESOLVED CONTRADICTION (originally flagged inside Document A itself, unresolved from Phase 1 through Step 4, resolved at Step 5):
- ORIGINAL CONTRADICTION: Document A Section 3 said out-of-stock products "should not appear in the catalog at all," but also said they "can be shown at admin's discretion with a contact-expert action." Document A's own Section 10 summary table flagged this explicitly as needing final confirmation.
- RESOLUTION: Both behaviors are correct and coexist - the choice is a PER-PRODUCT admin setting, not a mutually exclusive global rule. A third state was also confirmed: out-of-stock with a tag and NO action at all. See Section 3.5 for full detail.
- STATUS: Fully resolved, confirmed, three-state model locked.

REJECTED/AVOIDED ASSUMPTIONS (things Claude explicitly declined to assume without confirmation, per DDD instructions):
- Did NOT assume Sale Mode and Inventory display behavior were a single combined field - explicitly asked and confirmed they are two independent fields (Section 3.5/3.6).
- Did NOT assume the admin-defined Formula could directly set final price without a Base Price Entry - explicitly asked; business owner confirmed no such exception exists (Section 5.6).
- Did NOT assume all products need the full four-unit model - explicitly asked; business owner confirmed simple single-unit products exist (Section 6.4).
- Did NOT assume the Conversion Factor could be formula-derived - explicitly asked; confirmed it is always a fixed stored number (Section 6.2).
- Did NOT infer tier discount boundary values from competitor data (retail/wholesale ~1 ton threshold seen in competitor docs) as a business rule for Foolad Shahyar Sepand - this remains explicitly UNRESOLVED/OPEN per business owner's own flag that it's a competitive-advantage decision pending a dedicated session.
- Did NOT treat any competitor-derived formula or category structure as a Foolad Shahyar Sepand rule unless explicitly confirmed (the one explicit confirmation being overall product domain breadth, Section 3.2).
- Technical/architectural details volunteered by the business owner (Stimulsoft invoice rendering pipeline; frontend fetching calculator metadata with caching) were explicitly logged as pre-existing technical decisions but NOT incorporated into domain modeling, per the working method (business before technology).

===========================================================
8. ARCHITECTURAL DECISIONS WITH SUPPORTING BUSINESS EVIDENCE
===========================================================

DECISION 1: Product is a single generic aggregate with category-dependent Specification, not one aggregate type per product family.
- Evidence: repeated explicit statements about needing to register diverse products; explicit future expansion beyond steel to construction materials/nails/screws; per-family aggregates would require code/schema changes for every new category, conflicting with stated goals.
- Confidence: High (explicitly confirmed twice).

DECISION 2: Catalog and Pricing are BOTH Core Domains (Bounded-Context-level implication: likely a close/partnership relationship rather than typical core-vs-supporting distance; to be formalized in Step 10).
- Evidence: business owner explicitly named tiered pricing as "part of our creativity and competitive advantage" (Document A); business owner explicitly confirmed both Catalog and Pricing are core when asked directly.
- Confidence: High (explicit confirmation).

DECISION 3: Order Management, Ancillary Cost handling, and Invoice/Pre-invoice generation are Supporting subdomains; the public Weight Calculator is a Generic subdomain but is a downstream CONSUMER of the core Pricing engine's Formula capability (not a fully isolated generic subdomain).
- Evidence: no competitive-advantage language applied to these; weight calculator formulas are stated to be standard/industry formulas, BUT the calculator engine is explicitly the same engine used for admin-defined product pricing formulas (business owner confirmed reuse).
- Confidence: Medium-high for Supporting classification (inferred, not directly stated); High for the Formula-engine-reuse relationship (explicitly confirmed).

DECISION 4: Specification is a typed-but-optional structure; not raw key-value, not a rigid mandatory schema.
- Evidence: Document A requires structured, filterable fields (size, thickness, grade, factory); business owner confirmed simple single-unit products (box, carton, container) must NOT be forced into the complex four-unit / heavy Specification model.
- Confidence: High (both sides of the tension explicitly confirmed).

DECISION 5: Conversion Factor and Pricing Formula are fully independent mechanisms; Conversion Factor is always a static number, Formula always computes a variable quantity/multiplier consumed by the Base Price Entry lookup.
- Evidence: explicit business owner confirmation on both points in direct response to direct questions.
- Confidence: High.

DECISION 6: No price-locking mechanism anywhere in the system; live price lookup at every stage; pre-invoice expiration is the mechanism that handles price-change-during-order-lifecycle, backed by a customer-facing terms agreement.
- Evidence: explicit business owner correction and confirmation (Correction 1, Section 7).
- Confidence: High.

===========================================================
9. WORKING DOMAIN CONCEPTS (NAMES ARE PROVISIONAL, NOT TECHNICAL DESIGN)
===========================================================

- Product (aggregate): generic, category-agnostic, holds Sale Mode, Out-of-stock display setting, Specification, Registration Unit, Sale Unit, Conversion Factor(s) if applicable, reference to one or more Price Entries.
- Specification (value object / typed structure): category-dependent, optional-by-default fields; includes price-making variables (size, thickness, length, grade, factory/brand) plus non-price-making descriptive/market-slang terms (currently display-name-only).
- Price Entry / "Base Price per Specification Combination" (entity, name not finalized): keyed by a full, specific combination of Specification values (including factory/brand); carries its own Pricing Unit; append-only history; can be marked obsolete (not deleted).
- Pricing Formula (per-product, admin-defined or default): computes effective quantity/multiplier from Specification values; feeds into Price Entry lookup; same engine powers the public weight-calculator tool.
- Tier Discount Policy (independent layer): per-category or global-default with override; per-item; live/unlocked; applied on top of Formula output x Price Entry result.
- VAT layer: final calculation step, 10% (or admin-configured N%), applied after tier discount; base (pre-VAT) price always stored separately.
- Out-of-stock Display Policy (per-product setting): one of {hidden entirely, shown with tag/no action, shown with contact-expert action}.
- Sale Mode (per-product/category setting): one of {fully online, semi-custom with base-size online + custom-via-phone, fully custom/contact-only}.
- Ancillary Cost line item (independent order/invoice line, currently manual entry, potential future default-fixed-value from backend).
- Pre-invoice expiration mechanism: tied to price changes on referenced Price Entries; exact trigger timing not yet resolved (deferred to Step 9).

===========================================================
10. UNRESOLVED / OPEN QUESTIONS (as of this knowledge base)
===========================================================

OPEN-1 (Document A, high priority, per business owner): Exact freight/shipping cost calculation formula/rule (basis: distance, total weight, destination zone?) is not yet decided; requires a dedicated session. Current interim behavior: manual admin entry per order, with a possible backend default fixed number.

OPEN-2 (Document A, high priority, per business owner): Exact tiered-discount boundaries (weight/count thresholds and price change per tier, per product family) not yet finalized; explicitly called out as competitive-advantage material requiring a dedicated session. Interim requirement: must be admin-panel-editable without code changes.

OPEN-3 (Document A, low priority): Complete list of all market-slang terms similar to Fabrik/Persi/Loghme/Nimeh/Sabok-Sangin is not yet compiled; explicitly deprioritized by business owner.

OPEN-4 (low priority, structural): Whether Sale Mode is a strictly closed 3-value enum or may be extended with additional values in the future - not yet asked/answered; does not block current modeling.

OPEN-5 (deferred to Step 9 by design): Exact trigger mechanics for pre-invoice expiration (e.g., immediate on any price change vs. checked only at order finalization/payment time).

OPEN-6 (deferred, minor): Exact mechanics of how Fabrik and Persi are represented as "independent variants" of the generic Product aggregate - i.e., fully separate Product records with no formal link, vs. some lightweight cross-reference. Not yet finalized; does not block progress per explicit note when originally raised in Step 3.

OPEN-7 (deferred to technical design phase, explicitly NOT a domain-modeling question per business owner's own volunteered technical decisions): Stimulsoft-based invoice/pre-invoice rendering pipeline (backend renders to image, converts to base64, frontend renders/downloads as image or PDF); frontend fetching of calculator metadata (name, fields, types, formula) with caching requirement. These are logged as pre-existing technical decisions by the business owner, to be revisited only after domain modeling is complete.

OPEN-8 (not yet reached in the workflow): Any question belonging to Steps 7 through 11 of the approved execution plan (phone-order/manual-order Business Process modeling with Actors and Domain Events; final Ancillary Cost modeling detail; Invoice/Pre-invoice full modeling; Bounded Context map and relationships; final full-model review pass) has not yet been asked or answered as of this knowledge base. These steps are PENDING, not completed.

===========================================================
11. PROCESS STATE / HOW TO CONTINUE
===========================================================

Working method in force for this project (must be preserved by any continuing agent):
- Reality over assumptions; never invent rules; distinguish Confirmed Facts / Derived Conclusions / Unknown / Open Questions at all times; flag contradictions and stop for clarification before proceeding; business modeling strictly before any technical/architecture/code decisions; no praise language; plain text output; phase discipline (documentation collection -> planning -> execution one small step at a time, each step confirmed before moving to the next); explicit regression checks against all previously confirmed facts before finalizing any step, with any change to a prior conclusion explicitly logged (what changed / why / which conclusion is now invalid) rather than silently overwritten.

Approved 11-step execution plan (Phase 2), current progress:
1. Ubiquitous language - DONE (Section 2 of this document, plus refinements added through later steps).
2. Subdomains / core vs supporting vs generic - DONE (Section 8, Decisions 2-3).
3. Product aggregate modeling - DONE (Section 3.6, Section 9).
4. Unit / conversion model - DONE (Section 6).
5. Sale-mode vs inventory (independent attributes + resolution of the out-of-stock contradiction) - DONE (Section 3.5, 3.6).
6. Pricing engine (base price keyed by full spec combination, no price lock, formula-then-price-entry order of operations, tier discount as independent layer) - DONE (Section 5).
7. Phone order process / admin manual order creation, as a formal Business Process with Actors and Domain Events - NOT YET STARTED.
8. Ancillary costs (final detailed modeling) - NOT YET STARTED (baseline facts exist in Section 3/4, formal step pending).
9. Invoice / Pre-invoice modeling (including pre-invoice expiration trigger mechanics, OPEN-5) - NOT YET STARTED.
10. Bounded Contexts and Context Map (including formalizing Catalog+Pricing core relationship, and Pricing-Upstream/Calculator-Downstream relationship) - NOT YET STARTED.
11. Final full-model review pass against all confirmed facts, plus final list of remaining low-priority open items - NOT YET STARTED.

Any agent continuing this project should resume at Step 7, using this document as complete grounding, and should continue applying the same working method (fact/inference/unknown separation, explicit regression checks, no assumption-filling, stop-and-ask on contradiction) exactly as practiced through Steps 1-6.

END OF KNOWLEDGE BASE.

===========================================================
PART 4 BEGINS BELOW (DECISION LOG, FULL, UNABRIDGED)
===========================================================

DECISION LOG
HyperAhan - Catalog and Pricing Modules
Full extraction, covering Phase 1 (documentation) through Phase 3 Step 6.
No entry is summarized; each field is filled with complete supporting detail as it was established in the conversation.

===========================================================
DECISION ID: D-01
===========================================================
Decision: Every product (or product category) must carry a Sale Mode attribute with exactly three defined values: (1) fully online, (2) semi-custom with a default online base configuration plus a phone path for customization, (3) fully custom/contact-only.
Reason: The site's purchasing UI (add-to-cart button, contact/request-quote button, or admin-only/no public listing) must be driven by a single, explicit, per-product classification rather than inferred from other properties, because the three sale behaviors described in the business document are structurally different purchase flows, not variations of the same flow.
Business Evidence: Document A, Section 1, explicitly defines three categories of steel products by how they are sold: standard products with direct online price and cart purchase; semi-custom products (I-beam) with a default standard-size online path plus a phone path for custom variations; fully custom products (wall-post, bolts) built from raw materials plus ancillary costs, priced only by phone. Document A's own Section 11 executive summary states explicitly: "Every product must have a 'sale mode' characteristic: fully online / online display with phone completion / fully custom-phone."
Alternatives Considered: (a) Deriving purchase-flow behavior implicitly from inventory/price-availability state rather than a dedicated field. Rejected because inventory and sale mode were later confirmed (D-13, D-28) to be fully independent concerns; conflating them would have made the fully-custom category indistinguishable from a merely out-of-stock standard product.
Confidence: High - directly stated in the primary business document with no ambiguity.
Current Status: Confirmed, locked at Step 1 (Ubiquitous Language) and re-confirmed at Step 5 (D-28).

===========================================================
DECISION ID: D-02
===========================================================
Decision: Category 3 (fully custom) products DO appear in the public catalog, without a price, with a "contact expert" action button - they are not admin-panel-only/hidden from the public catalog.
Reason: Document A contained a direct internal contradiction: one sentence stated these products "don't necessarily need public display in the catalog and can be registered only via the admin panel," and the very next sentence stated "but we do show them in the public catalog too, without price, with a contact-expert button." Per working method, the later, more specific statement resolves the earlier, more general one, and this reading was carried forward as the operative rule pending confirmation; no contradiction remained to raise back to the business owner because the text itself provided the resolution in sequence.
Business Evidence: Document A, Section 1, Category 3 description: initial sentence saying no public display is necessarily needed, immediately followed by "اما در کاتالوگ عمومی هم آن‌ها را به نمایش می‌گذاریم ولی بدون قیمت و با دکمه تماس با کارشناس."
Alternatives Considered: (a) Treating the first sentence (admin-only, no public listing) as authoritative and the second as a minor exception. Rejected because the second sentence is unqualified and follows the first directly as a correction within the same paragraph, indicating the author's final intended rule.
Confidence: High - both statements are explicit textual evidence; the sequence itself is the resolution mechanism, not an inference beyond the text.
Current Status: Confirmed, locked at Step 1 documentation analysis, never contradicted in later steps.

===========================================================
DECISION ID: D-03
===========================================================
Decision: Admin panel must support building an order entirely from scratch - not merely managing customer-submitted orders - including manual product selection, manual pricing outside standard tiers if needed, adding ancillary costs, and issuing a payment link to the customer.
Reason: The phone-order process (both Path A and Path B) requires an admin-originated order-creation capability that does not depend on a pre-existing customer-submitted cart; this is structurally different from typical e-commerce admin panels that only manage existing orders.
Business Evidence: Document A, Section 2: "نتیجه‌ی طراحی: پنل ادمین باید بتواند یک سفارش را از صفر بسازد (نه فقط سفارش‌های ثبت‌شده توسط مشتری را مدیریت کند) — شامل انتخاب محصول، تعیین قیمت دستی (خارج از پله‌های استاندارد در صورت نیاز)، افزودن هزینه‌های جانبی، و صدور لینک پرداخت برای مشتری."
Alternatives Considered: None presented as genuine alternatives in the source material; this was stated as a direct, singular design implication by the business document itself, not a choice among options.
Confidence: High - directly and explicitly stated as a required design implication in Document A.
Current Status: Confirmed. Note: full Business Process modeling of this capability (Actors, Domain Events) is explicitly deferred to Step 7, which has not yet been executed as of this log. This entry records the requirement itself, not its detailed process model.

===========================================================
DECISION ID: D-04
===========================================================
Decision: Sale Unit (the unit a customer orders in) is not a single global assumption across the system; it must be definable at the product level as weight, count/piece, or both simultaneously.
Reason: Document A explicitly rejected the initial assumption that "everything is always calculated in kilograms," citing that fully counted/no-fixed-size products (I-beam, wall-post, bolts) vary by project need and are coordinated by phone, while I-beam can exceptionally be priced and invoiced directly online as a 12-meter bar by count.
Business Evidence: Document A, Section 4: "بر خلاف فرض اولیه‌ی «همه‌چیز همیشه با کیلوگرم حساب می‌شود»..." and the explicit design conclusion: "واحد فروش نباید یک فرض ثابت سراسری (فقط کیلوگرم) در سیستم باشد. باید در سطح محصول/دسته مشخص شود که آن محصول با «وزن»، «تعداد شاخه»، یا هر دو قابل سفارش است."
Alternatives Considered: (a) Global fixed kilogram-only unit assumption, matching the majority behavior seen later in competitor reference documents (Documents B-F), where the final invoice unit is always converted to kilograms regardless of customer order unit. This competitor pattern was explicitly identified in analysis as being in direct tension with Document A's stated rule; Document A's explicit, business-specific instruction was given priority over the competitor pattern because competitor data is reference-only and does not override an explicit primary-source business rule.
Confidence: High for the core rule (directly stated); the tension with competitor practice was flagged as a point requiring owner awareness but was not separately re-confirmed by the business owner as of this log, since Document A's instruction was treated as already authoritative on its own terms.
Current Status: Confirmed as the operative rule from Document A. Superseded in refined form by D-23/D-25 (Step 4), which generalized this into the broader four-unit model with an explicit simple/complex product distinction - this decision (D-04) is not invalidated, it is the origin point that D-23/D-25 elaborate on.

===========================================================
DECISION ID: D-05
===========================================================
Decision: Freight/ancillary cost is recorded as an independent line item in the order/invoice, never folded into the product price; until a calculation formula is decided, entry is manual by the admin, with the possibility that the backend can return a default fixed number.
Reason: Document A states that part of retail sale profit is embedded specifically in the freight line, implying it must remain visible and separately controllable rather than being absorbed into product price; the exact calculation basis (distance, total weight, destination zone) is explicitly unresolved and requires a dedicated future session.
Business Evidence: Document A, Section 5: "کرایه حمل به‌صورت یک ردیف مستقل در فاکتور ثبت می‌شود... بخشی از سود فروش خرده در همین کرایه حمل نهفته است," followed by the explicitly flagged open ambiguity about the calculation formula, and the interim instruction: "سیستم باید امکان درج دستی مبلغ کرایه توسط ادمین... را داشته باشد, بدون فرمول خودکار... این ماژول میتواند به‌صورت پیش‌فرض در بکند عدد ثابتی برگرداند."
Alternatives Considered: (a) Embedding freight cost inside the product's unit price. Explicitly rejected by Document A's requirement for a separate line item. (b) Immediately building an automatic distance/weight-based formula. Rejected/deferred because the exact formula basis is explicitly unresolved and marked high-priority-but-pending a dedicated session; building it now would violate the "never invent business rules" constraint.
Confidence: High for the "independent line item, manual for now" rule; the note about a possible fixed default number introduces a minor tension (a fixed default is itself a trivial automatic rule) that was flagged in analysis but not separately re-resolved by the business owner as of this log.
Current Status: Confirmed as current interim behavior. Formula finalization remains Open Item OPEN-1 (see Knowledge Base Section 10), pending a dedicated session per the business owner's own statement.

===========================================================
DECISION ID: D-06
===========================================================
Decision: Size, thickness, length, grade, and manufacturing factory must be structured fields (not free text) on the product, because they are the direct basis for pricing and for catalog filtering/search.
Reason: Free-text fields cannot support reliable price computation, filtering, or search consistency across a catalog intended to scale to many diverse product families.
Business Evidence: Document A, Section 6-1: "سایز، ضخامت، طول، گرید و کارخانه‌ی سازنده — این‌ها فیلدهای اصلی هستند که باید به‌صورت ساختاریافته (نه متن آزاد) ثبت شوند، چون مستقیماً پایه‌ی قیمت‌گذاری و امکان فیلتر/جست‌وجو هستند."
Alternatives Considered: None offered in source material as genuine alternatives; stated as a direct, singular requirement.
Confidence: High - directly and unambiguously stated.
Current Status: Confirmed. Later elaborated by D-30 (Step 6), which established that pricing is keyed by the FULL combination of these structured values (plus factory), not by any single one of them in isolation.

===========================================================
DECISION ID: D-07
===========================================================
Decision: Fabrik and Persi must be modeled as two fully independent products/variants, never as a single shared field with two possible values on one product.
Reason: Fabrik and Persi are treated in real invoices and real market usage as entirely distinct products with independent identities, not as a binary attribute of a common underlying product.
Business Evidence: Document A, Section 6-2, table of market terms: "فابریک | یکی از دو نوع مجزای محصول (مثلاً ورق آجدار فابریک)؛ یک محصول کاملاً جدا از نوع «پرسی», نه یک ویژگی روی محصول مشترک" and "پرسی | محصول جدا و مستقل از «فابریک»." Explicit design conclusion: "«فابریک» و «پرسی» باید به‌عنوان دو محصول/واریانت کاملاً جدا در کاتالوگ ثبت شوند، نه یک فیلد مشترک."
Alternatives Considered: (a) A single product with a "type: fabrik/persi" field, which would have been the simpler/more conventional e-commerce modeling choice. Explicitly and directly rejected by the source document's own stated conclusion.
Confidence: High - explicit, unambiguous instruction directly from Document A.
Current Status: Confirmed. Exact mechanics of "independent variant" representation within the single generic Product aggregate (D-15) remain an open, low-priority detail (OPEN-6), not yet finalized, but the independence requirement itself is fully locked.

===========================================================
DECISION ID: D-08
===========================================================
Decision: Market-slang terms other than Fabrik/Persi (light/heavy, half, cut-piece/Loghme) are stored only in the product's display name for now, not as separate structured fields, unless a direct and formulable price effect is later proven for a given term.
Reason: These terms currently function as descriptive/naming conventions observed in real invoices rather than confirmed independent price-determining variables; promoting them to structured fields without evidence of price impact would violate the "never invent business rules" principle.
Business Evidence: Document A, Section 6-2 conclusion: "سایر اصطلاحات بازاری (سبک/سنگین، نیمه) در حال حاضر می‌توانند در نام قابل‌نمایش محصول (Display Name) گنجانده شوند؛ در صورتی که در آینده معلوم شود اثر مستقیم و قابل‌فرمول‌بندی روی قیمت دارند، باید به فیلد ساختاریافته‌ی جداگانه ارتقا یابند." Loghme specifically defined as "محصول برش‌خورده (تکه‌تکه‌شده) از یک پروفیل استاندارد... این یک محصول مشتق از محصول خام است، نه یک ماده‌ی اولیه‌ی جدید."
Alternatives Considered: (a) Proactively structuring all observed market terms as fields now, in anticipation of future price relevance. Rejected because Document A explicitly defers this and confirms the complete list of such terms is not even known yet and is explicitly low priority (OPEN-3).
Confidence: High for the "display-name-only for now" rule; the upgrade trigger condition is clear but its future application is inherently conditional/unresolved.
Current Status: Confirmed as current rule. OPEN-3 (complete term list) remains open and explicitly deprioritized.

===========================================================
DECISION ID: D-09
===========================================================
Decision: Tiered/volume pricing discounts are calculated strictly per-order-line-item (per-item), never as a function of the cart's total across multiple different products; and the applicable tier recalculates live/automatically (unlocked) as the customer changes quantity in the cart.
Reason: The business owner's own stated design rule is explicit and leaves no room for cart-level aggregation; per-item calculation prevents customers from combining small quantities of different products to improperly reach a wholesale threshold that was intended per product line.
Business Evidence: Document A, Section 7-1: "محاسبه‌ی پله بر اساس هر قلم سفارش به‌طور مستقل انجام می‌شود، نه مجموع کل سبد خرید... اگر مشتری هم‌زمان دو محصول متفاوت بخرد که هرکدام کمتر از حد نصاب عمده باشند، تخفیف عمده به هیچ‌کدام تعلق نمی‌گیرد" and "اگر مشتری مقدار سفارش خود را در سبد خرید افزایش دهد... قیمت واحد باید به‌صورت زنده و خودکار به نرخ جدید (عمده) تغییر کند — قیمت پله قفل‌شدنی نیست."
Alternatives Considered: (a) Cart-total-based tiering (sum of all line items determines the discount tier). Explicitly and directly rejected by Document A.
Confidence: High - explicit, unambiguous, with a worked example directly in the source text.
Current Status: Confirmed and re-confirmed as an independent layer at Step 6 (D-22): tier discount rules are defined completely independently of the pricing Formula and are applied on top of the Formula's final output.

===========================================================
DECISION ID: D-10
===========================================================
Decision: All customer-facing prices include 10% VAT; two prices (with/without VAT) are never displayed side by side; the backend always stores the pre-VAT base price separately, applying VAT only at final calculation; a fixed disclaimer text is shown on all relevant pages.
Reason: This matches the business owner's explicit instruction and is corroborated (not derived from) competitor practice, removing any need for further clarification.
Business Evidence: Document A, Section 8, in full: "تمام قیمت‌های نمایش‌داده‌شده به مشتری، شامل ۱۰٪ ارزش‌افزوده هستند و دو قیمت (با/بدون مالیات) در کنار هم نمایش داده نمی‌شود. در پشت سیستم، قیمت پایه (بدون مالیات) ذخیره می‌شود... یک متن ثابت راهنما باید در همه‌ی صفحات مرتبط نمایش داده شود."
Alternatives Considered: None - this was directly and unambiguously specified with no competing option presented.
Confidence: High.
Current Status: Confirmed, unchanged throughout the entire conversation; corroborated by competitor reference documents (Documents C, D, F) with no conflict identified.

===========================================================
DECISION ID: D-11
===========================================================
Decision: Every price change creates a new, append-only price history record; prior records are never deleted or edited; the system always uses the latest record for display/calculation; a price record can independently be marked "obsolete" so it is excluded from reports, without being deleted.
Reason: The business owner requires a durable, auditable price history while also needing a mechanism to exclude certain historical records from reporting without destroying the audit trail - these are explicitly two separate states (deleted vs. obsolete), not one.
Business Evidence: Document A, Section 9, in full: "هر تغییر قیمت به‌عنوان یک رکورد جدید ثبت می‌شود؛ رکوردهای قبلی هرگز حذف یا ویرایش نمی‌شوند. سیستم همیشه از آخرین رکورد برای نمایش و محاسبه استفاده می‌کند، اما مدیران باید بتوانند گزارش تاریخی قیمت هر محصول را مشاهده کنند. اما رکورد های قیمت میتوانند منسوخ شوند تا در صورت گرفتن گزارش در آن گنجانده نشوند."
Alternatives Considered: (a) Simple mutable price field with no history. Rejected - explicitly contradicted by "prior records are never deleted or edited." (b) Treating "obsolete" as equivalent to soft-deletion with no distinct meaning. Rejected in analysis because the source text explicitly frames obsolescence as a reporting-exclusion mechanism distinct from deletion; this distinction was flagged as important for modeling and treated as a hard requirement, not merged into a single status field without justification.
Confidence: High.
Current Status: Confirmed, unchanged. Directly reinforced by D-29 (Step 6): the "no price lock, always live" rule works together with this append-only history model - the live-read price is always whichever record is currently latest and non-obsolete.

===========================================================
DECISION ID: D-12
===========================================================
Decision: The originally read tension in Document A regarding out-of-stock catalog display ("should not appear at all" vs. "can be shown with a contact-expert action at admin's discretion") is resolved as a per-product, admin-configurable setting with three possible states: (1) fully hidden from catalog, (2) shown with an out-of-stock tag and no action button at all, (3) shown with a contact-expert action button.
Reason: Rather than being a system-wide fixed policy that must pick one behavior for all out-of-stock products, the business owner clarified that this is a decision made individually per product by the admin, and that a third state (no action at all) also genuinely exists for some products, which had not been described anywhere in the original document.
Business Evidence: Document A, Section 3, both original conflicting sentences, plus Document A's own Section 10 summary table explicitly flagging item 3 ("تناقض «تماس بگیرید برای ناموجودی» در برابر «عدم نمایش کامل محصول ناموجود»") as "نیازمند تأیید نهایی." Direct business owner clarification: "در اصل توسط ادمین مشخص می‌شوند که محصول نمایش داده بشه یا نشه" plus the stated rationale for the contact-button state (enabling the admin to suggest a substitute product or source an alternate supplier quote) and separate confirmation that "بعضی از محصولات هم ناموجود بدون هیچ اکشنی هستن," further clarified as "نمایش محصول با تگ ناموجود بدون هیچ اکشنی."
Alternatives Considered: (a) A single global rule choosing either always-hidden or always-shown-with-contact for all out-of-stock products (the two options as literally stated in the original document, treated as mutually exclusive). Rejected once the business owner clarified the choice is per-product, not global. (b) A two-state model (hidden vs. contact-button only), which was the analysis team's working hypothesis until the business owner introduced the third "tag with no action" state, requiring an explicit follow-up question before finalizing.
Confidence: High - reached only after two rounds of direct clarifying questions and explicit business owner answers; no part of this decision rests on inference.
Current Status: Confirmed and locked at Step 5. Directly reinforced by D-13.

===========================================================
DECISION ID: D-13
===========================================================
Decision: Sale Mode (D-01) and the out-of-stock display setting (D-12) are two fully independent attributes on the Product; they must not be collapsed into a single field, and the final catalog display behavior for any given product is the combined result of both independent fields.
Reason: A "contact expert" action button can arise from two entirely different causes - a fully-custom Sale Mode (D-01, category 3) or the independent out-of-stock display setting (D-12, state 3) - and conflating these into one field would make it impossible to distinguish "this product is always contact-only by nature" from "this specific unit run happens to be temporarily out of stock and the admin wants a contact button for it."
Business Evidence: Synthesized directly from Document A Section 1 (sale mode categories) and Section 3 (inventory display), which describe two structurally separate business concerns using similar surface-level UI language ("contact/تماس بگیرید"), combined with the business owner's explicit, separate answers to Sale Mode questions (Step 1 analysis) versus Inventory questions (Step 5, D-12) - at no point did the business owner indicate these two concerns should be merged.
Alternatives Considered: (a) A single combined "display state" enum covering both sale-mode-driven and inventory-driven contact scenarios in one field. This was the analysis team's own internally raised concern in the initial document review ("این دو بعد به یک اکشن مشابه ختم می‌شوند... همین باعث شده تناقض ایجاد شود") and was explicitly flagged as a question requiring confirmation rather than assumed; the business owner's subsequent independent, separate answers to Sale Mode and Inventory questions (never merging them) served as the de facto confirmation that these remain two fields.
Confidence: High - modeling conclusion drawn from consistently separate treatment of the two concerns across the entire conversation, with no business owner statement ever merging them.
Current Status: Confirmed, locked at Step 5, and directly incorporated into the Product aggregate design (D-15).

===========================================================
DECISION ID: D-14
===========================================================
Decision: The product domain scope for Foolad Shahyar Sepand is confirmed to match the breadth of the competitor reference catalogs (Documents B-F: rebar, sheet in all its sub-types, profile/tube, I-beam, angle iron, channel, pipe, wire products, billet/slab/bloom, etc.), and additionally extends in the future beyond steel into construction materials, nails, screws, and similar categories.
Reason: This is the single point in the entire conversation where the business owner explicitly authorized promoting competitor-derived reference data (otherwise treated strictly as non-authoritative background knowledge) into a confirmed fact about Foolad Shahyar Sepand's own business, specifically in response to a direct question about product domain scope.
Business Evidence: Direct business owner statements: "دامنه محصولات مانند رقبا هست... ما باید قابلیت ثبت محصولات متنوعی داشته باشیم" and, earlier in the same exchange, "چون تمرکز روی محصولات آهنی هست. و بعدا قرار هست که محصولاتی مثل مصالح ساختمانی هم در این قرار بگیرند. یا میخ یا پیچ و..." and the twice-repeated statement "انتظار دارم بتوان در سیستم محصولات متنوعی رو قرار داد و قیمت‌گذاری کرد" / "ما باید قابلیت ثبت محصولات متنوعی داشته باشیم."
Alternatives Considered: (a) Treating Document A's explicitly named product examples only (rebar, sheet, angle iron, channel, profile, I-beam, wall-post, bolts) as the full and final product scope, with no broader inference permitted. This was the analysis team's default, conservative posture prior to this exchange, consistent with the rule against inferring business scope from competitor practice; it was superseded only by the business owner's direct, explicit statement.
Confidence: High - explicit business owner confirmation, not an inference.
Current Status: Confirmed. This is the direct evidentiary basis for D-15 (generic Product aggregate design).

===========================================================
DECISION ID: D-15
===========================================================
Decision: Product is modeled as a single, generic aggregate type applicable across all product families, rather than a separate aggregate type per family (e.g., not a distinct type for Rebar, another for Sheet, another for Profile, etc.). Each Product carries a category-dependent Specification (see D-26) rather than family-specific hard-coded fields.
Reason: Given the confirmed scope breadth (D-14) - including future expansion into entirely unrelated categories such as construction materials, nails, and screws - a per-family aggregate type approach would require a code change and data migration every time a new product family is introduced, directly conflicting with the business owner's explicit, repeated requirement for broad, flexible product registration capability.
Business Evidence: Same as D-14, plus the qualitative evidence gathered during the alternatives analysis: the sheer diversity of attribute sets already observed across just the steel domain (rebar's size/grade/factory vs. sheet's type/thickness/width/length/supply-form vs. pipe and angle iron's dual-unit size-in-inches-with-mm-thickness pattern) indicating that a hard-coded per-family schema would already be unwieldy even before considering non-steel expansion.
Alternatives Considered: (a) One aggregate/class per product family (e.g., RebarProduct, SheetProduct, ProfileProduct as distinct types), offering compile-time type safety and clearer per-family domain expression. Rejected as the final decision because of its direct conflict with the confirmed diversity and extensibility requirement (D-14); this was explicitly weighed and reasoned through as a genuine alternative with real merits (type safety, clarity) before being rejected, not dismissed without consideration. (b) A single generic aggregate with a structured, category-dependent Specification value object. Selected.
Confidence: High - directly requested confirmation was obtained from the business owner before this was locked, elevating what began as a medium-confidence inference (initially proposed as "gezine-ye alef" during Step 3 discussion) to a fully confirmed decision.
Current Status: Confirmed and locked at Step 3, re-confirmed via D-14 in the same exchange. Unchanged in all subsequent steps.

===========================================================
DECISION ID: D-16
===========================================================
Decision: Both the Catalog/Product-Variant subdomain and the Pricing Engine subdomain are classified as Core Domains of the business.
Reason: Pricing was independently identified as core based on the business owner's own explicit language calling the tiered-discount logic "part of our creativity and competitive advantage" - the only place in the entire conversation where the business owner used competitive-advantage language about any subdomain. Catalog was initially classified only as a medium-confidence logical inference (based on the unusual complexity and specificity of catalog decisions such as Fabrik/Persi separation and the multi-layer unit model), explicitly flagged as requiring direct confirmation rather than being asserted as fact. The business owner then directly and unambiguously confirmed both are core when asked.
Business Evidence: For Pricing: Document A, Section 7-1, open-ambiguity note: "قواعد دقیق پله‌های تخفیف... به گفته‌ی صاحب کسب‌وکار «جزو خلاقیت و مزیت رقابتی ماست»." For Catalog: no equivalent direct statement existed prior to the direct question; the business owner's direct answer was: "بله کاتالوگ و قیمت گذاری هر دو هسته های کسب و کار هستن."
Alternatives Considered: (a) Classifying Pricing alone as Core and Catalog as Supporting (feeding into Pricing). This was the analysis team's own working hypothesis prior to asking the direct confirming question, explicitly presented as a medium-confidence conclusion "نیازمند تأیید صریح" rather than asserted as settled. Rejected once the business owner's direct answer confirmed both are core.
Confidence: High for both, after the direct confirming question and answer. (Prior to that exchange, Catalog's core status specifically was Medium confidence, explicitly logged as such and not treated as fact until confirmed.)
Current Status: Confirmed and locked at Step 2. Direct implication (not yet formally executed): the Bounded Context relationship between these two core subdomains should be a close/Partnership-style relationship rather than a typical Core-to-Supporting Customer/Supplier distance; this implication is explicitly flagged for formal resolution at Step 10 (Context Map), which has not yet occurred as of this log.

===========================================================
DECISION ID: D-17
===========================================================
Decision: Order Management, Ancillary Cost Management, and Invoice/Pre-invoice Generation are classified as Supporting Domains (not Core, not Generic).
Reason: These are necessary operational processes for the business to function, but none of them has been described by the business owner using competitive-advantage or differentiation language anywhere in the conversation; their complexity (e.g., the dual-path phone order process) stems from a need for operational flexibility rather than from an intentional competitive differentiator.
Business Evidence: Absence of evidence is itself the evidence here - across all documents and all business owner statements, no claim of competitive advantage, uniqueness, or strategic differentiation was ever made about order handling, freight-cost handling, or invoice generation, in contrast to the explicit "creativity and competitive advantage" language used specifically about pricing tiers (see D-16).
Alternatives Considered: (a) Classifying Order Management as Core, given the non-trivial dual-path (Path A/B) complexity described in Document A Section 2. Considered and rejected because complexity alone, without an explicit differentiation claim, does not meet the bar used elsewhere in this analysis (contrast with Pricing, D-16, where an explicit differentiation claim exists) for Core classification.
Confidence: Medium-to-High - this classification is an inference from absence of competitive-advantage language and the general nature of the processes involved; it has not been separately, directly confirmed word-for-word by the business owner the way Pricing/Catalog core status was (D-16). This gap is explicitly logged rather than silently treated as equally certain.
Current Status: Confirmed as working classification, not yet independently re-verified with the business owner via a direct question. No contradicting evidence has emerged in any later step.

===========================================================
DECISION ID: D-18
===========================================================
Decision: The public-facing Weight Calculator tool is classified as a Generic Domain, but is explicitly a downstream consumer of the Core Pricing Engine's Formula capability, not a fully isolated, unrelated generic subsystem.
Reason: The underlying weight-calculation formulas themselves (sheet weight = length x width x thickness x density; profile weight = 2 x thickness x (H+W-2t) x density) are standard, industry-wide formulas with no business-specific logic or competitive value, which is the textbook definition of a generic subdomain. However, the business owner later revealed that the same admin-defined Formula engine used for product pricing is reused to power this exact calculator tool, meaning the tool's implementation is architecturally dependent on, and downstream of, the Core Pricing subdomain, even though its formula content is generic.
Business Evidence: Initial generic classification: formulas identically sourced from AhanOnline/MarkazAhan/EsfahanAhan reference documents (Documents C, D, E, F, G) with no business-specific variation. Reclassification of the relationship (not the generic label itself) came from the business owner's statement: "از این فرمول‌ها در قسمت جعبه ابزار هم میتوانیم استفاده کنیم. یعنی فرانت مشخصات فرم را از بکند میگیرد و در صورت فعال بودن نمایش فرمول در جعبه ابزار محاسبه‌گر نمایش داده شود." Business rationale for the tool's importance, independently stated: "ماشین حساب وزنی باید مثل یک ابزار ساده و قابل استفاده مکرر برای مراجعین سایت در نظر گرفته شود. چون ترافیک مناسبی را از کاربران میگیرد." - i.e., its value is traffic generation, not competitive pricing logic, consistent with a Generic classification for the tool's business purpose even while its technical implementation is Pricing-Engine-dependent.
Alternatives Considered: (a) Treating the calculator as entirely independent of the pricing engine, a pure standalone generic utility with its own separate formula implementation. Rejected once the business owner confirmed formula-engine reuse across both contexts. (b) Reclassifying the calculator itself as part of the Core Pricing domain because it shares the same engine. Rejected because the business owner's own stated justification for the tool (traffic generation) and the generic, non-differentiated nature of the formulas themselves are both still valid and support keeping it Generic at the domain-purpose level; only the technical relationship (Upstream/Downstream) needed adjustment, not the domain classification.
Confidence: High for the generic classification itself (unchanged since Step 2); High for the Upstream(Pricing)/Downstream(Calculator) relationship once the formula-engine-reuse fact was confirmed.
Current Status: Confirmed. Formal Context Map entry for this Upstream/Downstream relationship is explicitly deferred to Step 10, not yet executed as of this log.

===========================================================
DECISION ID: D-19
===========================================================
Decision: Active design and modeling work is scoped exclusively to the Catalog and Pricing modules for the remainder of this engagement; Order, Discount, Authentication, Notification, and Blog modules are explicitly deferred to separate, later decisions. The overall system is a Modular Monolith built with Clean Architecture.
Reason: The business owner directly narrowed the working scope after the subdomain identification step, distinguishing the full eventual system (which includes all these modules) from the immediate design focus.
Business Evidence: Direct business owner statement: "این محصول به صورت کلی شامل سیستم سفارش، تخفیف، احراز هویت، سیستم اعلانات، قیمت گذاری و کاتالوگ، بلاگ و غیره میباشد. الان صرفا تمرکز روی طراحی ماژول های مرتبط با کاتالوگ، قیمت گذاری مباشد. و ماژول های دیگه بعدا تصمیم گیری میشوند. ماژول های نامبرده در یک سیستم ماژولار مونولیتیک هستن که به صورت کلین طراحی و پیاده شدن."
Alternatives Considered: None - this was a direct scoping instruction from the business owner, not a modeling choice with weighed alternatives.
Confidence: High - explicit, direct instruction.
Current Status: Confirmed and currently in effect; governs all decisions from this point forward in the conversation (D-20 onward all operate within this narrowed scope).

===========================================================
DECISION ID: D-20
===========================================================
Decision: Beyond the default pricing formula (base price x quantity + tax), the system supports an admin-defined, per-product custom Formula, computed from that product's Specification values, and this same Formula engine is reused to power the public weight-calculator toolbox described in D-18.
Reason: The business owner explicitly requested that ability for the admin to define specifications and, based on them, define a formula that determines pricing and display, framed as an ideal capability ("فکر میکنم... عالی هست"), and separately confirmed that the same formulas already power the public calculator tool.
Business Evidence: "فکر میکنم اگر بتوان محصول رو ثبت کرد و specification ها رو توسط ادمین مشخص کرد و بر اساس اون ادمین بتونه با مشخص کردن یک فرمول بین این specification ها قیمت گذاری و نمایش رو داشته باشه عالی هست" combined with the calculator-engine-reuse statement quoted in D-18's evidence.
Alternatives Considered: (a) A fixed, closed set of formula templates chosen from a predefined list rather than a fully admin-definable formula. Not explicitly proposed by the business owner and not selected; the business owner's language ("مشخص کردن یک فرمول") implies definitional freedom rather than template selection, though the exact technical mechanism (formula language, expression syntax) is explicitly out of scope for domain modeling and deferred to the technical design phase (OPEN-7).
Confidence: High for the existence and dual-use nature of the formula capability; the exact technical implementation is intentionally unaddressed at this stage per the "business before technology" working method.
Current Status: Confirmed. Directly refined and constrained by D-21 in the same exchange sequence.

===========================================================
DECISION ID: D-21
===========================================================
Decision: The admin-defined pricing Formula (D-20) never computes the final price directly; it always computes only the effective consumed quantity or multiplier (e.g., a calculated weight from dimensions) from the product's Specification. That computed value is then multiplied against the Base Price Entry (D-30) that matches the specific Specification combination to yield the pre-tax, pre-discount price. No exceptions to this rule have been identified.
Reason: This was directly and explicitly asked as a clarifying question before locking Step 6, specifically because leaving it ambiguous would have created two structurally different, incompatible models for how Formula and base price interact; the business owner's rebar and nail pricing examples (see D-30 evidence) already implied that a fixed base price per exact combination exists independently of any formula, which only makes sense if the formula's output feeds into that price rather than replacing it.
Business Evidence: Direct business owner confirmation, in response to the explicit clarifying question: "نه مورد استثنایی تا الان شناسایی نکردم. پس لازم نیست. قیمت پایه برای همان ترکیب در فرمول قرار میگیره."
Alternatives Considered: (a) Allowing the Formula to sometimes bypass the Base Price Entry entirely and directly output a final price for certain products. This was explicitly posed as the clarifying question's second option and explicitly rejected by direct business owner confirmation that no such exception exists.
Confidence: High - explicit confirmation with an explicit denial of exceptions, obtained specifically to avoid modeling an incorrect or overly permissive relationship.
Current Status: Confirmed and locked at Step 6, without qualification or known exception as of this log.

===========================================================
DECISION ID: D-22
===========================================================
Decision: Tiered/volume discount rules (D-09) are defined completely independently of the pricing Formula (D-20/D-21); when an admin defines a discount and applies it to a product or a product group, it is applied on top of the final price - i.e., on the output of Base Price Entry x Formula-computed quantity - not blended into the Formula itself.
Reason: This distinction was explicitly required to resolve a potential structural ambiguity: without it, it would be unclear whether tier boundaries were themselves parameters inside a per-product formula (and thus could vary arbitrarily per product formula) or a separate, uniform layer that always applies after pricing regardless of which formula (default or custom) produced the pre-discount price.
Business Evidence: Direct business owner statement, in response to an explicit clarifying question on this exact point: "مرز های تخفیف مستقل از فرمول اصلی تعریف شده میباشند. و در صورت تعریف تخفیف توسط ادمین و اعمال اون روی یک محصول یا یک گروه محصول روی قیمت نهایی اعمال میشوند."
Alternatives Considered: (a) Tier discount logic embedded as a configurable component within each product's own Formula definition, allowing different discount mechanics per product formula. Implicitly rejected by the business owner's answer, which frames discounts as external to, and always operating on the output of, the main formula, applicable at the product OR group level uniformly.
Confidence: High - explicit, direct confirmation obtained via a targeted clarifying question.
Current Status: Confirmed and locked at Step 6. Consistent with, and elaborates on, the per-item/live tiering rule already confirmed in D-09.

===========================================================
DECISION ID: D-23
===========================================================
Decision: Four unit concepts are modeled as independent from one another: Registration Unit (the unit a product's Specification is defined in), Sale Unit (the unit the customer orders in - weight, count, or both, per D-04), Pricing Unit (the unit the base price is defined in), and Display Unit (the unit shown to the customer, separately configurable for the catalog view and for the invoice view).
Reason: The business owner explicitly stated that the system has a variety of standard measurement units and that the admin chooses which unit a product is registered in and which unit pricing is based on - implying these are not automatically the same unit and must be independently selectable, which in turn implies a fourth, display-facing concept is also needed since what is stored/priced does not have to equal what is shown.
Business Evidence: "سیستم دارای واحد های اندازه گیری استاندارد متنوعی میباشد و ادمین انتخاب میکند روی چه واحدی محصول را ثبت کند. یا قیمت سبد شده بر اساس چه واحدی باشد," combined with the earlier-confirmed independent, admin-controlled Display Unit rule for catalog and invoice (see D-27) and the earlier Document A rule on Sale Unit flexibility (D-04).
Alternatives Considered: (a) A simpler two-concept model (one "storage unit" and one "customer-facing unit"), collapsing Registration/Pricing into one and Sale/Display into another. Considered implicitly during analysis but not selected, because the business owner's later nail/rebar pricing examples (D-30) confirmed that Pricing Unit specifically can vary independently even within otherwise-identical products (e.g., per-unit vs. per-package pricing for the same nail size across different brands), which requires Pricing Unit to be its own independent concept rather than merged with Registration Unit.
Confidence: High - directly stated by the business owner, and further corroborated by the later Step 6 pricing examples.
Current Status: Confirmed and locked at Step 4. Refined at Step 6 by D-31, which established that Pricing Unit specifically is a property of each individual Price Entry rather than a single fixed setting for the whole product.

===========================================================
DECISION ID: D-24
===========================================================
Decision: The Conversion Factor relating different units on the same product (e.g., weight of one rebar bar in kilograms) is always a fixed, stored numeric value on the product - never the output of the pricing Formula (D-20/D-21) or any other computed/dynamic mechanism.
Reason: This was explicitly asked as a clarifying question before finalizing Step 4, because the business owner's earlier description of needing to divide total order weight by unit weight to compute piece count (a conversion-factor use case) could, in principle, have been satisfied either by a static stored number or by reusing the same dynamic Formula mechanism already introduced for pricing (e.g., a profile's weight-per-meter, which is itself derivable from dimensions via a formula in competitor reference material) - these are two structurally very different models and needed to be distinguished rather than assumed.
Business Evidence: Direct business owner answer to the explicit clarifying question: "سوال یک ضریب یک عدد ثابت هست."
Alternatives Considered: (a) Conversion Factor computed dynamically via the same Formula engine used for pricing (e.g., recomputing profile weight-per-meter from H, W, and thickness via formula rather than storing a static number). Explicitly posed as the clarifying question's second option and explicitly rejected by direct business owner confirmation that it is always a fixed number.
Confidence: High - explicit confirmation obtained via a targeted clarifying question specifically designed to prevent an incorrect default assumption.
Current Status: Confirmed and locked at Step 4, unchanged since.

===========================================================
DECISION ID: D-25
===========================================================
Decision: Not all products require the full four-unit distinction (D-23); some products can be modeled simply, using a single unit throughout (registration = sale = display = pricing unit are all the same), with no conversion factor needed at all. Explicit examples given: box, count/piece, carton, trailer-load, container.
Reason: This directly resolves the open question of whether the four-concept unit model (D-23) should be mandatory for every product; forcing every simple product through the full model would add unnecessary complexity inconsistent with the business owner's stated goal of supporting a wide, easy-to-register variety of products (D-14).
Business Evidence: Direct business owner answer to the explicit clarifying question posed at Step 4: "بعضی محصولات میتوانند ساده کار کنند و یک واحد داشته باشند. مثلا جعبه یا تعداد یا کارتن یا تریلی یا کانتینر و..."
Alternatives Considered: (a) Mandating the full four-unit model uniformly for every product regardless of complexity, for consistency of implementation. Explicitly rejected by direct business owner confirmation that simpler, single-unit products exist and are expected to be supported as a genuinely simpler case, not merely a degenerate case of the full model.
Confidence: High - explicit, direct confirmation with concrete named examples.
Current Status: Confirmed and locked at Step 4. Directly shapes D-26 (Specification must be optional/lightweight-by-default to avoid imposing this complexity on simple products).

===========================================================
DECISION ID: D-26
===========================================================
Decision: Specification is modeled as a typed structure (not raw, untyped key-value pairs), but with fields that are optional/lightweight by default, so that a simple product (D-25) is not forced into unnecessary structural complexity while a complex product (rebar, sheet, profile) can still make full use of the structured, typed model including multi-unit and conversion-factor support.
Reason: This directly reconciles two requirements that would otherwise be in tension: Document A's explicit requirement that price-making fields (size, thickness, grade, factory) be structured and typed for reliable filtering and pricing (D-06), versus the business owner's explicit requirement that simple products (D-25) must not be burdened with unnecessary structural complexity.
Business Evidence: Document A, Section 6-1 (structured field requirement, D-06 evidence) combined with the business owner's Step 4 answer establishing simple, single-unit products (D-25 evidence, "بعضی محصولات میتوانند ساده کار کنند"). No single business owner statement covers this exact synthesis in one sentence; it is the analysis team's explicit reconciliation of two separately confirmed facts, presented as the resolution to what would otherwise have been flagged as an open tension between D-06 and D-25.
Alternatives Considered: (a) Fully untyped, free-form key-value Specification for maximum flexibility across arbitrary future product types. Rejected because it directly conflicts with Document A's explicit structured-field requirement (D-06). (b) A single mandatory, uniformly-required typed schema applied identically to every product regardless of complexity. Rejected because it directly conflicts with the business owner's explicit simple-product requirement (D-25).
Confidence: High for both halves of the reconciliation individually (each is directly confirmed); the specific synthesis language ("typed but optional-by-default") is the analysis team's own framing of how to satisfy both confirmed constraints simultaneously and has not been independently re-confirmed word-for-word by the business owner as a single, separate statement.
Current Status: Confirmed and locked at Step 4 as the resolution mechanism; no contradicting evidence in later steps.

===========================================================
DECISION ID: D-27
===========================================================
Decision: On an invoice, a maximum of two units may be displayed simultaneously, and in most cases only one unit is shown; the admin controls which unit(s) display on the invoice, and this setting is independent from the catalog display-unit setting.
Reason: This directly resolves an apparent internal tension between Document A's statement that invoices should be able to show both weight-based and count-based line items together "exactly like real sample invoices" (implying dual-unit display is the norm) and the business owner's later statement that display unit choice (kilo vs. count) is simply "at the admin's discretion" (implying single-unit display might be the norm). The business owner's direct clarification established that dual-unit display is a bounded option (max two), not the default expectation, and that catalog and invoice unit choices are configured separately.
Business Evidence: Document A, Section 4, design conclusion: "فاکتور باید بتواند هر دو نوع واحد را همزمان در کنار هم نمایش دهد (دقیقاً مانند فاکتورهای واقعی نمونه که هم ردیف‌های کیلویی و هم ردیف‌های تعدادی داشتند)." Business owner's subsequent, direct clarification once this tension was explicitly raised as a question: "ادمین باید بتواند واحد های نمایش را مشخص کند همچنین در فاکتور هم باید بتواند مشخص کند بر اساس چه واحد هایی نمایش دهد. اما در فاکتور فقط دو واحد بیشتر مجاز نیست و در اکثر مواقع یک واحد نمایش داده میشود."
Alternatives Considered: (a) Treating Document A's "both units simultaneously" language as the universal default behavior for every invoice line, with no admin override. Rejected once the business owner clarified that dual-unit display is a bounded, admin-selectable option and single-unit display is actually the common case. (b) Treating the business owner's "admin discretion" language as fully overriding Document A's dual-unit requirement, i.e., assuming only one unit is ever supported. Rejected because the business owner explicitly preserved a genuine, bounded (max two) dual-unit display capability rather than eliminating it.
Confidence: High - reached through an explicit clarifying question specifically because the two source statements, read independently, could have been misread as contradictory; the business owner's direct answer fully reconciles them without leaving residual ambiguity.
Current Status: Confirmed and locked at Step 4.

===========================================================
DECISION ID: D-28
===========================================================
Decision: Sale Mode (D-01) and the out-of-stock display setting (D-12) are formally confirmed, at the Product-aggregate-design level (Step 3/5), to be two separate, independent top-level fields on the generic Product aggregate (D-15), not part of the category-dependent Specification (D-26) and not merged into each other.
Reason: This is the formal aggregate-design-level restatement and lock-in of the conceptual independence already established in D-13, made explicit specifically at the point of finalizing the Product aggregate's field-level shape in Step 5, to ensure this independence is carried into the structural model rather than remaining only a conceptual note.
Business Evidence: Same evidentiary basis as D-13 (Document A Sections 1 and 3, plus the business owner's separately-given, never-merged answers regarding Sale Mode versus Inventory Display), applied specifically to the aggregate-design conclusion at the close of Step 5.
Alternatives Considered: Same alternatives as considered in D-13; no new alternatives were introduced at this stage, as this decision is the structural formalization of D-13's conceptual conclusion rather than a new analytical question.
Confidence: High - direct continuation of the already-confirmed D-13 reasoning, applied at the structural/aggregate level.
Current Status: Confirmed and locked at the close of Step 5, incorporated into the Product aggregate's field list going forward.

===========================================================
DECISION ID: D-29
===========================================================
Decision: There is no price-locking mechanism anywhere in the system, from order creation through payment; price is always read live from the backend at every stage of the order process. A price change occurring mid-order is treated as an accepted, expected business event rather than a defect. If a pre-invoice was already issued and the referenced price subsequently changes, that pre-invoice becomes expired, per terms the customer has already agreed to when placing the order.
Reason: This decision directly and explicitly REPLACES a previously confirmed conclusion (see the Regression/Correction record below) after the business owner clarified that the earlier reading of Document A's "price lock" language was an over-complication introduced during analysis, not an accurate reflection of actual intended behavior.
Business Evidence: Direct business owner statement, given specifically in response to a clarifying question that had offered two possible interpretations of Document A's price-lock language: "منظور همون ثبت سفارش تا پرداخت هست. که فکر میکنم منظور رو پیچیده کرده. قیمت در نهایت در تمام مراحل از بکند گرفته میشود. و قفل قیمت نداریم. این رو در نظر نگیر. اگر قیمت یک محصول در طول سفارش تغییر کند ناگزیر هست. و کاربر درصورتی که مبلغ‌ها تغییر کردن پیش‌فاکتوری گرفته بود پیش‌فاکتور منقضی می‌شود. که در توافق‌نامه‌ای که کاربر اون رو تایید کرده هم آمده."
Alternatives Considered: At the time the clarifying question was posed, two interpretations were offered: (a) price is locked for the duration of a specific order/cart session regardless of later catalog price changes, or (b) price is only updated at fixed admin-controlled intervals and held constant between updates. Both were explicitly rejected by the business owner's direct statement that no lock exists at all and price is always read live.
Confidence: High - explicit, direct correction from the business owner, obtained specifically because the earlier reading (based on Document A's own ambiguous "لاک" language) had already been logged as a confirmed fact and needed either reaffirmation or explicit correction rather than silent continuation.
Current Status: SUPERSEDES a prior confirmed conclusion. REGRESSION RECORD: WHAT CHANGED - the previously confirmed statement "unlike tier discounts which are never locked, the base market price is locked to prevent momentary fluctuation" (originally drawn directly from Document A Section 7-1 and logged as a confirmed fact during the Step 1 document analysis and reaffirmed in the Step 5-to-6 consistency check) is now INVALID. WHY IT CHANGED - the business owner explicitly stated this reading over-complicated the actual intended meaning, which only concerned the order-to-payment span, and explicitly instructed "قفل قیمت نداریم... این رو در نظر نگیر." WHICH PREVIOUS CONCLUSION IS NOW INVALID - the "price pool is locked against momentary fluctuation" conclusion from Document A analysis and its restatement in the Step 5 regression/consistency check are both explicitly invalidated and replaced by this decision. This correction was announced explicitly at the time it occurred, per the working method's requirement never to silently overwrite prior knowledge.

===========================================================
DECISION ID: D-30
===========================================================
Decision: A product's base price is keyed by the FULL, specific combination of that product's Specification values - of which manufacturing factory/brand is only one dimension, not the sole determinant. Different combinations of size, thickness, factory, etc., for what might otherwise be considered "the same product," each have their own fully independent base price record. Furthermore, the pricing unit itself (per-kg, per-unit, per-package) is part of each such price record, not a single fixed setting for the whole product.
Reason: This directly and explicitly REPLACES a previously confirmed, narrower conclusion (see Regression Record below) after the business owner provided concrete pricing examples that could not be explained by a factory-only keying model.
Business Evidence: Direct business owner statement and worked examples: "میلگرد میتواند برند و کارخانه متفاوت داشته باشد. میتواند سایز متفاوت داشته باشد. مثلا ضخامت متفاوت داشته باشد. به ازای هرکدام از این ها قیمت واحد ثبت میشه. مثلا قیمت میلگرد ذوبی ۱۴: ۷۳ هزار تومن به ازای هر کیلو گرم. قیمت میلگرد ذوبی ۱۲: ۷۴ هزار تومن به ازای هر کیلو گرم. قیمت میلگرد ابرکوه ۱۴: ۷۳،۵۰۰ به ازای هر کیلو گرم. یا بعدا میخ سایز ۳ برند اکس هر عدد ۲۰ تومن. میخ سایز ۳ برند الکس هر بسته ۲ میلیون تومن. میخ سایز ۳ اشترکوه هر عدد ۲۵ هزار تومن."
Alternatives Considered: (a) The previously held model - base price varies by factory only, with a single implied pricing unit for the whole product, and other Specification values (size, thickness) treated as separate multipliers or fixed attributes rather than co-determinants of the price-record key. This was the working assumption established at the close of Step 3 (under the provisional name "ProductFactoryPrice") and was directly disproven by the rebar example (same factory - Zob Ahan - producing two different prices for two different sizes) and further disproven by the nail example (same size and comparable market segment, but different pricing units - per-unit vs. per-package - across different brands).
Confidence: High - grounded in multiple concrete, worked numeric examples directly provided by the business owner, leaving no reasonable alternative reading.
Current Status: SUPERSEDES a prior confirmed conclusion. REGRESSION RECORD: WHAT CHANGED - the Step 3 conclusion that "قیمت پایه به‌ازای هر کارخانه یک رکورد مستقل است" (base price is a per-factory-independent record), provisionally named ProductFactoryPrice, is now INVALID as stated because it is too narrow. WHY IT CHANGED - the business owner's rebar and nail examples demonstrated that price varies independently by factory AND by size (and, for nails, by pricing unit type) simultaneously within what could be considered the same base product, which a factory-only key cannot represent. WHICH PREVIOUS CONCLUSION IS NOW INVALID - the Step 3 "per-factory price record" framing (and its provisional name ProductFactoryPrice) is invalidated and replaced by the "full Specification-combination-keyed Price Entry" model described in this decision. This correction was explicitly announced as a regression event at the time it occurred, per the working method's requirement never to silently overwrite prior knowledge.

===========================================================
DECISION ID: D-31
===========================================================
Decision: The Pricing Unit (one of the four independent unit concepts, D-23) is confirmed to be a property attached to each individual Price Entry (D-30), not a single, fixed setting applied uniformly across an entire product.
Reason: The nail pricing example shows the same product/size sold under different brands with entirely different pricing units - per-unit for Brand X and Ashtarkooh, per-package for Brand Alex - which is only representable if Pricing Unit varies at the same granularity as the price record itself (i.e., per Specification combination), rather than being fixed once for the whole product.
Business Evidence: Same worked example as D-30: "میخ سایز ۳ برند اکس هر عدد ۲۰ تومن. میخ سایز ۳ برند الکس هر بسته ۲ میلیون تومن. میخ سایز ۳ اشترکوه هر عدد ۲۵ هزار تومن." - three price entries for essentially the same nail size, with two different pricing units (per-unit vs. per-package) appearing across them.
Alternatives Considered: (a) Pricing Unit as a single fixed product-level setting, consistent with the original Step 4 framing where Pricing Unit was introduced alongside Registration/Sale/Display Unit as one of four unit concepts without yet specifying its exact granularity. This was not explicitly rejected as wrong in Step 4 - it simply had not yet been resolved to this level of granularity - and was refined, not reversed, once the Step 6 nail example required a per-Price-Entry level of granularity to be representable at all.
Confidence: High - directly evidenced by a concrete worked example with no alternative explanation available.
Current Status: Confirmed and locked at Step 6, as a refinement (not a contradiction) of the Pricing Unit concept originally introduced in D-23.

===========================================================
END OF DECISION LOG.
Prepared through Phase 3, Step 6 of the approved 11-step execution plan. Decisions for Steps 7 through 11 (phone-order Business Process modeling, ancillary cost detailed modeling, invoice/pre-invoice modeling, Bounded Context map, final review pass) do not yet exist and are not included, as those steps have not yet been executed.
===========================================================

===========================================================
PART 5 BEGINS BELOW (UBIQUITOUS LANGUAGE, FULL, UNABRIDGED)
===========================================================

UBIQUITOUS LANGUAGE
HyperAhan - Catalog and Pricing Modules
Complete glossary of every business concept established through Phase 3, Step 6.
Format per entry: Concept / Synonyms / Meaning / Relationships / Business Owner / Bounded Context.
"Business Owner" below means the business-side role/actor who defines, controls, or is authoritative for this concept (Admin, Customer, System/Backend, or the business owner Arash/Foolad Shahyar Sepand as the source of the rule itself) - not a person's job title in the org chart.
Bounded Context values used: Catalog, Pricing, Order (deferred/out of current design scope but referenced), Ancillary Cost (deferred), Invoice (deferred), Calculator. Where a Bounded Context has not yet been formally assigned (Step 10 not yet executed), this is marked "Not yet assigned (Step 10 pending)."

===========================================================

Concept: Product
Synonyms: محصول
Meaning: The single, generic aggregate representing any sellable item in the catalog, regardless of product family (steel or otherwise). Not modeled as one type per family.
Relationships: Has one Specification. Has a Sale Mode. Has an Out-of-Stock Display Setting. Has a Registration Unit, a Sale Unit, and optionally Conversion Factors. Has one or more Price Entries. May be linked to a Fabrik/Persi counterpart as an independent variant.
Business Owner: Admin (registers and configures the product); Business Owner/Arash (defines that the model must support broad, diverse product registration).
Bounded Context: Catalog.

===========================================================

Concept: Specification
Synonyms: مشخصات محصول, فیلد ساختاریافته, Specification VO
Meaning: A typed, category-dependent structure attached to a Product, holding price-making and descriptive variables (size, thickness, length, grade, factory/brand, etc.). Fields are optional/lightweight by default so simple products are not burdened with unneeded structure, while complex products can use the full structured model.
Relationships: Belongs to exactly one Product. Its values (in combination) form the key for a Price Entry. Feeds the Pricing Formula as input. Contains Price-Making Variables as a subset of its fields.
Business Owner: Admin (defines which Specification fields apply per category); Business Owner/Arash (mandated that these fields be structured, not free text).
Bounded Context: Catalog.

===========================================================

Concept: Price-Making Variable
Synonyms: متغیر اصلی و قیمت‌ساز, متغیر قیمت‌ساز
Meaning: The subset of Specification fields that directly determine price and are used for catalog filtering - explicitly: size, thickness, length, grade, manufacturing factory.
Relationships: Subset of Specification. Together (in full combination) they form the key that identifies a specific Price Entry.
Business Owner: Business Owner/Arash (Document A, Section 6-1).
Bounded Context: Catalog / Pricing (spans both - structural definition lives in Catalog, but its purpose is entirely pricing-driven).

===========================================================

Concept: Sale Mode
Synonyms: نحوه فروش, دسته یک/دو/سه (Category 1/2/3), Sale Category
Meaning: A required attribute of every Product with exactly three defined values: (1) Fully Online - price shown, cart purchase available; (2) Semi-Custom - a Base Size is priced and purchasable online by default, any deviation requires a phone call; (3) Fully Custom/Contact-Only - price is never predetermined, always requires a phone call; still shown in the public catalog without price, with a contact-expert button.
Relationships: Independent of Inventory/Out-of-Stock Display Setting (must never be merged with it, per explicit confirmation). Determines default catalog UI (add-to-cart vs. contact button vs. admin-only). Category 2 relates directly to Base Size.
Business Owner: Business Owner/Arash (defines the three categories); Admin (assigns the value per product).
Bounded Context: Catalog.
Open item: whether this is a strictly closed 3-value enum or extensible in the future is unresolved (low priority, OPEN-4).

===========================================================

Concept: Base Size
Synonyms: سایز پایه
Meaning: For Semi-Custom (Sale Mode category 2) products such as I-beam, the standard/default size (e.g., a 12-meter bar) on which the default online price is based and which is purchasable online without a phone call.
Relationships: Belongs to a Product with Sale Mode = Semi-Custom. Any Specification value combination deviating from the Base Size triggers the Custom/Contact path instead of the online path.
Business Owner: Business Owner/Arash (I-beam example, Document A); confirmed general pattern applies to any product with a recognized standard size.
Bounded Context: Catalog.

===========================================================

Concept: Custom Size / Custom Order (in the Sale Mode sense)
Synonyms: سایز سفارشی, برش خاص, طول متفاوت
Meaning: Any deviation from a product's Base Size (for Semi-Custom products) or, more generally, any order that does not fit the standard online-purchasable configuration; requires phone contact rather than online checkout.
Relationships: Opposite/complement of Base Size within Sale Mode category 2. Also generally describes the entirety of Sale Mode category 3 products.
Business Owner: Business Owner/Arash.
Bounded Context: Catalog / Order (the custom fulfillment itself belongs to Order, which is out of current design scope).

===========================================================

Concept: Out-of-Stock Display Setting (Inventory Display Policy)
Synonyms: وضعیت موجودی, تگ ناموجود, ابهام "تماس بگیرید" برای ناموجودی
Meaning: A per-Product, admin-configurable setting controlling how an out-of-stock product appears in the catalog. Exactly three possible states: (1) fully hidden from the catalog, (2) shown with an "out of stock" tag and no action button at all, (3) shown with a "contact expert" action button.
Relationships: Fully independent from Sale Mode - must never be collapsed into it, even though state 3 and Sale Mode category 3 can both surface a visually similar "contact" button in the UI, from two different underlying fields. State 3's business rationale: lets the admin offer a substitute product or a quote from another supplier.
Business Owner: Admin (sets the value per product); Business Owner/Arash (defined the three-state resolution after the original document's internal contradiction was clarified).
Bounded Context: Catalog.

===========================================================

Concept: Call Path A
Synonyms: مسیر الف
Meaning: One of two phone-order fulfillment paths. Admin builds the entire order in the system on the customer's behalf and issues a payment link/notification; the customer logs in, reviews, and pays.
Relationships: Alternative to Call Path B. Both occur after a phone conversation where Admin determines final price including any Ancillary Cost. Requires the "admin builds order from scratch" capability.
Business Owner: Admin (executes this path); Business Owner/Arash (defined the two-path model, Document A Section 2).
Bounded Context: Order (deferred from current design scope; documented for future modeling in Step 7).

===========================================================

Concept: Call Path B
Synonyms: مسیر ب
Meaning: The second phone-order fulfillment path. The entire transaction (agreement and payment) happens manually, outside the system; admin later enters the order into the system solely for reporting/statistics purposes.
Relationships: Alternative to Call Path A. General principle governing when either path is used: whatever can be automated should happen on the website; phone paths are reserved for negative-feedback cases or genuinely complex orders.
Business Owner: Admin; Business Owner/Arash.
Bounded Context: Order (deferred).

===========================================================

Concept: Admin-Built Order / Order From Scratch
Synonyms: ساخت سفارش از صفر, ثبت سفارش توسط ادمین
Meaning: The capability for an admin to create a brand-new order directly in the system - not limited to managing customer-submitted orders - including product selection, manual pricing outside standard tiers if needed, adding Ancillary Costs, and issuing a Payment Link.
Relationships: Required by both Call Path A and Call Path B. Distinguishes this admin panel from a typical order-management-only admin panel.
Business Owner: Admin; Business Owner/Arash (explicit design implication in Document A Section 2).
Bounded Context: Order (deferred).

===========================================================

Concept: Payment Link
Synonyms: لینک پرداخت, نوتیفیکیشن پرداخت
Meaning: A link or notification issued to the customer (in Call Path A) allowing them to log in, view the order and its price, and complete payment.
Relationships: Output of the admin-built-order process under Call Path A. Its underlying payment mechanism/gateway is unspecified and out of scope for domain modeling as of this document.
Business Owner: Admin (issues it); Business Owner/Arash (mandates its existence).
Bounded Context: Order (deferred).

===========================================================

Concept: Fabrik
Synonyms: فابریک
Meaning: One of two fully independent products/variants (the other being Persi) - e.g., "ribbed sheet, Fabrik type." Not a shared field/value on a common product; a fully separate product identity.
Relationships: Independent of, but conceptually paired with, Persi. Both are instances of the generic Product aggregate. Exact mechanics of "independent variant" representation (fully separate records vs. lightweight cross-reference) is unresolved (OPEN-6, low priority).
Business Owner: Business Owner/Arash (Document A, Section 6-2).
Bounded Context: Catalog.

===========================================================

Concept: Persi
Synonyms: پرسی
Meaning: The counterpart to Fabrik - a fully independent product/variant, never merged into Fabrik as a shared attribute.
Relationships: Same as Fabrik (mirrored entry).
Business Owner: Business Owner/Arash.
Bounded Context: Catalog.

===========================================================

Concept: Market Slang Terms (general category)
Synonyms: اصطلاحات بازاری
Meaning: An umbrella term for market-usage naming conventions observed in real invoices that are not (yet) structured Specification fields. Includes Sabok/Sangin, Nimeh, and Loghme (each listed separately below). Stored only in the Product's display name unless a direct, formulable price effect is later proven, at which point the specific term is promoted to a structured field.
Relationships: Subset relationship to Specification (currently NOT part of it; candidate for future promotion into it). The complete list of such terms beyond the four named ones is not yet known (OPEN-3, low priority, explicitly deprioritized by the business owner).
Business Owner: Business Owner/Arash.
Bounded Context: Catalog.

===========================================================

Concept: Sabok / Sangin (Light / Heavy)
Synonyms: سبک / سنگین
Meaning: A weight-class descriptor for a product (e.g., "ناودانی ۸ - سنگین"), currently recorded only in the display name, not as a structured field.
Relationships: Instance of Market Slang Terms.
Business Owner: Business Owner/Arash.
Bounded Context: Catalog.

===========================================================

Concept: Nimeh (Half)
Synonyms: نیمه
Meaning: A version of a product with lower thickness/weight than the standard version (e.g., "نبشی نیمه ۵"), currently recorded only in the display name.
Relationships: Instance of Market Slang Terms.
Business Owner: Business Owner/Arash.
Bounded Context: Catalog.

===========================================================

Concept: Loghme (Cut Piece)
Synonyms: لقمه
Meaning: A cut/segmented product derived from a standard profile - a DERIVED product from a raw product, not a new raw material in its own right. Mostly weighed, sometimes counted.
Relationships: Instance of Market Slang Terms; conceptually derived FROM another (raw) Product, implying a possible parent-child relationship not yet formally modeled.
Business Owner: Business Owner/Arash.
Bounded Context: Catalog.

===========================================================

Concept: Tiered / Volume Pricing (Tier Discount)
Synonyms: قیمت‌گذاری پلکانی, پله قیمتی, تخفیف حجمی, پله تخفیف
Meaning: A discount mechanism where unit price depends on purchase volume (weight or count): smaller purchase = higher unit price, larger = lower. Calculated strictly per order line item (per-item), never on cart-wide totals. Recalculates live/automatically as cart quantity changes (never locked). Defined completely independently of the Pricing Formula and applied on top of the Formula's final output. Can be defined per product family with a global default that is overridable per category.
Relationships: Independent of Pricing Formula and Base Price Entry - operates as a distinct layer applied after them. Named by the business owner as the company's specific competitive advantage - this is the primary evidence for classifying Pricing as a Core Domain.
Business Owner: Business Owner/Arash (defines tiers as core competitive IP); Admin (configures tier boundaries via panel, without code changes).
Bounded Context: Pricing.
Open item: exact tier boundary values (thresholds, percentages) not yet finalized (OPEN-2, high priority per business owner, requires a dedicated future session).

===========================================================

Concept: VAT / Tax
Synonyms: مالیات بر ارزش افزوده, ارزش افزوده
Meaning: A 10% value-added tax embedded in all customer-facing prices. Two prices (with/without VAT) are never shown together. Backend always stores the pre-VAT base price separately; VAT is applied only at final calculation, as the last step after Tier Discount.
Relationships: Final layer in the price-calculation order of operations (Formula output x Base Price -> Tier Discount -> VAT). Accompanied by a mandatory fixed disclaimer text shown on all relevant pages.
Business Owner: Business Owner/Arash (Document A, Section 8).
Bounded Context: Pricing.

===========================================================

Concept: Price History Record
Synonyms: تاریخچه‌ی قیمت, رکورد قیمت
Meaning: Every change to a price creates a new, append-only record. Prior records are never deleted or edited. The system always uses the latest (non-obsolete) record for display and calculation. Admins can view the full historical report.
Relationships: Directly tied to Price Entry (each Price Entry has its own history over time). Interacts with the "no price lock" rule (D-29/Section 5.4) - the live-read price at any moment is whichever record is currently latest and non-obsolete.
Business Owner: Business Owner/Arash (Document A, Section 9); Admin (views historical reports, marks records obsolete).
Bounded Context: Pricing.

===========================================================

Concept: Obsolete Price Record
Synonyms: رکورد قیمت منسوخ, منسوخ‌شدن
Meaning: A distinct status a Price History Record can be given so that it is excluded from reports, WITHOUT being deleted. Explicitly a different state from deletion.
Relationships: A status/flag on a Price History Record. Does not affect the append-only, never-edited nature of the underlying record.
Business Owner: Admin (marks records obsolete); Business Owner/Arash (defined the distinction from deletion).
Bounded Context: Pricing.

===========================================================

Concept: Ancillary Cost
Synonyms: هزینه جانبی, کرایه حمل (its primary named example, freight/shipping cost)
Meaning: Any cost, such as freight, that is recorded as an independent line item in the order/invoice rather than being folded into the product's unit price. Currently entered manually by the admin (no automatic formula yet); the backend may return a default fixed number.
Relationships: Applied at the Order/Invoice level, separate from Product pricing. Part of the "business owner explicitly stated part of retail profit is embedded in this line" rationale for keeping it visible and separate.
Business Owner: Admin (enters the value manually); Business Owner/Arash (mandated the independent-line-item rule; the exact calculation formula is explicitly still undecided).
Bounded Context: Ancillary Cost (deferred subdomain, Supporting classification) / Order (deferred) / Invoice (deferred).
Open item: exact calculation formula (distance/weight/zone-based) not yet finalized (OPEN-1, high priority per business owner, requires a dedicated future session).

===========================================================

Concept: Registration Unit
Synonyms: واحد ثبت
Meaning: The unit in which a Product's Specification is defined/registered (e.g., kilograms for rebar).
Relationships: One of four independent unit concepts (with Sale Unit, Pricing Unit, Display Unit). Related to Sale/Pricing/Display units via a Conversion Factor when they differ. For Simple Products, this is the same as all other unit roles (no distinction needed).
Business Owner: Admin (chooses the unit a product is registered in).
Bounded Context: Catalog.

===========================================================

Concept: Sale Unit
Synonyms: واحد فروش
Meaning: The unit the customer orders in - weight, count/piece, or both simultaneously, depending on the product. Not a single global system-wide assumption; must be definable per product.
Relationships: One of four independent unit concepts. Related to the tiered-pricing "per-item" quantity used in Tiered/Volume Pricing calculations.
Business Owner: Business Owner/Arash (Document A, Section 4, rejecting the "everything in kg" default assumption); Admin (configures per product).
Bounded Context: Catalog / Order (Order use of the sale unit is deferred).

===========================================================

Concept: Pricing Unit
Synonyms: واحد قیمت‌گذاری
Meaning: The unit the base price is expressed in for a specific Price Entry (e.g., Toman per kilogram, Toman per unit, Toman per package). Confirmed to be a property of each individual Price Entry, NOT a single fixed setting applied uniformly across the whole product - the same product/size can have different Pricing Units across different brands/Price Entries (e.g., nails priced per-unit for one brand, per-package for another).
Relationships: One of four independent unit concepts. Lives on the Price Entry, not the Product itself. Directly used in the price-calculation order of operations (Formula output x Base Price, where Base Price is expressed per this unit).
Business Owner: Admin (chooses pricing unit per Price Entry); Business Owner/Arash (nail/rebar examples establishing this granularity).
Bounded Context: Pricing.

===========================================================

Concept: Display Unit
Synonyms: واحد نمایش
Meaning: The unit shown to the customer. Configured independently for two separate contexts: the Catalog view and the Invoice view. In the invoice specifically, a maximum of two units may be shown simultaneously, and in most cases only one unit is shown.
Relationships: One of four independent unit concepts. Independent of Registration/Sale/Pricing Unit - what is shown does not have to equal what is stored, sold-in, or priced-in. Related to Conversion Factor, which enables translating an underlying quantity into the chosen Display Unit (e.g., showing a piece count computed from total weight).
Business Owner: Admin (sets catalog display unit and invoice display unit(s) independently).
Bounded Context: Catalog (catalog display) / Invoice (invoice display, deferred subdomain).

===========================================================

Concept: Conversion Factor
Synonyms: ضریب تبدیل
Meaning: A fixed, stored numeric value relating two units on the same product (e.g., the weight of one rebar bar in kilograms), enabling conversion of total order weight into a piece count, or vice versa. Always a static stored number - never the output of the Pricing Formula or any dynamic/computed mechanism.
Relationships: Connects Registration/Sale/Pricing/Display Units when they differ for a given Product. Explicitly and deliberately kept independent of the Pricing Formula concept, even though both "compute a number from product data" superficially.
Business Owner: Admin (defines/edits the fixed value); Business Owner/Arash (confirmed it must always be a fixed number, not formula-derived).
Bounded Context: Catalog.

===========================================================

Concept: Simple Product
Synonyms: محصول ساده
Meaning: A product that uses a single unit throughout (Registration = Sale = Display = Pricing Unit are all the same), requiring no Conversion Factor and no multi-unit Specification complexity. Explicit examples: box, count/piece, carton, trailer-load, container.
Relationships: Opposite/complement of Complex Product. Both are represented by the same generic Product aggregate and the same (optional-by-default) Specification model - the difference is in how many of the optional unit-related fields are actually populated.
Business Owner: Business Owner/Arash (explicitly confirmed this category exists and must be supported without forcing full complexity).
Bounded Context: Catalog.

===========================================================

Concept: Complex Product
Synonyms: محصول پیچیده (implicit counterpart term, not separately named by the business owner but used in analysis to contrast with Simple Product)
Meaning: A product requiring the full four-unit distinction and Conversion Factor(s) - e.g., rebar, sheet, profile - where Registration, Sale, Pricing, and Display Units may all differ from one another.
Relationships: Opposite/complement of Simple Product.
Business Owner: Business Owner/Arash (implicit, via rebar/sheet/profile examples used throughout).
Bounded Context: Catalog.

===========================================================

Concept: Pricing Formula
Synonyms: فرمول قیمت‌گذاری, فرمول اختصاصی, فرمول
Meaning: A mechanism, either the system Default Formula or an Admin-Defined Custom Formula, that computes the effective consumed quantity or multiplier for a Product from its Specification values. Confirmed to NEVER compute final price directly - its output is always multiplied against the matching Base Price Entry to yield the pre-tax, pre-discount price. The same engine is reused to power the public Weight Calculator Tool.
Relationships: Consumes Specification values as input. Produces a quantity/multiplier consumed by Price Entry lookup. Upstream of Tier Discount and VAT in the price-calculation order of operations. Shared/reused by the Calculator bounded context (Upstream/Downstream relationship, formal Context Map entry pending Step 10).
Business Owner: Admin (defines custom formulas per product); Business Owner/Arash (requested this capability and confirmed no exceptions to the "never outputs final price" rule).
Bounded Context: Pricing (primary owner) / Calculator (downstream consumer).

===========================================================

Concept: Default Formula
Synonyms: فرمول پیش‌فرض
Meaning: The system-wide fallback pricing formula: Final price = Base Price x order quantity (count or weight) + N% tax.
Relationships: One of the two forms of Pricing Formula (the other being Custom Formula). Used automatically for any product without a defined Custom Formula.
Business Owner: Business Owner/Arash.
Bounded Context: Pricing.

===========================================================

Concept: Custom Formula
Synonyms: فرمول اختصاصی, فرمول تعریف‌شده توسط ادمین
Meaning: An admin-authored formula, specific to one product, computing the effective quantity/multiplier from that product's Specification values (e.g., computing sheet weight from length x width x thickness x density-like logic).
Relationships: Alternative to Default Formula. Reused by the Weight Calculator Tool. Exact authoring mechanism (expression syntax, technical implementation) is explicitly deferred to the technical design phase (OPEN-7) and not part of domain modeling.
Business Owner: Admin (authors it); Business Owner/Arash (requested the capability).
Bounded Context: Pricing.

===========================================================

Concept: Weight Calculator Tool
Synonyms: ماشین‌حساب وزن, جعبه ابزار محاسبه‌گر, ابزار محاسبه وزن عمومی
Meaning: A public-facing tool for site visitors to compute product weight from dimensional inputs. Its underlying formulas are standard, industry-wide (not business-specific), sourced identically from competitor reference documents. Explicitly reuses the exact same Formula engine used for internal product pricing. Its business purpose is to generate visitor traffic, not to express competitive pricing logic.
Relationships: Downstream consumer of the Pricing Formula engine (Upstream = Pricing). Classified as a Generic Domain in terms of its formula content, despite its technical dependency on the Core Pricing subdomain.
Business Owner: Business Owner/Arash (confirmed its purpose is traffic generation and confirmed formula-engine reuse).
Bounded Context: Calculator (Generic Domain classification; formally as a downstream consumer of Pricing - Context Map entry pending Step 10).

===========================================================

Concept: Price Entry (provisional name, not finalized)
Synonyms: قیمت پایه به‌ازای ترکیب مشخصات (no single fixed Persian term used by the business owner; "Base Price" and "قیمت واحد" used descriptively in examples), tentatively also referred to in earlier analysis as "ProductFactoryPrice" (SUPERSEDED naming, see Decision Log D-30)
Meaning: The pricing record tied to one specific, full combination of a Product's Specification values (including but not limited to factory/brand, and including size, thickness, etc.). Carries its own Pricing Unit. Subject to the append-only Price History Record model (never deleted/edited, can be marked Obsolete).
Relationships: Keyed by a full Specification-value combination. Multiplied by the Pricing Formula's output to yield pre-tax, pre-discount price. Has its own Pricing Unit (independent from other Price Entries of the same Product). Has its own Price History over time.
Business Owner: Admin (creates/edits price entries per combination); Business Owner/Arash (established the combination-keyed model via the rebar and nail examples).
Bounded Context: Pricing.

===========================================================

Concept: Factory / Brand
Synonyms: کارخانه سازنده, برند, کارخانه
Meaning: The manufacturer or brand of a specific product instance. Confirmed to be one dimension (among several, alongside size, thickness, etc.) of the full Specification combination that keys a Price Entry - NOT the sole determinant of price as originally assumed.
Relationships: Part of Specification. One of the Price-Making Variables. Co-determines, together with other Specification values, which Price Entry applies.
Business Owner: Business Owner/Arash (rebar example: Zob-Ahan vs. Abarkooh factories at different prices for the same size, and different prices for different sizes within the same factory).
Bounded Context: Catalog (as a Specification field) / Pricing (as a price-key dimension).

===========================================================

Concept: Minimum Order Quantity
Synonyms: حداقل مقدار قابل‌فروش, محدودیت حداقل سفارش
Meaning: A per-product threshold (by weight or count) below which the product cannot be ordered at all.
Relationships: A constraint associated with a Product's Sale Unit / order quantity; enforced presumably at Order time (deferred subdomain).
Business Owner: Business Owner/Arash (Document A, Section 7-2).
Bounded Context: Catalog (as a product-level constraint definition) / Order (as the enforcement point, deferred).

===========================================================

Concept: Pre-invoice
Synonyms: پیش‌فاکتور
Meaning: A preliminary invoice document issued to a customer, referencing then-current prices. Can become EXPIRED if the referenced product price(s) change before the order is finalized, per terms the customer has agreed to.
Relationships: Related to Invoice (a distinct but related document). Its expiration is triggered by Price History Record changes on referenced Price Entries. Exact trigger timing (immediate vs. checked only at finalization) is unresolved (deferred to Step 9).
Business Owner: Admin (issues it); Business Owner/Arash (confirmed the expiration mechanism and its link to the "no price lock" rule).
Bounded Context: Invoice (deferred Supporting subdomain, Step 9 pending).

===========================================================

Concept: Invoice
Synonyms: فاکتور
Meaning: The final billing document for a completed order. Displays prices inclusive of VAT, subject to the max-two-units display rule, and reflects Ancillary Cost as an independent line item.
Relationships: Downstream of Order, Pricing (final calculated price), Ancillary Cost, and Display Unit configuration.
Business Owner: Business Owner/Arash; Admin (configures invoice display unit(s)).
Bounded Context: Invoice (deferred Supporting subdomain, Step 9 pending).

===========================================================

Concept: Terms Agreement (Customer Agreement)
Synonyms: توافق‌نامه
Meaning: The agreement a customer accepts when placing an order, which includes acknowledgment that a Pre-invoice may expire if prices change, and that price changes mid-order are expected/accepted.
Relationships: Legal/procedural backing for the "no price lock" rule and Pre-invoice expiration behavior.
Business Owner: Business Owner/Arash; Customer (accepts it).
Bounded Context: Order (deferred) / Invoice (deferred).

===========================================================

Concept: Price Lock (REJECTED CONCEPT)
Synonyms: قفل قیمت
Meaning: A previously assumed mechanism (drawn from an ambiguous reading of Document A) whereby the base market price would be held constant to prevent momentary fluctuation. EXPLICITLY REJECTED AND CONFIRMED NOT TO EXIST by the business owner at Step 6. Retained in this glossary specifically as a rejected concept, per the instruction not to omit anything, and to prevent any future re-introduction of this assumption without cause.
Relationships: Directly superseded by the "always live price, pre-invoice expiration" model. See Decision Log D-29 for the full regression record.
Business Owner: Business Owner/Arash (explicitly rejected it: "قفل قیمت نداریم. این رو در نظر نگیر.").
Bounded Context: Pricing (as a rejected concept within this context).

===========================================================

Concept: Core Domain
Synonyms: زیردامنه هسته, هسته کسب‌وکار
Meaning: A DDD classification for a subdomain that carries the business's primary competitive differentiation. Confirmed to apply to both Catalog/Product-Variant and Pricing.
Relationships: Contrasted with Supporting Domain and Generic Domain.
Business Owner: Business Owner/Arash (explicitly confirmed both Catalog and Pricing are core).
Bounded Context: Applies at the subdomain-classification level, spanning Catalog and Pricing bounded contexts.

===========================================================

Concept: Supporting Domain
Synonyms: زیردامنه پشتیبانی
Meaning: A DDD classification for subdomains necessary for business operation but not a source of competitive differentiation. Confirmed (with medium-to-high, not directly re-verified, confidence) to apply to Order Management, Ancillary Cost Management, and Invoice/Pre-invoice Generation.
Relationships: Contrasted with Core Domain and Generic Domain.
Business Owner: Inferred by absence of competitive-advantage language; not yet independently re-confirmed word-for-word by the business owner.
Bounded Context: Applies to Order, Ancillary Cost, and Invoice bounded contexts (all deferred from current design scope).

===========================================================

Concept: Generic Domain
Synonyms: زیردامنه عمومی
Meaning: A DDD classification for a subdomain with standard, non-business-specific logic. Confirmed to apply to the Weight Calculator Tool, though its technical implementation is a downstream consumer of the Core Pricing subdomain's Formula engine.
Relationships: Contrasted with Core Domain and Supporting Domain.
Business Owner: Business Owner/Arash (implicitly, via confirming the tool's formulas are standard and its purpose is traffic generation).
Bounded Context: Calculator.

===========================================================

Concept: Modular Monolith
Synonyms: سیستم ماژولار مونولیتیک
Meaning: The overall system architecture style within which all modules (Catalog, Pricing, Order, Discount, Auth, Notification, Blog, etc.) are built as distinct modules inside a single deployable system, following Clean Architecture principles.
Relationships: The architectural container for all Bounded Contexts in this project.
Business Owner: Business Owner/Arash (explicit architectural instruction, Step 3 scope-narrowing message).
Bounded Context: N/A (this is the architectural style, not a bounded context itself); noted here as a technical/architectural fact per the instruction not to omit anything, even though it is technically a Technology-layer decision rather than a business/domain concept.

===========================================================

Concept: Clean Architecture
Synonyms: معماری کلین
Meaning: The internal layering/dependency-direction style mandated for each module within the Modular Monolith.
Relationships: Governs internal structure of Catalog and Pricing bounded contexts (and all others, once addressed).
Business Owner: Business Owner/Arash.
Bounded Context: N/A (architectural style).

===========================================================

Concept: Admin (Actor)
Synonyms: ادمین, پنل ادمین
Meaning: The internal staff role responsible for configuring products, Specifications, Sale Mode, Out-of-Stock Display Setting, Price Entries, Custom Formulas, Tier Discounts, Ancillary Costs, and for building phone-based orders from scratch.
Relationships: Primary actor across nearly every concept in Catalog and Pricing. Distinguished from Customer and System/Backend.
Business Owner: N/A (Admin IS a business owner role in the domain sense, acting on behalf of Foolad Shahyar Sepand).
Bounded Context: Spans Catalog, Pricing, and (deferred) Order, Ancillary Cost, Invoice.

===========================================================

Concept: Customer (Actor)
Synonyms: مشتری, کاربر
Meaning: The external party purchasing products, either online (Sale Mode: Fully Online, or the online portion of Semi-Custom) or via phone (Call Path A/B), subject to the Terms Agreement.
Relationships: Interacts with Product display (Sale Mode, Out-of-Stock Display), receives Payment Links, accepts Terms Agreement, receives Pre-invoices/Invoices.
Business Owner: N/A (Customer is an external actor, not a source of business rules in this conversation).
Bounded Context: Spans Catalog (as a viewer) and (deferred) Order, Invoice.

===========================================================

Concept: System / Backend (Actor)
Synonyms: بکند, سیستم
Meaning: The technical system itself, which always serves live price data (per the "no price lock" rule), computes Formula outputs, applies Tier Discounts and VAT, and can return a default fixed Ancillary Cost number.
Relationships: Executes the price-calculation order of operations across Pricing; serves Formula metadata to the frontend for the Calculator Tool (explicitly noted as a technical, not domain, detail).
Business Owner: N/A (technical actor).
Bounded Context: Spans Pricing and Calculator; underlies all bounded contexts technically.

===========================================================

END OF UBIQUITOUS LANGUAGE DOCUMENT.
Prepared through Phase 3, Step 6 of the approved 11-step execution plan. Terms belonging strictly to Steps 7-11 (formal Business Process actors/events for phone orders beyond what is captured here, detailed Ancillary Cost formula terms, formal Invoice/Pre-invoice field-level terms, and the full formal Context Map) will be added once those steps are executed; they are not yet part of this glossary because those steps have not yet occurred.

===========================================================
PART 6 BEGINS BELOW (PROJECT STATE, FULL, UNABRIDGED)
===========================================================

PROJECT STATE
HyperAhan - Catalog and Pricing Modules, DDD Business Analysis
Prepared as a resumption handoff. Any AI or analyst reading only this document, plus the accompanying Knowledge Base, Decision Log, and Ubiquitous Language documents, should be able to continue this project immediately without reading the original conversation transcript.

===========================================================
1. WORKING METHOD IN FORCE (must be preserved by whoever continues)
===========================================================

This project is run under a strict Senior DDD Architect / Business Analyst discipline, not a normal chat assistant mode. The following rules are currently active and must continue to be applied without exception:

- Reality over assumptions. Never invent business rules, workflows, entities, or technical decisions. Every conclusion must be traceable to explicit user statements or valid logical reasoning. Missing information must be stated as missing, never filled in from common industry practice unless explicitly requested.
- Every statement must be separated into: Confirmed Fact, Derived Conclusion, Unknown, Open Question. These are never merged.
- Contradictions, ambiguities, or duplicated concepts must stop the analysis and be raised as explicit questions before continuing.
- Business understanding must be complete before any architecture, database, API, or code decisions are made.
- No praise language, no filler, no markdown decoration in the analytical output style established so far (note: this current set of four deliverable documents uses structured headers for organization purposes, which is a reasonable exception for reference documents; the constraint against decorative/praise language in analytical prose remains in force).
- Phase discipline: Phase 1 (documentation collection) is CLOSED. Phase 2 (planning) is CLOSED and approved. Phase 3 (execution) is IN PROGRESS, one small step at a time, each step fully confirmed before moving to the next.
- Explicit regression checks are required before finalizing any step: every new fact must be checked against every previously confirmed fact; if a prior conclusion must change, this must be explicitly logged as "what changed / why it changed / which conclusion is now invalid" - never silently overwritten. Two such regressions have already occurred and are fully logged (see Section 4).
- Competitor-derived reference data (from the AhanOnline/MarkazAhan/EsfahanAhan documents) is NEVER auto-promoted to a business rule for Foolad Shahyar Sepand without explicit confirmation from the business owner. Exactly one such promotion has occurred so far (product domain scope breadth - see Section 4, Decision D-14 in the Decision Log).
- Current design scope is explicitly narrowed to the Catalog and Pricing modules only. Order, Discount, Authentication, Notification, and Blog modules exist in the overall system (a Modular Monolith with Clean Architecture) but are explicitly deferred to separate, later decisions. This narrowing was an explicit business owner instruction and remains in force for all subsequent steps.

===========================================================
2. DOCUMENT SET (what exists, and where authority lives)
===========================================================

Four reference documents together constitute the current state of this project:
1. This document (Project State) - where we are, what's next.
2. Project Knowledge Base (HyperAhan_Catalog_Pricing_KnowledgeBase.md) - full extraction of confirmed facts, business rules, invariants, terminology, unresolved questions, resolved contradictions, architectural decisions, and rejected assumptions, organized by domain.
3. Decision Log (HyperAhan_Decision_Log.md) - 31 individually logged decisions (D-01 through D-31), each with Decision ID, Decision, Reason, Business Evidence, Alternatives Considered, Confidence, Current Status. Includes two explicit regression/correction records (D-29 superseding an earlier price-lock conclusion, D-30 superseding an earlier factory-only price-keying conclusion).
4. Ubiquitous Language (HyperAhan_Ubiquitous_Language.md) - complete glossary of every business concept established so far, each with meaning, relationships, business owner (the business-side role authoritative for the concept), Bounded Context where already known, and synonyms. Includes one deliberately retained REJECTED concept (Price Lock) to prevent future re-introduction.

These four documents are the authoritative memory of this project. This Project State document does not repeat their full content; it summarizes structurally so a resuming agent knows where to look and what to do next.

===========================================================
3. SOURCE DOCUMENTS RECEIVED (Phase 1, CLOSED)
===========================================================

- Document A: Primary, authoritative internal business document from Foolad Shahyar Sepand (v1.0, 1405/05/01). The single source of truth for actual business rules. Contains its own explicitly flagged open ambiguities (freight formula, tier boundaries, an inventory-display contradiction later resolved, and a low-priority market-term list).
- Documents B, C, D, E, F, G (five separate documents in total, submitted across several messages): Competitor reference data extracted from AhanOnline, MarkazAhan, EsfahanAhan - covering category structures, weight-calculation formulas, unit tables, pricing display patterns, and calculator tool structures. Treated strictly as non-authoritative background knowledge UNLESS explicitly promoted by the business owner (this happened exactly once - product domain scope breadth).
- Business owner explicitly confirmed "documentation is complete" - this closed Phase 1 and authorized moving to Phase 2 (Planning).

===========================================================
4. PHASE 2: APPROVED EXECUTION PLAN (CLOSED, APPROVED, IN EXECUTION)
===========================================================

An 11-step plan was presented and explicitly approved by the business owner. The steps, and their current completion status, are:

1. Ubiquitous Language - COMPLETE. (Locked at Step 1; refined further at Steps 4-6 as new terms emerged - e.g., Price Entry, Conversion Factor, Simple/Complex Product were added during later steps and back-integrated into the glossary.)
2. Subdomains / Core vs. Supporting vs. Generic classification - COMPLETE. Catalog and Pricing = Core (explicitly confirmed by business owner). Order Management, Ancillary Cost, Invoice/Pre-invoice = Supporting (inferred, medium-high confidence, not separately re-verified word-for-word). Weight Calculator = Generic but downstream of Core Pricing engine.
3. Product aggregate modeling - COMPLETE. Product is a single generic aggregate (not one type per product family), carrying a category-dependent Specification. Confirmed directly by business owner given confirmed future expansion beyond steel (construction materials, nails, screws).
4. Unit / conversion model - COMPLETE. Four independent unit concepts (Registration, Sale, Pricing, Display), fixed-number-only Conversion Factor, explicit Simple-Product (single-unit) vs. Complex-Product (full model) distinction, Specification as typed-but-optional structure, invoice max-two-units display rule.
5. Sale-Mode vs. Inventory independence - COMPLETE. Resolved the original document's internal contradiction about out-of-stock display into a three-state, per-product admin setting, fully independent from the three-value Sale Mode attribute.
6. Pricing engine - COMPLETE. No price lock anywhere (live price through checkout, pre-invoice expiration on price change - this SUPERSEDED an earlier confirmed "price is locked" reading). Base price keyed by FULL Specification combination including but not limited to factory (this SUPERSEDED an earlier "per-factory-only" reading). Pricing Unit is itself a property of each individual Price Entry, not fixed per product. Two-tier formula system (Default vs. Custom), with the Custom Formula confirmed to NEVER compute final price directly - it always computes a quantity/multiplier that is then multiplied against the matching Price Entry. Tier Discount confirmed to be a fully independent layer applied on top of the Formula's final output.
7. Phone order process / admin manual order creation, modeled formally as a Business Process with Actors and Domain Events - NOT STARTED.
8. Ancillary costs, final detailed modeling - NOT STARTED (baseline facts already captured in Knowledge Base Section 3/4 and Ubiquitous Language, but no formal Step 8 pass has been executed).
9. Invoice / Pre-invoice modeling, including resolving the exact pre-invoice expiration trigger mechanics (immediate vs. checked-at-finalization) - NOT STARTED.
10. Bounded Contexts and Context Map - NOT STARTED. This step must formally resolve: (a) the relationship type between the two Core bounded contexts, Catalog and Pricing (expected to be a close/Partnership-style relationship rather than typical Core-to-Supporting distance, per the reasoning already logged in Decision D-16, but not yet formally drawn); (b) the Upstream(Pricing)/Downstream(Calculator) relationship for the shared Formula engine (per Decision D-18/D-20); (c) formal context boundaries for Order, Ancillary Cost, and Invoice even though their detailed internal modeling is deferred.
11. Final full-model review pass against all confirmed facts, plus a final list of remaining low-priority open items - NOT STARTED.

===========================================================
5. EXACTLY WHERE WE STOPPED
===========================================================

Steps 1 through 6 of the approved 11-step plan are COMPLETE, LOCKED, and have passed explicit regression checks (requested and performed twice during this project, after Step 4 and after Step 5, both confirming no unresolved conflicts among Steps 1-4 and Steps 1-5 respectively; Step 6 itself introduced two explicit, fully-logged regressions against earlier steps, not a regression failure - see Decision Log D-29 and D-30).

Immediately after Step 6 was locked, the business owner (via an instruction framed as a "Behavior Reset" / consistency-check request, and separately via explicit regression-check requests) did NOT provide new business content but instead requested four deliverable documents be produced from everything established so far:
1. Project Knowledge Base - DELIVERED.
2. Decision Log - DELIVERED.
3. Ubiquitous Language - DELIVERED.
4. This Project State document - IN PROGRESS (being delivered now).

No new business facts, rules, or decisions have been introduced since the close of Step 6. All four deliverables are extractions/reorganizations of Steps 1-6 content; they do not represent new analysis.

===========================================================
6. LOCKED DECISIONS (quick-reference index; full detail lives in the Decision Log)
===========================================================

All of the following are LOCKED (confirmed, not subject to re-litigation unless new business evidence emerges):
- D-01: Sale Mode, three values, required per product.
- D-02: Category 3 products appear in public catalog without price, with contact button.
- D-03: Admin can build orders from scratch (Path A/B support).
- D-04: Sale Unit is per-product, not globally fixed to kilograms.
- D-05: Ancillary cost (freight) as independent line item, manual entry currently, formula still open (OPEN-1).
- D-06: Size/thickness/length/grade/factory must be structured fields.
- D-07: Fabrik and Persi are independent products/variants, never a shared field.
- D-08: Other market slang terms stay in display name only, unless proven price-relevant.
- D-09: Tier discount is per-item, live/unlocked, never per-cart-total.
- D-10: VAT included in displayed price, no dual display, disclaimer text required.
- D-11: Price history is append-only; obsolete is a distinct state from deleted.
- D-12: Out-of-stock display is a per-product, three-state admin setting (hidden / tag-no-action / contact-button).
- D-13 / D-28: Sale Mode and Out-of-Stock Display are two independent Product-level fields, never merged.
- D-14: Product domain scope = competitor breadth + future non-steel expansion (explicitly confirmed, not inferred).
- D-15: Product is a single generic aggregate, not one type per product family.
- D-16: Catalog AND Pricing are both Core Domains (explicitly confirmed).
- D-17: Order, Ancillary Cost, Invoice are Supporting Domains (inferred, not separately re-verified).
- D-18: Weight Calculator is Generic but downstream of the Core Pricing Formula engine.
- D-19: Current design scope narrowed to Catalog + Pricing only; Modular Monolith, Clean Architecture confirmed as the overall system style.
- D-20: Admin-defined Custom Formula exists, reused by the public Calculator tool.
- D-21: Formula never computes final price directly, no exceptions identified; always feeds into a Price Entry lookup.
- D-22: Tier Discount is independent of Formula, applied on the Formula's final output.
- D-23: Four independent unit concepts confirmed (Registration/Sale/Pricing/Display).
- D-24: Conversion Factor is always a fixed stored number, never formula-derived.
- D-25: Simple, single-unit products are explicitly supported (box, count, carton, trailer, container).
- D-26: Specification is typed but optional-by-default.
- D-27: Invoice display allows max two units, usually one; catalog and invoice display units configured independently.
- D-29 (SUPERSEDES prior conclusion): NO price lock anywhere; live price through checkout; pre-invoice expires on price change per Terms Agreement.
- D-30 (SUPERSEDES prior conclusion): Base price ("Price Entry") is keyed by the FULL Specification combination, not factory alone.
- D-31: Pricing Unit is a property of each individual Price Entry, not fixed per product.

Two decisions carry lower confidence and are explicitly flagged as such (see Decision Log for full detail, not repeated here): D-17 (Supporting Domain classification, inferred) and the specific synthesis language of D-26 (typed-but-optional Specification, the analysis team's own reconciliation framing).

===========================================================
7. OPEN QUESTIONS NOT YET RESOLVED (do not assume answers; ask if needed)
===========================================================

- OPEN-1 (high priority per business owner): exact freight/ancillary-cost calculation formula (distance/weight/zone basis) - requires a dedicated future session with the business owner.
- OPEN-2 (high priority per business owner): exact tiered-discount boundary values (thresholds, percentages, per product family) - explicitly called out as competitive-advantage material requiring a dedicated session.
- OPEN-3 (low priority, explicitly deprioritized): complete list of market-slang terms beyond Fabrik/Persi/Loghme/Nimeh/Sabok-Sangin.
- OPEN-4 (low priority): whether Sale Mode is a strictly closed 3-value enum or extensible in the future.
- OPEN-5 (deferred to Step 9 by design): exact pre-invoice expiration trigger mechanics (immediate on price change vs. checked only at order finalization).
- OPEN-6 (deferred, low priority): exact mechanics of how Fabrik/Persi are represented as "independent variants" of the same generic Product aggregate (fully separate records vs. lightweight cross-reference).
- OPEN-7 (explicitly technical, deferred to technical design phase, NOT a domain-modeling question): Stimulsoft-based invoice/pre-invoice rendering pipeline (backend renders to image/base64, frontend renders/downloads); frontend fetching of calculator metadata (name, fields, types, formula) with a caching requirement. These were volunteered by the business owner as pre-existing technical decisions and explicitly logged rather than incorporated into domain modeling, per the "business before technology" rule.
- OPEN-8: Any question belonging to Steps 7-11 that has not yet been asked. These are NOT YET KNOWN because those steps have not been executed - a resuming agent must generate and ask these questions when reaching each step, not assume they are already answered.

===========================================================
8. CURRENT STEP
===========================================================

There is no active, in-progress analytical step right now. Step 6 is fully closed and locked. The most recent activity (production of the four reference documents) was a documentation/handoff task, not a new Phase 3 step, and is now also complete.

The project is in a PAUSED-BETWEEN-STEPS state, ready to resume at Step 7.

===========================================================
9. NEXT STEP
===========================================================

Step 7: Model the phone order process and admin-built manual order creation as a formal Business Process, with explicit Actors (Admin, Customer, System) and Domain Events.

Before beginning Step 7, the resuming agent should:
1. Re-state the working method (Section 1 of this document) to the business owner if there is any doubt it is still in force, OR simply continue applying it silently if context is already established (consistent with how Steps 2-6 proceeded without needing to re-explain the method every time).
2. Ground itself fully in Section 4 (Document A, Sections 2 and 7-2) and the corresponding Knowledge Base/Ubiquitous Language entries for Call Path A, Call Path B, Admin-Built Order, Payment Link, and Minimum Order Quantity before asking any new questions.
3. Identify what is genuinely still unknown about this process (e.g., exact Domain Events such as "OrderCreatedByAdmin," "PaymentLinkIssued," "OrderRecordedForReporting" are NOT yet confirmed by the business owner in this exact form - these are analytical hypotheses to propose and confirm, not facts to assume) before proposing a model.
4. Apply the same rigor as Steps 1-6: propose, cite evidence, list alternatives considered, flag anything uncertain, and stop to ask before locking anything that depends on an unconfirmed assumption.

===========================================================
10. REMAINING ROADMAP (Steps 7-11, in order)
===========================================================

Step 7 (next): Phone order process and admin manual order creation - Business Process, Actors, Domain Events. Must integrate Call Path A, Call Path B, Payment Link, and the "admin builds order from scratch" capability already confirmed in Steps 1-2.

Step 8: Ancillary cost, final detailed modeling. Must integrate the already-confirmed rule (independent line item, manual entry, possible fixed backend default) and explicitly flag that the calculation formula itself remains OPEN-1, not to be invented at this step.

Step 9: Invoice / Pre-invoice modeling. Must resolve OPEN-5 (exact pre-invoice expiration trigger timing) and formally model the Invoice's relationship to Price Entry, Ancillary Cost, VAT, and the max-two-units Display Unit rule (D-27).

Step 10: Bounded Contexts and Context Map. Must formally decide and diagram: Catalog-Pricing relationship type (expected Partnership given both are Core, per D-16 reasoning); Pricing-Calculator Upstream/Downstream relationship (per D-18/D-20); placement and relationship of Order, Ancillary Cost, and Invoice contexts relative to the two Core contexts, even though their internal detail is deferred; and explicit confirmation of which team/module owns which context if relevant to the business.

Step 11: Final full-model review pass. Must re-verify the entire model (all of Steps 1-10) against every confirmed fact for internal consistency (as was done via the two regression checks already performed after Steps 4 and 5, but now across the complete model), and produce a final, explicit list of remaining open items - distinguishing which are safe to leave open indefinitely (e.g., OPEN-3, OPEN-4, OPEN-6) versus which genuinely block moving into technical/architectural design (e.g., OPEN-1 and OPEN-2, both explicitly marked high priority by the business owner and both requiring a dedicated session before final commercial launch, even if they do not block further domain modeling in the meantime).

After Step 11 is complete and confirmed, Phase 3 (Execution) of the DDD/BA engagement, as originally scoped to the Catalog and Pricing modules, will be finished. Any move into technical architecture, database schema, or API design must not begin until Step 11 is explicitly confirmed complete by the business owner, per the working method's "business first, technology second" rule that has governed this entire engagement.

===========================================================
END OF PROJECT STATE.
===========================================================
END OF HANDOVER PACKAGE.
